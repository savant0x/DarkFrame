/** FID-20260906-012 P0: Tailwind v4 runs through @tailwindcss/postcss (the `tailwindcss` plugin was v3-only). CJS export for Next's PostCSS loader. */
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
