# FID-20260908-013: Dead-kit retirement — delete `components/ui` styling slabs + `components/transitions`, uninstall framer-motion

<!--
  ECHO Protocol v0.1.2 (single-agent) — FID. No attribution fields.
-->

**Filename:** `FID-20260908-013-dead-kit-retirement.md`
**ID:** FID-20260908-013
**Severity:** MEDIUM (bundle weight + dual design systems; no functional defect)
**Status:** converged
**Created:** 2026-09-08

---

## 1. Summary

The Phase 6 census recorded 24 kit-importing files; the fresh RED census falsifies that number — **26 files** still import `components/ui`, including two **direct framer-motion consumers FID-010 missed** (`CreateClanModal`, `JoinClanModal`), plus the two `transitions` consumers (`ClanLeaderboardPanel/View`) already on record. Once styling consumers reach zero, `components/ui` styling slabs and `components/transitions` are deleted, `lib/animations.ts` + `lib/microInteractions.ts` + `lib/designTokens.ts` (framer-dependent, zero external consumers) are deleted, and framer-motion is removed from `package.json` — dropping it from the webpack graph entirely.

## 2. Evidence (RED)

### 2.1 Styling-kit consumers (must reach 0 before deletion)

| File | Symbols |
| ---- | ------- |
| components/BankPanel.tsx | Panel, Button, Badge, Card, Divider |
| components/ErrorBoundary.tsx | Button, Card, Panel |
| components/ClanLeaderboardPanel.tsx | Button, Input, Badge, Divider (+ StaggerChildren/Item) |
| components/ClanLeaderboardView.tsx | Button, Input, Badge, Divider (+ StaggerChildren/Item) |
| components/chat/ChatPanel.tsx | Button, Badge, Input |
| components/chat/ChatMessage.tsx | Badge |
| components/admin/ModerationPanel.tsx | Panel, Button, Badge, Input |
| components/admin/ClanInspectorModal.tsx | Button, Input, Badge, Divider |

`app/layout.tsx` (ConfirmDialogHost) + 15 `confirmDialog` consumers + `ProfileView`/`ClanManagementView` (RichTextEditor) are **token-adjacent retentions**, not styling debt.

### 2.2 framer-motion graph (5 source files + 5 lib files)

| File | Usage |
| ---- | ----- |
| components/transitions/StaggerChildren.tsx | `motion.div` stagger wrappers |
| components/transitions/LoadingSpinner.tsx | `motion` spinner |
| components/transitions/PageTransition.tsx | `motion` page fade |
| components/clan/CreateClanModal.tsx | `AnimatePresence` + backdrop/modal fade-scale (FID-010 gap) |
| components/clan/JoinClanModal.tsx | `AnimatePresence` + expandable filters height animation (FID-010 gap) |
| lib/animations.ts | `tapScale/hoverLift/slideUp/scaleIn` — consumed ONLY by ui/Button, ui/IconButton, ui/Card, ui/Alert, ui/StatCard |
| lib/microInteractions.ts, lib/designTokens.ts | zero source consumers |

### 2.3 Deletion set

- `components/transitions/` (4 files)
- `components/ui/` styling slabs: Alert, Badge, Button, Card, Divider, IconButton, Input, Panel, ProgressBar, Skeleton, StatCard (11 files)
- `components/ui/index.ts` → re-export **only** `ConfirmDialog` + `RichTextEditor` (retained per FID-009/FID-011 audit rulings)
- `lib/animations.ts`, `lib/microInteractions.ts`, `lib/designTokens.ts`
- `framer-motion` dependency (direct dep, `^12.23.24`, npm + package-lock)

## 3. Impact Analysis

- **Affected:** 10 consumer files migrated to token primitives before deletion; 2 clan modals de-framer'd. Bundle sheds framer-motion (~30-50KB gzip) + the dead kit's CSS-in-Tailwind payload.
- **Blast radius:** no behavior change — confirm flows, chat, bank, admin, leaderboards byte-preserved except styling containers. `package-lock.json` regenerated by npm.

## 4. Five Questions

| Question | Answer |
| -------- | ------ |
| ALL cases? | Yes — census re-run post-deletion must show 0 imports of ui-slabs/transitions/framer-motion |
| Scales? | Yes — deletion is terminal; ESLint no-restricted-imports guard proposed in §8 |
| Hostile attacker? | N/A (build hygiene); ConfirmDialog retention keeps the audited confirm flow |
| Maintainable? | Yes — single design system (neon-noir tokens) remains |
| Industry standard? | Yes — dead-code retirement after full migration |

## 5. Proposed Fix (GREEN)

1. **Kit symbol migrations** (mapping from prior FIDs): `Card`/`Panel`→`nn-panel`, `Button`→`nn-btn`/`nn-abtn` family, `Input`→`nn-input`, `Badge`→`nn-chip`, `Divider`→`nn-divider`. Logic byte-preserved.
2. **Stagger wrappers** (leaderboard pair): `StaggerChildren className="space-y-3"`→`<div className="space-y-3">`, `StaggerItem`→`<div className="nn-fade" key=…>`.
3. **Framer modals**: `AnimatePresence`+`motion.div`→plain conditional nodes with `nn-fade` (entrance only; consistent with all previously migrated modals); JoinClan's height-animated filter panel → conditional render + `nn-fade`. Both modals' gradient slabs → `nn-panel--violet` (they were missed by FID-010's kit pass — no kit imports there, only framer).
4. **Deletions** per §2.3; `npm uninstall framer-motion`.
5. **Bundle proof chain:** tsc 0 (no dangling imports) + `grep -r "framer-motion\|components/transitions" app/ components/ lib/` = 0 + `npm ls framer-motion` = empty/not found.

**Verification plan:** tsc 0; eslint 0 (touched files); vitest full; rubric greps; deletion census.

## 6. Audit Record

| Method | What was checked | Evidence | Result |
| ------ | ---------------- | -------- | ------ |
| Method 1: static analysis | tsc / eslint / vitest / deletion census / npm ls | *(pasted at implementation)* | pass |
| Method 2: manual re-read | consumer-by-consumer logic diff = 0; retention boundary (ConfirmDialog/RichTextEditor) verified intact | *(pasted at implementation)* | pass |

- Audit outcome: PASS → `converged`.

## 7. Implementation Record

- **Status:** complete
- **Changes applied:**
  - **8 styling-kit consumers migrated by hand** to token primitives (mapping per §5.1): BankPanel, ErrorBoundary, ClanLeaderboardPanel, ClanLeaderboardView, chat/ChatPanel, chat/ChatMessage, admin/ModerationPanel, admin/ClanInspectorModal. All fetch/submit/confirm/action logic byte-preserved. Operator-mandated hand migration (no scripted bulk rewrite): each file's diff was re-read against HEAD after migration, which surfaced and fixed real defects the interim automated pass had introduced — 42 duplicate `className` attributes, silently **dropped dynamic variant ternaries** (5 files: active page/pagination/tab/badge states lost their highlight), un-gated `animate-spin`, doubled class fragments. All corrected by hand before gates ran.
  - **ClanLeaderboard pair:** Stagger wrappers → structural `div` children with `space-y-3` (§5.2, no entrance animation — list content is server-fetched).
  - **CreateClanModal / JoinClanModal de-framer'd by hand** (§5.3): AnimatePresence/motion → plain conditional nodes with gated `nn-fade`; gradient slabs → token void; filter expand → conditional render. FID-010's miss corrected. Honest note: the first hand pass left structural breakage in JoinClanModal (a stray `</motion.div>` closer, the AnimatePresence wrapper's closer mis-mapped to a div, one surplus `</div>`) — caught by TS parse diagnostics after tsc flagged TS17002, root-caused by tag-stack walk against the git diff, and fixed; final file parses clean.
  - **Deleted:** `components/transitions/` (4 files); `components/ui/` styling slabs (11 files: Alert, Badge, Button, Card, Divider, IconButton, Input, Panel, ProgressBar, Skeleton, StatCard); `lib/animations.ts`, `lib/microInteractions.ts`, `lib/designTokens.ts`. `components/ui/index.ts` now exports only `ConfirmDialog` (+`confirmDialog`/`ConfirmOptions`) and `RichTextEditor`.
  - **Uninstalled framer-motion** (`npm uninstall framer-motion`): removed from package.json deps + lockfile regen.
- **Gates:** tsc 0 · eslint 0 (touched files) · full vitest **362 passed / 0 failed / 1 skipped** · deletion census: 0 `components/transitions` imports, 0 styling-slab imports (`Button|Card|Panel|Badge|Input|Divider|StatCard|Skeleton|Alert|IconButton|ProgressBar` from ui), 0 `framer-motion` in source · `npm ls framer-motion` = (empty) — dependency removed from graph.
- **Audit Method 2:** per-consumer logic diff = 0 (fetch/submit/confirm flows untouched); retention boundary verified — `ConfirmDialog.tsx` + `RichTextEditor.tsx` live in `components/ui/`, exported via the slimmed barrel, styled on tokens (ConfirmDialog's one `bg-glass-darker` chrome hit migrated to a token glass color during this pass).

## 8. Closure

- **Gates:** [x] typecheck 0 · [x] lint 0 · [x] tests pass · [x] deletion census 0 · [x] dependency uninstalled
- **Commit hash (G2):** pending — agent prepares, operator commits
- **Staging plan:** `git add -A -- components/ui components/transitions lib/animations.ts lib/microInteractions.ts lib/designTokens.ts components/BankPanel.tsx components/ErrorBoundary.tsx components/ClanLeaderboardPanel.tsx components/ClanLeaderboardView.tsx components/chat/ChatPanel.tsx components/chat/ChatMessage.tsx components/admin/ModerationPanel.tsx components/admin/ClanInspectorModal.tsx components/clan/CreateClanModal.tsx components/clan/JoinClanModal.tsx package.json package-lock.json dev/fids/FID-20260908-013-dead-kit-retirement.md dev/session-summaries/SESSION-2026-09-08-002.md`
- **Follow-through (proposed, not executed):** ESLint `no-restricted-imports` guard for `framer-motion`, `components/transitions`, and the deleted slab paths; next `npm run build` confirms the webpack graph is framer-free in artifacts.
