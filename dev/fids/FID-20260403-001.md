# FID-20260403-001: Code Review & Project Health Fix

**Status:** COMPLETED  
**Priority:** HIGH  
**Complexity:** 4/5  
**Created:** 2026-04-03  
**Completed:** 2026-04-03T23:25:00  
**Estimated:** 2-3h  
**Actual:** ~1.5h

---

## 📋 Summary

Comprehensive code review and health fix for DarkFrame project after ~2 years of inactivity. The codebase compiles with 0 TypeScript errors but has multiple breaking issues, security vulnerabilities, dead code, and lint failures that prevent local development and deployment readiness.

---

## 🎯 Description

The project was built over 2 years ago and never deployed. A full audit revealed:
- **4 breaking issues** that prevent the app from running correctly
- **25 npm vulnerabilities** including 1 critical (RCE in Next.js)
- **10 dead component files** (`_OLD` suffix) never imported
- **80 ESLint errors** + **71 warnings**
- **8 failing test suites** (42 failing tests out of 335)
- **Missing authentication** on battle API endpoints

---

## ✅ Acceptance Criteria

1. [ ] `npm run dev` starts without errors (Next.js + Socket.io)
2. [ ] `npx tsc --noEmit` returns 0 errors
3. [ ] `npm run lint` returns 0 errors and 0 warnings
4. [ ] `npm test` passes all test suites (0 failures)
5. [ ] `npm audit` returns 0 critical/high vulnerabilities
6. [ ] Battle API has authentication middleware
7. [ ] All dead code removed (`_OLD` files, unused deps)
8. [ ] `proxy.ts` → `middleware.ts` (auth protection functional)
9. [ ] Server entry point uses `server.ts` (background jobs active)
10. [ ] React version mismatch resolved

---

## 🏗️ Implementation Approach

### Phase 1: Breaking Issues (Priority: Critical)
Fix 4 issues that prevent the app from running:
1. **Server entry point**: Update `package.json` scripts to use `server.ts` instead of `server.js`
2. **Middleware rename**: Rename `proxy.ts` → `middleware.ts` so Next.js recognizes it
3. **React version mismatch**: Downgrade `react-konva` from `^19.0.10` to `^18.x` (or upgrade React to 19)
4. **npm audit**: Run `npm audit fix` and manually patch remaining vulnerabilities

### Phase 2: Dead Code Cleanup (Priority: Medium)
Remove unused files and dependencies:
1. Delete 10 `_OLD` component files
2. Remove `string-similarity` from `package.json` (deprecated, never imported)
3. Delete `DarkFrame.zip` from project root (1GB)

### Phase 3: ESLint Errors (Priority: Medium)
Fix all 80 errors + 71 warnings:
1. **80 unescaped entity errors**: Replace `"` → `&quot;` and `'` → `&apos;` in JSX
2. **71 `react-hooks/exhaustive-deps`**: Add missing dependencies or wrap in `useCallback`/`useMemo`

### Phase 4: Security - Battle API Auth (Priority: High)
Add authentication to unprotected battle endpoints:
1. `app/api/battle/attack/route.ts` — add auth middleware
2. Verify all combat-related routes are protected

### Phase 5: Test Fixes (Priority: Medium)
Fix 8 failing test suites:
1. Diagnose root causes (likely mock/setup issues from dependency changes)
2. Fix test assertions and mocks
3. Verify all 335 tests pass

---

## 📁 Files to Modify

| File | Action | Purpose |
|------|--------|---------|
| `package.json` | MOD | Fix server entry, React version, remove dead deps, audit |
| `proxy.ts` | RENAME → `middleware.ts` | Restore auth protection on `/game` routes |
| `server.js` | DEL | Replace with `server.ts` as entry point |
| `components/*_OLD.tsx` (10 files) | DEL | Dead code removal |
| `DarkFrame.zip` | DEL | 1GB artifact, should not be in repo |
| `app/api/battle/attack/route.ts` | MOD | Add auth middleware |
| Multiple JSX files (~30 files) | MOD | Fix unescaped entity ESLint errors |
| Multiple component files (~40 files) | MOD | Fix exhaustive-deps warnings |
| `__tests__/**` (8 suites) | MOD | Fix failing tests |

---

## 🔗 Dependencies

- None (this is a maintenance/health FID)

---

## ⚠️ Risks & Considerations

1. **React version downgrade** may break `react-konva` features if any v19-specific APIs are used
2. **`npm audit fix`** may introduce breaking changes in minor/patch versions — verify after
3. **ESLint exhaustive-deps fixes** may cause re-render loops if not done carefully
4. **Test fixes** may reveal actual bugs that were masked by incorrect test setup
5. **Exposed credentials** in `.env.local` should be rotated (out of scope for this FID but flagged)

---

## 📊 Success Metrics

| Metric | Before | After |
|--------|--------|-------|
| TypeScript errors | 0 | 0 |
| ESLint errors | 80 | 0 |
| ESLint warnings | 71 | 0 |
| Test failures | 42 | 0 |
| Critical vulnerabilities | 1 | 0 |
| High vulnerabilities | 10 | 0 |
| Dead code files | 10 | 0 |
| Unprotected API routes | 2+ | 0 |

---

*Auto-created by ECHO v1.3.4 - 2026-04-03*
