# SESSION-2026-09-17-052 — Product remainder survey: chat stubs are the new P0, Alliance system is invisible

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** resume the interrupted product survey — what product work
remains now that the dead-route sweep, shrine line, and territory arc are all closed.

## Method

Full artifact read (session-030 re-verification + session-037 shrine disposition)
plus a fresh ground-truth pass: game-page inventory, player-panel census, TODO
density ranking, route-existence vs client-caller cross-checks, mount-graph
greps. Every claim probed live; two survey-era assumptions corrected in flight.

## Corrections to prior survey state (Law 16 probes)

- Session-030's "largest remaining P2" (territory UI) shipped as FID-013.
- Session-030 flagged shrine-extend as a gap — dissolved pre-execution
  (session-040 erratum), shrine line since closed by FID-20260917-002.
- P3 specialization: confirmed closed-live (no drift).
- Tutorial completion: confirmed ends by design; the queued work is only the
  `/complete` legacy deletion — route still alive with zero client callers.

## Findings (full detail: dev/audits/PRODUCT-REMAINDER-SURVEY-2026-09-17.md)

**P0 — chat action stubs lie to the player** (`ChatMessage.tsx` "Task 8"):
Report and Block show success toasts with NO backend anywhere (no report
persistence exists; moderation schema has mutes/bans/warnings only).
Per-message delete: the route EXISTS and ChatPanel itself calls
`/api/chat/delete` (:948) — but never passes `onDelete` into ChatMessage, so
the button toasts success and does nothing. Three lying success toasts in
one component.

**P1 — AlliancePanel mounted NOWHERE.** Complete UI + five live
`/api/clan/alliance/*` routes + services + schema — zero importers. An
entire diplomacy system exists on both ends and no player can reach it.

**P2 — chat polish cluster:** profile modal from chat (:223), item-details
modal (:259), MessageThread Socket.io real-time (:270/:275), ChatPanel
documented backlog (:2132-2139, react-window/typing/online-count).

**P2 queued:** tutorial `/complete` deletion FID (approved pre-restart,
interrupted; evidence re-verified today).

**Stale-comment find (not routed):** ChatMessage's item-link stub comment
claims the endpoint is unimplemented — it exists and is called; only the
details modal is missing. Server-side chat mutes: schema exists, no
enforcement found in the chat post path (recorded for the chat FID's scope).

## Recommended order

1. Chat honesty FID (P0) — wire delete through, build report persistence,
   product decision needed on block scope (chat-only vs global).
2. Alliance mount FID (P1) — highest value-per-effort: backend live, mount
   as ClanPanel social-tab region or standalone.
3. Tutorial `/complete` deletion micro-FID.
4. Chat polish cluster alongside future chat work.

## Board note

FID-009 implementation batch (now incl. the double-run oracle, suite 1017)
remains the only uncommitted work; Mongo Clusters A–D queued behind it.
