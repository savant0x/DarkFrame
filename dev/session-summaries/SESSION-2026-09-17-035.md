# SESSION-2026-09-17-035 — FID-20260916-013 implemented end-to-end (gates green; commit pending G1/G2)

**Agent:** Buffy (Codebuff) · **Model:** z-ai/glm-5.3-flash · **Date:** 2026-09-17
**Continuation of:** SESSION-2026-09-16-034 (handoff) + session 035 SCOPE block (re-plan)

## Operator directive

"Go-ahead: implement FID-013 end-to-end per the changes table, gates as completion evidence."

## What shipped

FID-20260916-013 final minimal scope — war-scoring unification + capture-flow repair + capture UI:

1. **lib/clanWarfareService.ts**
   - Session-034 scaffolding reverted: `deriveAttackerStrength` deleted (level-curve strength —
     zero references remain, grep-verified)
   - `computeClanArmyPower(clanId)`: Σ(strength × quantity) over all member `players.units`
     (battleService idiom), 100-member cap matching `notifyBothClans`; jsonb trust boundary
     via `asUnitList`
   - Capture formula (S1): `defenderWall = max(computeClanArmyPower(defender), CAPTURE_BASE_WALL=5000) × (1 + defenseBonus/100)` vs `attackerStrength × (1 ± CAPTURE_JITTER=0.15)` — armies on both sides, no new constants
   - **A1**: attacker treasury debit moved into the attacker's own success-transaction update; defender row touches only `territories` + `statsTotalTerritories−1`
   - **A2**: `captureTerritory` wrapper returns `success := result.captured` (route contract: success ≡ captured; low-level `{success, captured}` shape unchanged)
   - **Scoring**: capture success → `attackerScore += CAPTURE_WIN_POINTS (2)`; repel → `defenderScore += CAPTURE_REPEL_POINTS (1)` — in-transaction `sql` increments (recordWarBattleOutcome idiom)
   - **Feed events**: success → `TERRITORY_CLAIMED` (attacker) + `TERRITORY_LOST` (defender) via `logClanActivity` (completeness gap closed, pass 9); repel logs nothing (no `WAR_REPELLED` enum member — disclosed in FID, not invented)
   - **Settlement precedence**: `settleDueWars` compares total score FIRST; capture counts demoted to tiebreak (fixes "1 capture beats 99 battle wins")
   - **Multi-war**: `getCaptureTargets` → `{ activeWars: [...] }` for ALL outgoing ACTIVE wars (`orderBy(desc(declaredAt))`, no `limit(1)`), each with `capturesToday/capturesCap` + per-tile `defenseBonus` preview mirroring the capture math

2. **app/api/clan/warfare/capture/route.ts** — **A3**: `Daily capture limit reached` / `Capture costs` → 400; `not in clan` → 403; stale 70%-model docstring replaced with the contested army-power contract

3. **app/api/clan/warfare/capture/targets/route.ts** — rewritten to the multi-war shape; empty array = UI guidance state

4. **components/clan/ClanTerritoryPanel.tsx** — scaffolding imports reverted (3 lint errors gone); new **War Captures** section: fetches the targets route on mount (it is the section's data source), per-war defender tag + capture counter, per-tile capture buttons (officer+ gate mirroring server `requireRole`, NOT `canManageWars`), **confirm-then-fire** (§5 contract; win-or-lose fee), verbatim server toasts (capture → success, repel → error), cap-aware `disabled` + title, no-wars guidance

5. **Tests** — 16 new pins:
   - `__tests__/lib/clanWarfareV2.test.ts` (+10): scoring constants; weak-army deterministic repel / strong-army deterministic capture (per-call select-batch queue matching the path's 5 sequential selects); settle points-first + capture-tiebreak; multi-war enumeration; adjacency preview parity; empty-wars guidance; wrapper success ≡ captured (8-select queue)
   - `__tests__/clan/ClanTerritoryPanel.warfare.test.tsx` (new, 6): multi-war render (no limit(1) hiding), no-war guidance, non-officer read-only, cap disable, POST body contract + confirm pin, verbatim repel toast

## Mid-flight findings (honest record)

- First suite run: 7 failures. Causes were all test-side: shared-array mock vs sequential
  selects (rows[0] reads), capped-button title assertion, multiple-match, and
  `logClanActivity`/`notifySystem` mocks returning `undefined` where code awaits `.catch()`.
  All fixed in tests; production code unchanged by the debug cycle.
- The §5 "confirm-then-fire POST" row was under-built (fired immediately); aligned to the
  FID mid-flight (window.confirm guard) and pinned in tests. Found by self-audit against
  §5 before closure.

## Gates (completion evidence — fresh, post-final-edit)

```
npx tsc --noEmit   → TSC_EXIT=0
npm run lint       → LINT_EXIT=0   (3 scaffolding errors gone with the revert)
npm run test:ci    → 966 passed / 1 skipped (967)   (baseline 951/1 → +16 pins, 0 red)
```

## Law-4 reachability (all ≥1 production hit)

- `captureTerritory` → capture route:104 (sole caller)
- `getCaptureTargets` → targets route:42 (sole caller)
- `settleDueWars` → lib/jobs/clanWarSettlementManager.ts:52
- `recordWarBattleOutcome` → lib/battleService.ts (score feed)
- `computeClanArmyPower` → captureTerritory:872 + attemptTerritoryCapture:483

## Ledger state

- SCOPE.md session-035 block updated (implementation bullet, [x])
- FID-20260916-013: status `implemented`, §7 filled, §8 gates [x] ×4 — **closure pending
  commit hash (G2)**
- Paper trail ready for G1 (operator executes git): staging plan + G8 message in FID §8 —
  `fix(clan): unify war scoring, repair capture flow, wire capture UI (FID-20260916-013)`
- C1 sibling-writer hardening (claims/distributions) remains a separately-filed future FID
  (recorded, not absorbed)

## Open items for the next session

1. Operator: review + commit (G1) → paste hash into FID §8 (G2) → archive to `dev/fids/archive/`
2. Deferred (explicit non-goals, recorded): snapshot-writer hardening; siege mechanics
   (discarded by ruling); auto income collection
