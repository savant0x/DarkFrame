# SESSION-2026-09-16-028 — FID-20260916-012 filed + looped to loop-complete (clan research panel)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** file and run the loop for the clan research panel FID —
contribute/unlock UI, the largest unreached system from the survey.

## Ground truth (Law-16 probes before filing)

- `ClanPanel.tsx:290` — `'research'` is in the `ClanTab` union; :368-370 render the
  tab button; :429-431 render `<ComingSoonTab feature="Clan Research" />`. The
  survey's "no panel anywhere" was imprecise: the panel is a placeholder.
- `POST /api/clan/research/contribute` and `POST /api/clan/research/unlock` are live
  (`requireClanMembership` → `contributeRP` / `unlockResearch`); client grep shows
  **zero callers** (only the activityLogger path map at
  `lib/middleware/activityLogger.ts:120`).
- `app/api/clan/research/` has **no GET** — no state endpoint exists at all.
- `getResearchTree(clanId)` is fully implemented; after the FID-20260912-058 (C1) cut
  `RESEARCH_TREE` holds exactly 4 MILITARY nodes (mil_combat_1, mil_tactics_1,
  mil_warmachine, mil_domination) — the other branch arrays are structurally empty
  and their bonus types have no consumer.
- Unlock is role-gated server-side (LEADER/CO_LEADER/OFFICER,
  clanResearchService.ts:180-182); contributions spend personal
  `players.researchPoints` into `clans.researchResearchPoints`.

## Filed: FID-20260916-012 (`dev/fids/FID-20260916-012-clan-research-panel.md`)

- §2 RED: five paste-anchored claims (placeholder line, zero callers, no GET, 4-node
  tree, role gate) — with the survey erratum recorded.
- §5 GREEN: `GET /api/clan/research/state` (thin, `requireClanMembership` →
  `getResearchTree`); `components/clan/ClanResearchPanel.tsx` (fund header, single
  MILITARY rendering, node cards with unlocked/available/locked states, contribute
  flow, role-presentational unlock flow, server errors verbatim); one-line ClanPanel
  placeholder swap.
- §6 loop: 4 passes — RED re-probe, impact re-check, alternative sweep (bank-panel
  extension / endpoint folding / four fake branch tabs all rejected), not-to-change
  re-check (C1 cut preserved; server gates not duplicated; node data untouched). No
  findings after pass 2.

## Loop outcome

**loop-complete** (2026-09-16). Implementation is a separate approval step.

## Bookkeeping

- SCOPE row #72 appended (`Open — loop-complete reached; implementation gated on
  operator go-ahead`).
- Uncommitted in worktree: FID-012, SCOPE row #72, this record — plus the standing
  FID-011 implementation batch (8 files, session 026 plan).

## Staging plan (G1)

```bash
git add dev/fids/FID-20260916-012-clan-research-panel.md SCOPE.md \
  dev/session-summaries/SESSION-2026-09-16-028.md
git commit -m "docs(fid): file FID-20260916-012 — clan research panel, contribute/unlock UI (loop-complete)"
```

Footer-free. Not committed — operator trigger.
