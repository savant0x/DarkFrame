# SESSION-2026-09-17-053 — FID-20260917-010: unreachable clan UI reachable at last

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** commit the staged batches, then the Alliance FID first
(from the survey's recommended order; block = GLOBAL recorded for the later
chat-honesty FID).

## Commits first

- `70e6b2d` — FID-20260917-009 batch (Stripe VIP re-key + false-success fix +
  double-run oracle; 10 files).
- `0e7165f` — survey batch (row 87, audit + session 052; block=GLOBAL decision
  appended to the audit before committing).

## What shipped (FID-20260917-010, loop-complete)

`components/clan/ClanPanel.tsx`:
- **Alliances tab added** (Handshake icon, after Social): first-ever mount of
  `AlliancePanel` — complete diplomacy UI with five live
  `/api/clan/alliance/*` routes that had ZERO importers (survey P1).
- Five-prop contract honored: clanId (house idiom), playerId=player.username,
  role=playerRole, clanName=clanData.name, treasuryMetal=
  `clanData.bank?.treasury?.metal ?? 0` — the payload already carries it via
  rowToClan's bankTreasuryMetal mapping (FID-006's sanctioned route), zero API
  changes.
- **Research tab un-disabled**: the mount-surface probe found FID-012's
  ClanResearchPanel mounted behind a bare `disabled` TabButton — dead on
  arrival since that FID landed. The flag is gone; FID-012's work is
  reachable.

Pins (`__tests__/components/ClanPanelAllianceMount.test.tsx`, first
mount-level render pins in the house): real ClanPanel rendered under jsdom
(only GameContext + fetch mocked), fixture built from ClanPanel's actual read
set (level/settings/maxMembers/etc. — the first fixture crashed ClanHeader on
`level.currentLevel`, which doubles as proof the pin renders deep enough to
matter):
1. Mount pin — click Alliances → "Propose Alliance" renders.
2. Reachability pin — Research tab NOT disabled.
3. Panel-shape pin — AlliancePanel standalone hits
   `/api/clan/alliance?clanId=…` with the wired props.

## Gates

Pins 3/3 · tsc 0 (after the gate caught my three-prop first cut — TS2739 on
both call sites; the interface demanded all five, honored rather than
loosened) · eslint 0 · census 0 · full suite **1020 passed + 1 skip**.

## Disclosures

- Two components share the ClanManagementView name: the inner view in
  ClanPanel.tsx (tab system — this FID's mount surface) and
  components/clan/ClanManagementView.tsx (game-page sidebar, 'chat' tab, no
  social/research tabs). Only ClanPanel.tsx touched; the divergence is
  recorded as a candidate for a future unification FID.
- AlliancePanel's treasury gate is client-side only (canAfford check) —
  server-side enforcement is the alliance routes' existing responsibility and
  was NOT modified this FID.

## Board state

Batch staged-ready: FID-010 (ClanPanel + pins + FID + row 88). Queue after:
chat-honesty FID (delete/report/GLOBAL block), tutorial /complete deletion
micro-FID, chat polish, Mongo Clusters A–D.
