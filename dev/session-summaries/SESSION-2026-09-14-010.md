# SESSION-2026-09-14-010 — Unmapped-collection census: four silent no-ops pinned, spec FID converged

**Date:** 2026-09-14 · **Protocol:** ECHO v0.1.2 single-agent

**Task source:** operator directive — "Open and converge a FID for the unmapped units
collection: abandon's lost-unit accounting silently no-ops post-pivot while units live in
`players.units`."

---

## Intent (logged before implementation — Law 8)

The directive names the candidate recorded during the FID-20260914-004 sweep: the shim
silently no-ops every `db.collection(name)` that resolves to no schema table and no alias,
and `app/api/factory/abandon` counts/reads/deletes "units produced at the factory" from the
unmapped `units` collection — so abandoning a factory reports 0 units lost while units
persist in `players.units`. Intent: pin the defect with a full census (is it one collection
or a class?), live-probe the domain reality, open the FID, run the Perfection Loop to
convergence — document-only this session, implementation gated.

## Census verdict (evidence in FID-20260914-009 §2)

All **22 distinct** `collection('…')` names in `lib/` + `app/` were resolved against the
shim's resolution order (live dump: **110 registry keys covering 63 distinct tables** ∪
**11** `TABLE_ALIASES` — machine-verified by `scripts/censusCollectionMapping.ts`, which
mirrors `getTable()` verbatim; an initial grep-derived estimate of 39 tables/12 aliases in
the FID draft was corrected the same day, unmapped set unaffected). **Four names
resolve to undefined** — the directive's seed is one of a class:

| # | Unmapped name | Caller | Silent behavior |
| --- | --- | --- | --- |
| 1 | `units` | `app/api/factory/abandon/route.ts` (count/find/deleteMany + STR/DEF deductions) | abandon's lost-unit accounting never fires; units survive abandon |
| 2 | `BattleLog` | `app/api/logs/cleanup/route.ts` (legacy `clientPromise`, not even the shim) | battle-log retention counts/deletes nothing, reports success |
| 3 | `playerLevelHistory` | `lib/playerHistoryService.ts` ×4 (snapshot insert via cron, purge, growth rate, predictive distribution) | **daily snapshot cron has stored nothing since the pivot**; beer-base predictions run on their fallback |
| 4 | `clan_territories` | `lib/cacheWarming.ts` | read no-op; module itself is barrel-only dead code |

**The domain shift (why this is not an alias fix):** live probe shows **0 of 57** players'
unit entries carry `producedAt` (factory coords) — no unit anywhere is stamped with a
factory. The 2025-10 contract "units are stationed at the factory that produced them; the
factory's death kills them" is **not reconstructible** from current data. Mapping `units`
as an alias would translate filter keys (`owner/factoryX/factoryY`) onto nonexistent
columns — rejected by evidence (FID Option C).

## FID + Perfection Loop record

**FID-20260914-009** written, status `converged`. GREEN per finding: (1) remove abandon's
obsolete accounting block (Option A — accept the domain shift; Option B per-factory
stationing recorded as a feature FID if the operator wants the concept back); (2) rewrite
log-cleanup retention on the mapped `battleLogs` shim name with an honest count; (3) create
the missing home — `player_level_history` table + shim alias + four call-site translations
(snapshot history accrues from deploy; predictions keep their fallback until data exists);
(4) delete the dead territory branch.

Perfection Loop **converged in 2 of 10 iterations, no oscillation.** Loop 1 falsified the
draft's own Option-C-shaped approach against the live `players.units` shape and widened the
FID from one collection to four. Loop 2 verified the cron genuinely schedules the dead
write, confirmed the cleanup route's admin gate is sound (only its storage layer is dead),
and honestly downscaled Option B's backfill (57 players' units lack stamps — a backfill
could only approximate by `createdAt` heuristics), strengthening the Option A
recommendation. Two refinements folded; zero actionable findings remained.

## Session type: document-only

No code changed (the FID is a spec; Phases A/B are gated on operator go-ahead). Gates were
re-run as Method-1 proof of zero code drift, matching the FID's §6 claims:

| Gate | Result |
| --- | --- |
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` (eslint) | exit 0 |
| `npm run test:ci` (vitest) | **769 passed / 1 skipped / 0 failures** |

Scratch census/probe scripts were deleted after use; evidence preserved in the FID.

## Bookkeeping

- `dev/fids/archive/FID-20260914-009-unmapped-collections-units-abandon.md` — created (`converged`)
- `SCOPE.md` — Session 2026-09-14 (010) block + ledger row #38
- This record

## Commit plan (presented, not executed — G1)

Single docs-only commit, path-scoped:

```
docs(audit): census unmapped legacy collections; spec FID-20260914-009 converged (FID-20260914-009)

- SCOPE.md
- dev/fids/archive/FID-20260914-009-unmapped-collections-units-abandon.md
- dev/session-summaries/SESSION-2026-09-14-010.md
```

---

**Final status:** FID-20260914-009 `converged` (spec); implementation of Phases A/B awaits
operator go-ahead. This stream joins five other staged-and-waiting streams (FID-004 shim,
FID-005 battle-logs, FID-006 attack readout, FID-007 profile, FID-008 specialization).
