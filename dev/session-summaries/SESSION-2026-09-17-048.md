# SESSION-2026-09-17-048 — FID-20260917-008: /api/player/inventory pg migration

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** file and implement the FID for /api/player/inventory —
migrate the Mongo-era route to pg with requireAuth on darkframe_session; the
client calls it every game load and gets 401s.

## Ground truth → RED

Route read 0-EOF: dead `playerId`-cookie auth + `clientPromise` Mongo query —
both defects at line level; the pivot left it on the Mongo stack entirely.
Premise probed live (pg, player `fame`): 47 items / cap 2000 / diggers 25+23 /
bonuses "39.00"·"37.00" — matching the operator's own screenshot percentages;
the data was always in pg, only the route was dead. Sole consumer
(`InventoryPanel.tsx:180`) does `setInventory(data)` directly → contract is the
UNWRAPPED InventoryData; `expiresAt` must be an ISO string (client does
`new Date(...)`); legacy `resources`/`equipment` consumed by nobody → dropped.

## GREEN

Route rewritten (`requireAuth` → 8-column select → contract translation;
numeric parseFloat; ISO expiry; mixed jsonb rides unfiltered per the
factoryService discriminator doctrine; AUTH_USER_NOT_FOUND 401 for the
mid-request race — RESOURCE_NOT_FOUND maps to 400, wrong semantics).

## Verification (with the two probe-authored defects disclosed)

- Pins 5/5 (`__tests__/api/playerInventory.test.ts`), real envelope helpers.
- Test-authoring lesson: `@/lib/db/schema` must stay REAL in mocks — the `@/lib`
  barrel re-exports it; a full-replace mock starves the barrel (`userPresence`)
  and breaks imports. Also: the wrapped GET signature is `(request, context)`
  — five TS2554s fixed via the repo's callGET idiom.
- Driver defect #1 (self): unawaited premise import chain — no, the actual one:
  my first AUTH assertion expected `{error:{code}}` but the live refusal shape
  is `{ success:false, error:"Unauthorized" }` (string) — assertion updated to
  the observed truth, noted in the driver comment.
- LIVE probe 3/3 exit 0 (premise / auth / contract) against the real DB.
- Live server (:3000) hot-reload confirmed: curl+cookie → **HTTP 200** full
  contract; cookie-less → 401. This is the endpoint the operator's browser
  watched 401 all session 046.
- Gates: tsc 0 · eslint 0 (3 paths) · vitest 1004+1skip (999 → 1004).

## Board state

Two staged-ready batches now queue: (1) census gate batch (hook + census +
SCOPE row 82 + session 047), (2) this FID batch (route + test + driver + FID +
SCOPE row 83 + session 048). SCOPE worktree delta = rows 82+83, one each.
Follow-up idea recorded in the FID: dev-mode console warn on non-ok fetches
(the silent-skip guard hid this defect through two full sessions of play).
