# SESSION-2026-09-16-020 — Window-mechanics audit: no enforcement hole; three hygiene asks

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** with every protection surface enforced, audit the 72h window
mechanics themselves (expiry, re-trigger, clock skew, NULL handling) for edge-case defects.

## Method

Full read of `lib/playerProtection.ts` (the -002 artifact), census of every
`protectionUntil` writer and reader (lib/app/components/scripts), migration DDL
(`0004_*.sql:2` — plain `timestamp`), client display paths, and existing pin coverage.

## Headline

**No enforcement hole.** All five live refusal sites use the canonical predicate
(strict `>`, NaN degrades open per Law 14, injectable clock); NULL is unprotected
everywhere including the void's own idempotent no-op; expiry boundary is strict on
every implementation; no re-issuance exists (by design); the display layer clamps
stale windows to nothing. Bots/beer bases are born NULL → structurally unshieldable.

## Findings (FID-20260916-009)

- **F1 MEDIUM — predicate drift:** `movementService.ts:87` and
  `wmd/targetingValidator.ts:37` hand-roll `!= null && new Date(...) > now` instead of
  calling `protectionActive`; targetingValidator's message also diverges
  (`'Target is under protection'` vs `PROTECTION_REFUSAL_REASON`). Behaviorally
  equivalent today (degrade-open by JS accident), rot-prone later. → D1 asks to
  refactor both onto the canonical predicate.
- **F2 MEDIUM (latent) — timezone-naive column:** `protection_until timestamp` without
  tz. Node-pg round-trip is lossless today (UTC literal in, UTC literal out) — no live
  defect; hazard fires only if a non-pg writer (psql/migration/manual) or SQL-side
  comparison enters. → D2 asks to schedule a `timestamptz` migration (safe:
  existing values reinterpret as UTC, exactly how pg wrote them).
- **F6 cleanup — dead no-auth `createPlayer`** (playerService:342, zero callers) issues
  no protection: a silent unprotected-account minter if ever revived. → D3 deletion ask.
- **F3 clock/skew, F4 NULL, F5 corrupt values, F7 boundary, F8 UI staleness, F9 string
  path:** clean or recorded-info (details in the FID).

## Errata

None — first audit of the time/mechanics dimension; corrects nothing prior.

## Bookkeeping

FID-20260916-009 filed (`created`); SCOPE row #68 appended
(`Open (audit delivered; D1–D3 gate any follow-up)`). Staging plan (G1):
`git add dev/fids/FID-20260916-009-window-mechanics-audit.md
dev/session-summaries/SESSION-2026-09-16-020.md SCOPE.md` →
`docs(fid): window-mechanics audit — no enforcement hole; predicate-drift refactor +
timestamptz migration recommended (FID-20260916-009)`. Not committed — operator trigger.
