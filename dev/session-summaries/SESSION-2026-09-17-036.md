# SESSION-2026-09-17-036 — FID-20260917-001 implemented end-to-end (gates green; closed `6577707`)

**Agent:** Buffy (Codebuff) · **Model:** z-ai/glm-5.3-flash · **Date:** 2026-09-17
**Continuation of:** SESSION-2026-09-17-035 (FID filing) + the session 035 SCOPE block

## Operator directive

"Go-ahead: implement FID-20260917-001 end-to-end per the changes table, gates as completion evidence."

## What shipped

The C1 sibling-writer class is closed: **every clan-treasury writer now runs inside
`withClanTreasuryLock`** (SELECT … FOR UPDATE on the clan row inside a transaction) and moves
funds via **relative SQL deltas** (`treasuryDelta` / `playerResourceDelta`) instead of
snapshot-computed numbers.

| Piece | Detail |
|---|---|
| **New helper** | `lib/db/treasuryLock.ts` — `withClanTreasuryLock` + `treasuryDelta` + `playerResourceDelta`; first `.for('update')` usage in the codebase (feasibility probed against drizzle 0.45.2) |
| **territoryService** | claim + daily income: lock, daily guard re-checked INSIDE the lock (double-click double-collection dead), relative deltas |
| **clanBankService** | deposit/withdraw/upgrade: lock, capacity/sufficiency re-checked against the LOCKED row, clan+player updates now one atomic pair (+13th site **collectTax** — dynamic-key writer the census greps couldn't see, discovered via tsc dead-variable fallout, hardened same idiom) |
| **clanPerkService** | activate (lock + relative debit) / deactivate (lock; jsonb-only, F6 resolved: no funds columns) |
| **WMD** | deductWMDCost + admin missile refund: lock + relative deltas; `refundWMDCost` confirmed a stub (amendment noted) |
| **clanWarfareService** | declareWar locked; capture success/repel/spoils transfers relative-SQL; dead `winnerTreasury` snapshot read removed |
| **alliance + distribution** | the two already-relative writers wrapped in the shared lock with in-lock sufficiency re-checks (§5 rule governs the "smallest change" wording) |
| **Tests** | `__tests__/lib/treasuryConcurrency.test.ts` — 9 pins; FIRST coverage for bank/perk/distribution services; withdraw-refusal pin proves the in-lock re-check (passes preview, fails locked row); deposit pin proves 2-updates-in-1-transaction via observable `tx.update` spy |

## Mid-flight catches (self-audit, Law 3)

1. Dropped `const player =` declaration in withdrawFromBank's callback — tsc caught it, fixed.
2. The same dead-variable removal exposed `collectTax` (13th snapshot site, outside the FID's
   changes table) — disclosed in FID §7 as an honest deviation, hardened with the same idiom.
3. Two commit attempts hit the repo's commit-msg guard (unauthorized attribution watermark) —
   re-committed without the trailer per the operator's 2026-09-15 standing rule; no `--no-verify`.

## Completion evidence (fresh, post-final-edit)

```
npx tsc --noEmit → TSC_EXIT=0
npm run lint     → LINT_EXIT=0
npm run test:ci  → 975 passed / 1 skipped   (baseline 966+1 → +9 new pins)
```

Census grep: **0** computed treasury writes remaining; **8/8** services adopted the helper;
`withClanTreasuryLock(` → 17 call-sites, all route-reachable (Law-4 greps in FID §7).

## Ledger

Filing commit `800a4ec` · implementation commit **`6577707`** · closure batch: FID status
`closed`, archived to `dev/fids/archive/`, CHANGELOG 0.0.3, VERSION 0.0.2 → 0.0.3 (Savant
iteration bump), SCOPE 035-cont block marked implemented.
