# SESSION-2026-09-16-024 — P0 fixed: three missing player-side endpoints rebuilt (FID-20260916-010 verified)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** file and implement the P0 fix — rebuild the three missing
player-side endpoints (`/api/discoveries`, `/api/friends/online`, `/api/friends/block`)
against their existing services, with pins and gates.

## What shipped (FID-20260916-010: filed → loop-complete → implemented in one go-ahead)

- **`GET /api/discoveries`** — adapter over `getDiscoveryProgress`; the service's
  domain shape maps to the panel's contract field-by-field (enum case → UPPERCASE,
  `Date` → epoch ms, `totalAvailable→totalPossible`, `progressPercent→percentComplete`,
  numeric `byCategory` → `{discovered,total:5}` cells, `IN_PROGRESS→INCOMPLETE`).
  400 missing username, 404 unknown player. Sibling-wrapper pattern
  (`withRequestLogging` + typed error helpers).
- **`GET /api/friends/online?ids=`** — `requireAuth` (sibling pattern), id list capped
  at 200, one indexed drizzle read on `userPresence` (`inArray` + `expiresAt > now` —
  the 60s heartbeat TTL is the presence truth); every requested id appears in
  `statuses` with unknown/expired → `offline` (the panel merges without re-fetching).
- **`POST /api/friends/block`** — `requireAuth`; **caller is the session user, never
  the body**; `friendService.blockUser` (which removes the friendship rows then
  inserts the block, per the panel's doc contract). Typed error mapping exactly like
  `friends/[id]`: ValidationError→400, PermissionError→403, NotFoundError→404.

## Verification

- **12 pins** (`__tests__/api/p0MissingEndpoints.test.ts`), including the adapter-shape
  pin (every mapped field), the total-statuses pin, and the security pin that `blockUser`
  receives the *session* caller with the body supplying only the target.
- **Gates:** tsc 0 · eslint 0/0 · vitest **931 passed / 1 skipped**.
- **Law-4 reachability:** panel fetch paths grep-verified verbatim against the new
  route dirs; no rewrites involved (probed in the survey).

## Harness honesty (5 first-run failures, all mine, all disclosed in the FID)

`@/lib` mock missing `ErrorCode`; 401 fakes as plain `Response` (correctly failing the
route's `instanceof NextResponse` check); self-block fake as a bare object instead of
the real `ValidationError`. Gates also caught: the `withRequestLogging` wrapper type
requires the `context` argument at test call sites, and one unused `eq` import.

## Bookkeeping

FID-20260916-010 §7 done + gates ticked (`verified`, closed awaits G2 hash); SCOPE row
#70 disposition → `Verified (…; closed awaits G2 hash)`. Staging plan (G1):
`git add app/api/discoveries app/api/friends/online app/api/friends/block
__tests__/api/p0MissingEndpoints.test.ts dev/fids/FID-20260916-010-p0-missing-player-endpoints.md
dev/session-summaries/SESSION-2026-09-16-024.md SCOPE.md` →
`fix: rebuild three missing player-side endpoints — discoveries, friends online, friends block (FID-20260916-010)`.
On landing: -010 closes with the hash, archives, and takes the CHANGELOG line. Not
committed — operator trigger. Still queued from before: the Law 16 protocol batch
(session 022) and, if separated, the -009 closure micro-batch.
