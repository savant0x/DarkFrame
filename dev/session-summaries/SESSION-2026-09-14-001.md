# SESSION-2026-09-14-001 — Complete the uncommitted FID-20260914-001/-002 work stream

**Date:** 2026-09-14 · **Protocol:** ECHO v0.1.2 single-agent

**Task source:** operator directive — "Pick up the uncommitted FID-20260914-001/-002 work
stream, finish whatever is incomplete, and present the full verification evidence."

---

## Intent (logged before implementation — Law 8)

The tree carried two uncommitted FID streams. FID-20260914-001 (troop-transport toggle,
status `verified` with implementation evidence already recorded) appeared complete.
FID-20260914-002 (battle-numbers raid audit, status `analyzed`, Implementation Evidence
"pending — filled at GREEN") was implemented in the tree but unfinished on paper, and the
audit found real defects inside its stream. This session: audit every modified file,
attribute it to its FID, finish what is incomplete, correct what is wrong, and present
the full verification evidence. No new scope.

---

## WIP audit (Law 1 — every modified file read as a diff)

| Files | Attribution | Verdict |
| --- | --- | --- |
| `lib/validation/schemas.ts`, `app/api/move/route.ts`, `lib/antiCheatDetector.ts`, `context/GameContext.tsx`, `components/MovementControls.tsx`, `components/MovementControls.test.tsx` (+`app/neon-noir.css` transport block) | FID-20260914-001 (troop-transport) | Complete and matching the FID contract; tests present (2 toggle tests) |
| `app/api/combat/logs/route.ts` | FID-002 issue 1 (counters) | Implemented per FID; verified against `components/BattleLogLinks.tsx` render contract and the `battle_logs` schema columns |
| `app/api/combat/attack/route.ts` | FID-002 issue 2 (garrison) + issue 3 (RP statement) | Implemented; two contract deviations found (see findings) |
| `lib/jobs/flagBotManager.ts` | FID-002 issue 6 (12h hold-drop) | Implemented but **corrupted** (column-0 `} else {`) |
| `vercel.json` | FID-002 issue 6 (cron entry) | Correct entry, **malformed indent** |
| `app/api/flag/drop/` (untracked) | FID-002 issue 4 (route half) | Implemented per FID; **UI half missing entirely** |
| `docs/design/BASE_RAID_BALANCE.md` (untracked) | FID-002 design doc | One phantom knob found (see findings) |
| `vitest.setup.ts`, 20+ test files with pragmas, `__tests__/lib/beerBaseScheduler.test.ts`, `lib/jobs/beerBaseManager.ts` (`getISOWeek` export) | FID-20260913-001 (vitest jsdom fix) | Pre-stream, complete, untouched |
| `server.ts` | FID-20260912-093b (label backfill wiring) | Pre-stream, complete, untouched |
| `CHANGELOG.md` | FID-20260913-001 bookkeeping | Pre-stream, intact |

Not attributed to either FID (left exactly as found): `.freebuff/project-id`,
`docs/design/llms-*.txt` (gitignored scraped docs), `public/assets/tiles/new-bases/`
(recorded in SESSION-2026-09-13-001 as untracked art awaiting the operator's call).

## Findings and fixes (all in the FID-002 stream)

1. **Corruption — `lib/jobs/flagBotManager.ts:168`:** the FID-002 hunk's `} else {` sat at
   column 0 (leading whitespace lost, the exact CRLF-write failure class FID-20260914-001's
   self-correct note documented). The gates could not flag it — the tree compiled — but the
   job's branch structure was one edit away from breaking. Repaired to the correct 6-space
   indent; verified by grep (0 column-0 hits) and re-read.
2. **Corruption — `vercel.json`:** the purge-old-data block's opening brace was misindented
   to column 0 (still valid JSON, still parseable — a hazard, not a break). Indent restored;
   `JSON.parse` verified post-fix.
3. **FID contract deviation — garrison size floor:** the FID promises
   `clamp(ceil(totalDefense/20), 8, 60)` but the stream's code used `Math.max(1, …)`.
   Aligned with a named `GARRISON_SIZE_FLOOR = 8` constant (matches the doc's T1 tuning
   row: 8 units at totalDefense 150).
4. **Doc-vs-implementation drift — `GARRISON_HP`:** the design doc lists a `GARRISON_HP = 15`
   tuning knob, but `lib/battleService.ts` derives unit HP from `strength > 0`
   (`HP_PER_STR_UNIT = 10`, `HP_PER_DEF_UNIT = 15` — line 120-121, 143-145). The synthesized
   garrison units are STR-class, so 10 HP is derived, not tunable. Doc corrected (HP row now
   marked derived; T1 tuning row 120→80 HP). The doc remains the honest tuning source of
   truth.
5. **Missing UI half — issue 4:** `POST /api/flag/drop` existed untracked but nothing called
   it (a Law-4 violation the FID's own audit rubric exists to catch). Wired:
   `onDrop` prop + Drop button in FlagTrackerPanel's bearer self-view (hidden during an
   active steal channel — the channel owns the flag's fate), `handleFlagDrop` on the game
   page fetching the route with the panel's existing `extractApiError` envelope handling.
6. **FID bookkeeping:** FID-20260914-002 `analyzed` → `verified`; Implementation Evidence +
   Resolution filled with tool evidence; stray `{}` artifact at EOF removed; Lessons Learned
   gained the uncommitted-stream lesson.

---

## Verification evidence (Double Audit — tool output)

- `npx tsc --noEmit` → **exit 0** (2026-09-14 17:15 EDT, post-all-edits)
- `npm run lint` (eslint .) → **exit 0, zero findings** (2026-09-14 17:15 EDT)
- `npm run test:ci` → **75 passed | 1 skipped (76 files), 736 passed | 1 skipped (737
  tests), 0 failures, ~11-13s** (2026-09-14 17:15 EDT)
- `node -e "JSON.parse(vercel.json)"` → valid (post-indent-repair)
- Law 4 reachability greps: `startFlagBotJob` ← `server.ts:299` (12h drop executes on the
  30-min cadence; vercel cron covers production); `GARRISON_SIZE_FLOOR` declared
  (`attack/route.ts:109`) + consumed (`:120`); drop flow wired end to end —
  `onDrop` (`FlagTrackerPanel.tsx:64`), button (`:344`), handler binding
  (`app/game/page.tsx:1450`), route fetch (`:940`); Battle Log panel contract
  (`attackCount/defenseCount/infantryCount/landMineCount`) matches the rewritten summary
  endpoint.
- Baseline before this session (boot, 16:53 EDT): tsc 0 · eslint 0 · vitest 736/1/0 —
  the gates held exactly; no regression, no new test count.

## Commit plan (presented, NOT executed — G1/G2/G3/G4/G8)

| # | Type(scope) | Paths | FID |
| --- | --- | --- | --- |
| 1 | `fix(vitest)` — jsdom collection fix: setup polyfill removal, 25 node pragmas, ISO-week fix, `getISOWeek` export | `vitest.setup.ts`, 20+ test files, `lib/jobs/beerBaseManager.ts`, `dev/fids/FID-20260913-001*`, `dev/session-summaries/SESSION-2026-09-13-001.md`, its CHANGELOG section | FID-20260913-001 |
| 2 | `fix(movement)` — troop-transport toggle: schema, ownership gate, multi-step loop, step-normalized anti-cheat, toggle UI + tests, CSS | `lib/validation/schemas.ts`, `app/api/move/route.ts`, `lib/antiCheatDetector.ts`, `context/GameContext.tsx`, `components/MovementControls.*`, `app/neon-noir.css`, `dev/fids/FID-20260914-001*` | FID-20260914-001 |
| 3 | `fix(combat)` — battle-numbers: real counters, weight-class garrison, stated RP, 12h hold-drop, drop route + UI, design doc | `app/api/combat/logs/route.ts`, `app/api/combat/attack/route.ts`, `lib/jobs/flagBotManager.ts`, `vercel.json`, `app/api/flag/drop/`, `components/FlagTrackerPanel.tsx`, `app/game/page.tsx`, `docs/design/BASE_RAID_BALANCE.md`, `dev/fids/FID-20260914-002*` | FID-20260914-002 |
| 4 | `chore(server)` — FID-093b backfill migration wiring | `server.ts` | FID-20260912-093b |
| 5 | `docs(protocol)` — session bookkeeping | `SCOPE.md`, this file | — |

Excluded pending operator call: `public/assets/tiles/new-bases/` (untracked art),
`docs/design/llms-*.txt` (gitignored), `.freebuff/project-id`, `df-094*.log` (file-locked;
delete after stopping the dev server). Per G2, FIDs `closed` require a committed hash —
both FIDs stay `verified` (not `closed`) until these commits exist.

## Out-of-scope notes (recorded, not acted on)

- The troop-transport toggle requires the operator to restart the dev server + hard reload
  to appear (the running process predates the changes) — FID-20260914-002 issue 5.
- `flagBotManager` is run by both the in-process scheduler and (in production) the new
  vercel cron; both consume the same idempotent drop logic, so double-execution is a no-op
  (the drop fires once; the second pass sees no holder).
- The operator's `REDIS_URL` shell export remains handled test-side only (vi.hoisted pin
  from session 2026-09-07-003); no new exposure this session.
