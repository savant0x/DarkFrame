# SESSION-2026-09-16-031 — Territory-capture FID filed and looped (FID-20260916-013)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** file and run the loop for the territory-capture UI FID — the
next P2 system from the survey.

## Ground truth (all probed live before writing)

- **RED anchor confirmed:** zero client callers of `POST /api/clan/warfare/capture`.
- **Latent defect found (the turn's discovery):** the route's `captureTerritory` →
  `attemptTerritoryCapture` passes `attackerStrength = 0` against `defensePower >= 5000`
  — every route-level capture is mathematically guaranteed to fail, and the repel path
  still debits 25k/25k treasury and consumes a daily attempt. Unreachable defect:
  dead UI meant no player ever hit it.
- `GET /api/clan/wars` already provides war context (WarDto with tags, status);
  `ClanWarfarePanel.tsx:96` consumes it. War state must be fetched inside the
  territory-panel flow (sibling tabs don't share props).
- Presentational gate subtlety: server allows OFFICER+ for capture
  (`requireRole`), but `canManageWars` is false for OFFICER — the UI gate must
  mirror the server, not the generic wars permission.

## What was filed (FID-20260916-013, loop-complete)

1. **Service fix** — `captureTerritory` derives real attacker strength (formula to be
   pinned at implementation; `attemptTerritoryCapture` signature unchanged) + pure
   derivation helper with V2-test pins.
2. **New GET** `/api/clan/warfare/capture/targets` — attacker-war-aware enumeration of
   the defender's tiles; no-war returns a soft `{ targets: [], activeWar: null }`.
3. **UI** — "Capture from [TAG]" section in ClanTerritoryPanel: enemy tiles with
   defense bonuses, OFFICER+ capture buttons, confirm-then-fire, repel shown honestly
   (server message already discloses the cost), errors verbatim, no-war guidance.

Not-to-change: signature, cap (3), cost (25k/25k), defense-wall scale (5000), jitter
(±15%), wars GET, claim/unclaim flows, route error-mapping.

**Loop:** 4 passes (RED re-probe, impact re-check incl. transaction-body read of the
repel path, alternative sweep, not-to-change re-check). No findings. Header status
set to `loop-complete` at filing time — the header-lag defect class from -011/-012
not repeated.

## Bookkeeping

- SCOPE row #74 appended (`Open — loop-complete; implementation gated on go-ahead`).
- Uncommitted, staged-ready batch (3 paths): `dev/fids/FID-20260916-013-territory-capture-ui.md`,
  this session record, SCOPE row #74.

## Staging plan (footer-free — commit-msg hook bans attribution trailers)

```
git add dev/fids/FID-20260916-013-territory-capture-ui.md \
        dev/session-summaries/SESSION-2026-09-16-031.md SCOPE.md
git commit -m "docs(fid): file FID-20260916-013 territory-capture UI (loop-complete)"
```
