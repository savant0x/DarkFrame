# SESSION-2026-09-16-013 — Protection parity audit across remaining PvP surfaces

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** audit the remaining PvP surfaces (factory attack, espionage, anything
that hits another player) for protection parity with the infantry and WMD seams.

## Method

Three sweeps cross-checked for reachability: (1) every existing void/refusal site,
(2) every route/service whose name or body matched attack/spy/scout/sabotage/raid, (3) every
surface's **client caller** — because a server gap behind an unreferenced API is latent, not
live. Every matrix claim carries a file:line from this session's tool output.

## Findings (full matrix in FID-20260916-006 §2)

**Reference seams (parity baseline):** infantry (void `battleService:675` + refusal route :113),
WMD launch (void `missileService:233` + target validation `-005`), war-clan join
(`clanService:539`, ACTIVE-war gate). Beer-base raid intentionally clean (bots-only, documented
at battleService:669-673). Territory capture is war-gated by design.

**Live gaps:**

1. **Spy sabotage — HIGH.** `executeSabotage` (`spyService:476`) → `applySabotageDamage`
   (:1239) destroys missile components / delays builds / wastes resources;
   `grep -c protection spyService.ts` → **0**. Reachable via the live `WMDIntelligencePanel`
   → `/api/wmd/intelligence`. The FID-005 shield-bypass shape, one layer down.
2. **Factory capture — MEDIUM.** Target-side refusal already exists (`factoryService:386`,
   FID-002 seam) but **no attacker void** — a shielded player captures player-owned factories
   risk-free. Live via `FactoryButton` / R-key.
3. **Player battle route — LATENT.** `/api/battle/attack` → `resolveBattle` has the void but
   no defender refusal; **no client caller found** (mounted, unreferenced). Cheap pin prevents
   a resurrection bug.

**Operator policy calls (no code until decided):** recon-mission targeting (information ops
under `startMission` — `validateMissionTarget:853` checks only existence + own-clan) and flag
steal (`startChallenge`, live via `app/game/page.tsx` — contested-object minigame; does the
bearer's hold imply accepted steal risk?).

## Artifacts

- **`dev/fids/FID-20260916-006-protection-parity-audit.md`** — `created`, HIGH aggregate.
- **SCOPE row #65** — `Open (audit delivered; enforcement FIDs on decision)`.

## Staging plan (G1 — presented, not executed)

```bash
git add dev/fids/FID-20260916-006-protection-parity-audit.md \
  dev/session-summaries/SESSION-2026-09-16-013.md SCOPE.md
git commit -m "docs(fid): protection parity audit — 3 live gaps (spy sabotage HIGH) + 2 policy calls (FID-20260916-006)"
```

**Note:** SCOPE also carries uncommitted row #64's disposition (FID-005 implementation, staged
plan pending) — hunk-level staging applies when this batch commits first.

**Final status:** complete — audit delivered, FID-006 filed at `created`.
