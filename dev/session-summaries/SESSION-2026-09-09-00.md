
## FID-20260909-028 — admin flows + design-pass failures (converged)
Six defects, one FID, full perfection loop:
- **Crash ×2:** PlayerDetailModal called `.toISOString()` on wire-string timestamps
  (sessions + flags tabs) → both tabs typed honestly as strings.
- **VIP grant:** was absent from the player-detail ADMIN tab entirely; Grant 7d/30d/1yr +
  Revoke added against the existing audited routes; AdminView error surfacing fixed
  (`data.error` object interpolation → `error.message`).
- **Clan creation:** schema requires `tag`; modal never sent one → every attempt 400'd.
  Tag field added (2–6, uppercased, check-name reuse); cost banner corrected to the real
  1.5M/1.5M (modal previously showed phantom RP); doc-conformant per
  docs/COMPLETE_CLAN_SYSTEM_PLAN.md.
- **Shrine buff:** probe test convicted a NaN-poisoning bug — unknown/legacy item rarity →
  `undefined` duration → 200 with `Invalid Date` persisted. Both activate/boost-all now
  refuse loudly (400 with rarity diagnostics). 5 regression tests.
- **Stats + Shrine redesigns:** full structural neon noir passes (gradient heroes, emoji
  tabs/headers, raw focus classes, doubled bg classes all eliminated; token primitives only).
- **Bonus find:** 12 back buttons in game/page.tsx carried the same dead-void-bg class bug.
Gates: tsc 0 · eslint 0 · vitest 403/1skip · build 238/238 exit 0.

## FID-20260909-029 — no-clan page, RP economy, activity coverage (converged)
Four operator questions, four root causes, all fixed hand-edited:
- **"Those 2 content boxes" + "old requirements":** gradient create/join slabs showed dead
  50K/100-RP pricing. THREE stale-cost copies existed (page cards, in-game NoClanView,
  in-game CreateClanView — the last also still missing the required `tag`, so in-game
  creation was still broken post-FID-028). All now source CLAN_CONSTANTS (1.5M/1.5M, no
  RP); /clan page mounts the real ClanPanel instead of a dead-end duplicate.
- **"What happened to RP?"** Nothing — it's schema-backed, sanitized, and spent by WMD
  research. The broken link was the standalone /tech-tree: its /api/research ran on the
  Mongo seam spending phantom `gold` (every unlock failed) and writing phantom
  `unlockedTechnologies`. Migrated to Drizzle + spendResearchPoints + the real
  players.unlockedTechs column; page gates on RP, labels costs in RP, hydrates unlocks
  on mount. Design truth: docs/CHANGELOG_RP_OVERHAUL.md (6,000 RP/full map).
- **Admin Activity tab empty:** tracking WORKS (15,135 live rows) — the viewed player was
  bot-seeded with zero API-mediated actions. Real gap: 5 of 9 loggers (attack, factory,
  banking, tech, trade) had zero call sites — every money/fight action was invisible to
  anti-cheat. Wired into 9 routes (bank ×3, battle ×2, factory ×2, auction ×2) + research.
- **RP curve note for operator:** standalone tree prices (5K–75K RP) vs WMD tree (2.7M RP)
  are two scales; tuning is an open design decision (FID §7).
Gates: tsc 0 · eslint 0 · vitest 408/1skip (5 new) · build 238/238 exit 0.

## Bot-name hygiene — DB repair + second generator bypass closed
Operator spotted space-named bots ("Rusted Redoubt") and mash names ("pamtpkziq5"):
- **Root cause 1 (spaces/hyphens):** pre-9c8ea0f generators emitted "Descriptor Noun" /
  "Flag-Bearer-4523"; the Sep-7 fix corrected all five generators + added contract tests,
  but scripts/fix-invalid-usernames.ts was never RUN. Executed it now: deleted the 4
  invalid bot rows (Flag-Bearer-4523, Thundering Depot, Rusted Redoubt, Forsaken Outpost;
  zero FK-style references found in flags/battles/chat/clans/auctions). 0 invalid remain.
  Respawn paths (beer-base weekly manager, summoning, admin spawn) recreate them validly.
- **Root cause 2 (mash names):** TWO generator bypasses existed. The admin bot-spawn route
  hand-rolled `${specialization}Bot_${n}` slugs instead of calling generateBotName() —
  fixed to the themed generator (pamtpkziq5 itself was likely a manual/summon artifact,
  not from any current code path).
- **Dead accounts census (operator decision pending):** p3probe, verifybuyer, smoketest1,
  testplayer1, pamtpkziq5 — all is_bot=0 human-format rows, level 1, no activity beyond
  registration. Deleted NOTHING (humans are never touched by the bot script); flagged for
  operator call — they can be removed via the existing admin player-delete flow.
Gates: tsc 0 · eslint 0 · vitest 408/1skip (botNameGeneration contract 10/10).

## FID-20260909-030 — Bot placement contract (converged)
Root cause: no bot spawn path (createBotPlayer / spawnBeerBase / admin
bot-spawn) ever read the tiles table — raw Math.random() coordinates, no
terrain check, no occupancy claim. Docs rule found: README_NEW.md designates
Wasteland as spawn terrain; the human path (findAndClaimSpawnTile) already
implemented it. Also: admin route had MAP_SIZE=5000 on a 150x150 map, and
weekly respawn never released tile claims (permanent Wasteland drain).
Fixes: shared claimBotBaseTile/releaseBotBaseTile helpers (race-safe
conditional claim, Wasteland+zone bounds, loud exhaustion) adopted by all
three paths; MAP_SIZE bug removed; removeBeerBase releases claims; 19 ghost
claims purged live; Flag_Bearer_1027 re-seated Factory(114,55) ->
Wasteland(41,88). 5 placement-contract tests added.
Gates: tsc 0, eslint 0, vitest 413/1, build exit 0. Live DB verified
terrain-legal, owner-consistent.

## UI fix — viewport bottom corners vs status strip overlap
Bottom corner brackets (bottom: 6px, 26px tall) descended into the ~27px
status strip, colliding with the cooldown chip ("16h 46m until reset").
Fix: .nn-viewport gains --nn-viewport-strip-h: 44px; strip gets the fixed
height (taller bar per report); bottom corners anchor at
calc(strip-h + 6px) — same 6px inset as the top pair, frame symmetric.
CSS-only, single consumer (TileRenderer).

## UI fix — harvest/attack result decks centered readout
Harvest result deck left-hugged: single-resource well rendered in a 2-col
grid (half-width, left column), label/number left-aligned, flavor message
left. Added .nn-deck__stats to the design system: centered flex row,
wells equal-width (cap 240px), each column centers micro-label + display
num; .nn-deck__msg--center for the message. Applied to harvest resource
readout, cave-item well, bonus line, result message, and the attack deck's
power comparison. Factory status deck keeps nn-grid2x2 (true 2x2 + meter).

## FID-20260909-031 — Economy Statistics tracker (converged)
EconomyTab COMING SOON stub replaced with a complete tracker: GET
/api/economy/stats (Drizzle-native aggregate over trade_history, auctions,
player_activity jsonb flow, player supply; session auth; 60s cache) + full
neon noir EconomyTab (market pulse, price band/active book, 7-day flow with
meter bars, supply, top traders). 3 contract tests; live SQL smoke
ECONOMY-AGG-VERIFY-OK. Gates: tsc 0, eslint 0, vitest 416/1, build 0.

## FID-20260909-032 — tutorial flow integrity, admin activity view, factory tracking, HUD polish (CONVERGED)

Operator playtested the tutorial end-to-end and filed eight defects. All root-caused
against live DB probes / read code paths, then fixed hand-edited:

- **Admin Activity tab empty (route 500):** `mode() within (group by …)` is invalid
  Postgres. Replaced with grouped top-1 subquery; route now returns per-action
  breakdown; modal renders rows + honest error state; missing-param now 400
  (VALIDATION_MISSING_FIELD), not 404. Live-verified: fame = 15,150 actions.
- **D-pad too small / OUT OF RANGE pill clipping:** CSS geometry fixes in neon-noir.
- **Tutorial reward copy + celebration:** detailedHelp de-emoji'd and structured;
  overlay celebrates on result.reward; validation failure surfaces inline.
- **build-unit `$each` blob (CRITICAL):** the cast-smuggled `{ $each: … }` operand was
  still live in HEAD — any new build would re-corrupt `players.units`. Removed;
  plain folded entry array pushed; 5 regression tests pin the class; live DB 0 blobs.
- **Invested Metal/Energy 0 + Lifetime 0%:** honest math — per-factory production
  spend reconstructed from `producedAt` provenance (new optional PlayerUnit field)
  priced via UNIT_CONFIGS, merged with upgrade-path spend; truthful labels.

Gates: tsc 0 · eslint 0 · vitest 429/1 skipped (13 new) · build exit 0 · live probes clean.
