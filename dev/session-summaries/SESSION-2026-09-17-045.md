# SESSION-2026-09-17-045 — Inverted route census: the called-but-never-built sweep (FID-20260917-007)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** run the inverted route census — every client fetch's URL must
resolve to an existing app/api route; file the FID for any other
called-but-never-built endpoints.

## Method (the tool is the deliverable)

`scripts/invertedRouteCensus.cjs` — repeatable, gate-usable (exit 1 on any MISSING):
harvests every `fetch(`/`apiFetch(` literal/template URL across the client estate
(302 call sites after comment stripping), normalizes with interpolation sentinels
(one segment per `${…}`), strips block + full-line comments, matches against the
238-route set with `[param]` wildcards, reports WAIVED (documented false positives,
with reasons) / MISSING / UNPARSED. Iteration path disclosed in the FID: four
tooling defects found and fixed by probe (`154 → 41 → 4 → 2 → 0` MISSING across the
matcher fixes), including the per-chunk query-cut bug that leaked `&k=` fragments as
bogus segments, and a `node --check` catch of a partial str_replace brace before any
run.

## Findings (all verified in context)

- **REAL ×2:** `GET /api/clan?clanId=` — called by `StatsPanel.tsx:188` (clan tag)
  and `TopNavBar.tsx:132` (nav clan badge); the route never existed, and both
  callers' silent `if (response.ok)` guards meant the 404 was invisible: those UI
  elements never rendered for anyone. Same class as FID-006, third and fourth
  instances.
- **False positive ×2 (documented, not fixed):** VIP `${action}` resolves to literal
  child routes `grant`/`revoke` at runtime (waiver with reason in the script); the
  `/api/battle` hit was a line-commented doc example (fixed structurally by
  comment-stripping, not waived).

## Fix

Both rewired to the canonical `GET /api/clan/[id]` from FID-006 (envelope
`{ success, clan }` unwrapped, guarded set) — no twin endpoint minted, honoring the
dead-twin doctrine; FID-006's route now has four callers. StatsPanel's two pins
updated to the new URL + envelope.

## Verification

Census exit 0 (MISSING 0, UNPARSED 0, WAIVED 1) · tsc 0 · eslint 0 · vitest
**999+1skip** (pins updated in place; count unchanged by design).

## Board state

Worktree carries exactly this batch (5 files): StatsPanel.tsx, TopNavBar.tsx,
StatsPanel.test.tsx, census script, FID — plus SCOPE (row 81 staged in index) and
this record. Uncommitted per convention.

## Staging plan (footer-free, single batch)

```
git add components/StatsPanel.tsx components/TopNavBar.tsx \
        components/StatsPanel.test.tsx scripts/invertedRouteCensus.cjs \
        dev/fids/FID-20260917-007-inverted-route-census.md SCOPE.md \
        dev/session-summaries/SESSION-2026-09-17-045.md
git commit -m "fix(clan): rewired two more never-built /api/clan callers found by the inverted route census (FID-20260917-007)"
```
(SCOPE staged delta = row 81 only — verified in index before this record.)
Then on go-ahead: FID → closed on the batch hash, archived, SCOPE #81 → Closed,
CHANGELOG [0.0.8], VERSION bump.
