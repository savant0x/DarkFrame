# SESSION-2026-09-16-011 — WMD launch target-validation FID filed (FID-20260916-005)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent · **Branch:** `main`
**Operator directive:** file a follow-up FID for the out-of-scope discovery — WMD launch
accepts any username with no target validation.

## Method

Grounded the full strike chain 0-EOF (route → service → cron impact resolver) before
writing: Law 1 re-ground first, and the `-004` discovery deserved more than a citation —
its severity depends entirely on what *happens* at impact.

## Chain evidence (all tool-verified today)

1. **Route** (`app/api/wmd/missiles/route.ts:239-242`): `launchMissile(validated.missileId,
   validated.targetId, auth.username)` — the target select at :226-238 feeds only the
   broadcast name; no checks of any kind.
2. **Service** (`lib/wmd/missileService.ts:191-231`): `launchMissile` validates the
   missile (exists + READY) and persists `targetId` verbatim. Zero target reads.
3. **Dead validator re-confirmed:** `validateTargeting` (existence, protection :37-38,
   level ≥ 10 :41-42) — definition + tests only, zero production callers.
4. **Impact resolver** (`lib/wmd/jobs/missileTracker.ts`): `grep -n protection` → **zero
   hits**. Nonexistent target → `if (!target) return { unitsDestroyed: 0, … }` (:136-140) —
   damage no-ops while the missile still resolves to a terminal state.

## Findings (RED — sharpened beyond the operator's one-liner)

- **Dud waste (the operator's discovery):** any garbage `targetId` launches fine; at
  impact the damage no-ops but the warhead is consumed — a silent clan-arsenal griefing
  vector for anyone with launch access.
- **Shield bypass (elevated during grounding):** no production code ever consults
  target-side protection, so the FID-002 72h shield does not stop incoming WMD strikes —
  infantry refuses protected defenders (FID-002 seam); WMD never checks. The flagship
  newcomer protection is bypassed by the game's largest incoming-damage event. Both
  defects share one root cause: the launch seam is the only gate the target ever faces,
  and it has none.

## Artifacts

- **`dev/fids/FID-20260916-005-wmd-launch-target-validation.md`** — `created`, HIGH.
  RED table (6 findings), impact analysis, five questions, GREEN sketch (target reads in
  `launchMissile` before the FID-004 void — an invalid target refuses *without*
  forfeiting the launcher's shield, per the committed-action principle; one open decision
  recorded: keep or drop the validator's level ≥ 10 floor). Loop deliberately not run.
- **SCOPE row #64** — `Open (filed at created; loop on operator directive)`.

## Deliberately not done

Perfection Loop (RED re-verify + GREEN audit), implementation, and live probes are gated
on operator directive per the amended vocabulary — filing is the whole of this session.

## Staging plan (G1 — presented, not executed)

```bash
git add dev/fids/FID-20260916-005-wmd-launch-target-validation.md \
  dev/session-summaries/SESSION-2026-09-16-011.md SCOPE.md
git commit -m "docs(fid): WMD launch accepts any username — shield-bypass + dud-waste findings (FID-20260916-005)"
```

**Note (shared-tree care):** SCOPE.md also carries the uncommitted row #63 (hook audit)
and the FID-004-implementation batch (row #62 disposition, session 010, the -004 FID with
§7 verified) — if the -005 doc batch commits first, SCOPE will need the same hunk-level
staging care used before; rows #63/#62 belong to their own batches.

**Final status:** complete — FID-20260916-005 filed at `created`, ledger row #64 appended.
