# SESSION-2026-09-16-014 — Spy-sabotage enforcement spec loop-complete (FID-20260916-007)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** file the enforcement-spec FID for the spy-sabotage gap and run its
Perfection Loop to loop-complete.

## Grounding rewrote the picture (all claims tool-verified, re-run in loop pass 1)

- **RED 1 — transposed route call:** `app/api/wmd/intelligence/route.ts:218-223` passes
  `(spyId, targetId, targetType, auth.playerId)` against the service signature
  `(spyId, targetType, targetId, targetPlayerId)` — the missile id lands in the type slot.
- **RED 3 — impossible ownership assertion:** `validateSabotageTarget` requires the target
  asset's owner to equal the passed 4th arg, which the route fills with the **caller's**
  `auth.playerId` — the operator, who never owns the victim's missile.
- **RED 4 — net effect:** live sabotage always refuses "Invalid sabotage target" before any
  damage write. The harm is latent, not live (corrects FID-006's reachability verdict); the
  feature is broken. Protection therefore ships **together with** the repair.
- **RED 7 — hijack hazard:** `getSpy` binds no ownership; without an operator-binding
  refusal, a naive void site could punish a spy's victim. Binding added to the spec.
- **Dead twin:** second `executeSabotage` in `sabotageEngine.ts` with a different contract,
  zero importers (`lib/wmd/index.ts:56` deliberately skips it) — recorded out-of-scope.

## Loop record (evidence in FID §6)

Pass 1 re-verified every load-bearing RED claim live (no corrections). Pass 2 audits: import
graph acyclic (`playerProtection` imports drizzle/db/schema only); void source is the spies
row's **stored `ownerUsername`** (safe: signup-immutable identity + operator binding refuses
hijacks before the void site); ordering = after every refusal precondition, before the
success roll (committed-action parity with -004); owner derivation needs no new helpers
(`getPlayerUsername` already in use); refusal message reuses `PROTECTION_REFUSAL_REASON`.
**CONVERGENCE criterion met — zero open findings.**

## Artifacts

- **`dev/fids/FID-20260916-007-spy-sabotage-protection-enforcement.md`** — `loop-complete`.
- **SCOPE row #66** — `Open (loop-complete…; implementation gated on operator go-ahead)`
  (one self-correction en route: the cell initially said "Closed" — wrong under the amended
  vocabulary; fixed before commit).

## Staging plan (G1 — presented, not executed)

```bash
git add dev/fids/FID-20260916-007-spy-sabotage-protection-enforcement.md \
  dev/session-summaries/SESSION-2026-09-16-014.md SCOPE.md
git commit -m "docs(fid): spy sabotage protection enforcement spec loop-complete — pipeline repair + void/refusal seams (FID-20260916-007)"
```

**Final status:** complete — FID-007 loop-complete; implementation a separate go-ahead.
