# SESSION-2026-09-17-056 - Cluster B batch 1: stats, check-name, tutorial to pg (FID-20260917-015)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** "Start Mongo census Cluster B: migrate the remaining live clientPromise call sites to pg."

## Fresh census (the 4-session-old audit was stale)

- **73 files** import lib/mongodb (audit said 13 clientPromise users); live count of direct
  `clientPromise` users is **15** (14 routes/services + the shim). FID-008's inventory hit is
  a history comment only now. Batch 1 = the census's "trivial" slice: stats, check-name,
  tutorial main + decline. Batch 2 = friends x2, DM x3, ban-player, clear-flags,
  logs/cleanup, build-unit, antiCheatDetector (813 lines, 7 hits).

## What shipped

| File | Change |
|---|---|
| `app/api/stats/route.ts` | Full pg rewrite: one `orderBy(...).limit(10)` leaderboard (the census's noted perf win over fetch-then-sort), one SQL aggregate for gameStats, COUNT probes on battle_logs + occupied tiles. **D1: total_power is NOT a pg column** - power is derived as totalStrength + totalDefense in SQL sort/sum/response, matching rankingService:123. Wire shape preserved exactly (consumer-derived): _id -> username (React key only), resources flattened to metal/energy. |
| `app/api/clan/check-name/route.ts` | lower() equality on clans.name; kills the `new RegExp(^${name}$)` built from user input (injection/ReDoS seam). Case-insensitivity is load-bearing (pg unique indexes are case-sensitive). |
| `app/api/tutorial/route.ts` | Eligibility level read -> drizzle; POST connection theater removed; restart deleteOne -> pg `db.delete(tutorialProgress)` (table exists, player_id unique index). |
| `app/api/tutorial/decline/route.ts` | Pure connection theater removed (declineTutorial was already pg). |

## Verification

- Pins x8 (`__tests__/api/clusterB1PgRewrites.test.ts`): wire shape (topPlayers item fields,
  derived totalPower, flattened metal), limit(10) structural pin, per-sortColumn pins
  (level -> level, metal -> resources_metal, power -> DERIVED expression and NOT the
  nonexistent total_power column), check-name lower()-equality + length gate short-circuit,
  tutorial restart pg-delete shape, decline source pin. Source pins assert defect signatures
  (`from '@/lib/mongodb'`, `await clientPromise`), not comment text.
- Live probe (`scripts/e2eClusterB1Live.ts`) via HTTP against the parallel session's dev
  server (port 3000) with a minted JWT session cookie: **16/16 green, exit 0** - stats 200
  with topPlayers 1..10 and the top player equal to a direct SQL oracle; all four gameStats
  counters equal direct SQL counts (66 players / metal SUM / 37 battles / occupied tiles);
  case-mangled real clan name "sAvAnT" -> available:false; probe name -> true; tutorial
  restart POST 200 deletes by session identity only (probe row for a different player
  untouched). Probe row cleanup.
- Gates: tsc 0; eslint 0 (6 files); census exit 0; suite 1048 passed + 1 skip (+8).

## Gate findings (drilled and fixed)

- `withRequestLogging`'s `RouteHandler` type declares a REQUIRED second `context` param -
  direct handler calls in tests need `{ params: Promise.resolve({}) }` (Next always supplies
  it at runtime; the type just finally noticed). My first sed fix landed the context arg
  INSIDE the NextRequest constructor (runtime-tolerated, type-invalid); caught and moved.
- The image-manifest fetch error in stderr is pre-existing vitest-setup noise
  (lib/imageService fetches a relative URL in node), unrelated to this batch.

## Standing notes

- Remaining clientPromise users after batch 1 (batch 2 queue): friends x2, DM x3,
  ban-player, clear-flags, logs/cleanup, build-unit, antiCheatDetector, plus inventory's
  comment-only mention (candidate for comment cleanup).
- Cluster C (aggregates: clan leaderboard, logs cleanup) still pending with the
  double-run-oracle requirement; Cluster D (shim deletion + mongoId drop) rides on B+C.
