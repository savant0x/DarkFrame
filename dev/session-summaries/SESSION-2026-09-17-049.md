# SESSION-2026-09-17-049 — Mongo-residue census: the shim is the beast (row #24 audit)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** audit every route still importing from lib/mongodb or
clientPromise — enumerate the full Mongo-era residue beyond inventory and
propose the pg migration plan for row #24.

## The reframe (Law-16 probes all the way down)

- My first sweep flagged the just-rewritten inventory route as a clientPromise
  importer — the hit was **my own closure comment** describing the old defect.
  False positive retired at line level.
- `lib/mongodb.ts` was assumed (by me too, in FID-008's prose) to be a small
  compat shim. It is a **1,554-line Mongo-over-Postgres emulation layer**:
  `clientPromise` is `Promise.resolve(_compatClient)`, and the module translates
  Mongo filters/`$set/$inc/$push`/pipelines into drizzle or in-memory JS.
  Row #24 is therefore a **translation-layer retirement**, not a data move.

## Census results (48 importer files, 4 classes)

- Class 1 — real shim consumers (15): incl. `stats` route global $group and
  `clan/leaderboard` pipelines, both **in-memory over full table fetches**
  (`runPipeline` at lib/mongodb.ts:851 evaluates stages over the fetched doc
  array) — correct today, O(table) forever. cacheWarming does 11
  fetch-then-sort top-100s.
- Class 2 — dead connection fetches (13 files / ~9 sites): `await clientPromise;
  const _db = mongoClient.db(...)` with `_db` underscore-unused (dm:79/191,
  friends:68/137 probed) — real work goes through pg-backed services.
- Class 3 — connectToDatabase/getDatabase-only consumers (~20): mechanical
  swaps to `@/lib/db`; several hide Class-1-style collection ops.
- Class 4 — stragglers: `scripts/validate-referrals-cron.ts` uses the REAL
  `new MongoClient(MONGODB_URI)` — validating a database that is no longer the
  source of truth; `beerBaseAnalytics` ObjectId is type-only; three files carry
  ObjectId doc-comments only.

## Deliverable

`dev/audits/MONGO-RESIDUE-CENSUS-2026-09-17.md` — capability map (aggregate is
the known-degraded surface; createIndex no-op), per-file classification with
pg-table mappings, and the clustered plan A (dead weight) → B (read-path
rewrites) → C (aggregate → SQL with the shim as the double-run correctness
oracle) → D (delete lib/mongodb.ts + drop `mongodb@^6.10.0` from package.json;
mongoId column retirement gated on the Stripe mapping sub-FID). Three decisions
requested: ActionLog/adminLogs → mod_log mapping, cron disposition, sequencing.
SCOPE row #84 appended (Closed — audit disposition). Zero code changes.

## Board state

Worktree now carries THREE queued batches: census gate (row 82), FID-008
inventory (row 83), this audit (row 84) — each footer-free and owner-checked.
Dev server still up on :3000 (PID 44884).
