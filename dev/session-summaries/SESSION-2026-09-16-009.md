# SESSION-2026-09-16-009 — .githooks integrity audit (commit-msg + pre-push)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent · **Branch:** `main`
**Operator directive:** audit the other .githooks for the brittle-output-matching defect class the pre-commit gate had (vitest-4 ANSI vs its `Test Files  1 passed` grep).

## Method

Read both hooks 0-EOF, classified their parsing surface, then **drilled every branch live** with fabricated hook stdin/files — matching the double-audit rule (tool output over self-report), same standard as the pre-commit root-cause work in `21a59fd`.

## Verdicts

### commit-msg — SOUND (no change)

- No tool-summary parsing: greps the plain message file git hands it; no ANSI in that artifact.
- Handles grep's tri-state exit explicitly (0 = hit → refuse; 1 = clean → pass; other = unreadable → exit 2).
- Live drill: watermark trailer → exit 1 ✅ · clean message → exit 0 ✅ · missing/unreadable file → exit 2 ✅ · case-insensitive `generated with CODEBUFF` → exit 1 ✅.

### pre-push — DEFECT FOUND + HARDENED (1 commit)

- Parsing surface was clean (`git log --format=%B`, no tool summaries), but the **failure-handling** carried the same defect class as the pre-commit matcher: tool failure impersonating a pass on a hook whose own header says fail-closed.
  - `git rev-list --reverse "$range" 2>/dev/null` — enumeration error swallowed → empty loop → `violations=0` → green "✅ attribution scan clean", exit 0.
  - `git log -1 --format=%B "$sha" 2>/dev/null || true` — same flavor per commit.
- **Demonstrated live before fixing:** fabricated push line with a nonexistent local SHA (`deadbeef…`) → `✅ pre-push: attribution scan clean`, exit **0**. A range the hook could not scan passed as clean.
- **Hardening (`.githooks/pre-push`):** capture `git rev-list` output + explicit status check → exit 2 with a refusal message on failure; same for `git log` per commit. Empty output with exit 0 remains the legitimate zero-commits pass. No matcher changed (the regex itself was sound).
- **Re-drill (all three paths):** clean range `5117ecd..HEAD` → exit 0 ✅ · bogus SHA → exit **2** (silent pass closed) ✅ · real watermarked commit → exit **1** ✅ (fixture planted via an empty watermarked commit on a throwaway branch `probe-wm-audit` at `163c08e` — required `--no-verify`, which is itself evidence the commit-msg guard works — branch deleted immediately, never pushed).

## Distinguishing note (why commit-msg/pre-push never hit the pre-commit bug)

The pre-commit gate greps **vitest's colorized stdout summary** — an ANSI-laden tool artifact that changed format under it. commit-msg and pre-push grep plain-text git artifacts whose shape is stable. The shared lesson across all three hooks: every exit status and every parsed artifact must be either (a) format-stable, or (b) explicitly status-checked — never assumed.

## Bookkeeping

- SCOPE row #63 (Closed — hardened, probe evidence herein).
- Staging plan (G1, path-scoped): `.githooks/pre-push` + `SCOPE.md` + this record — message `fix(ci): pre-push watermark scan failed open on rev-list/log errors — explicit status checks (hook audit)`.
