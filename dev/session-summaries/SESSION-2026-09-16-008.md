# SESSION-2026-09-16-008 — FID-20260916-004 drafted + loop-complete (Option B enforcement spec)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent · **Branch:** `main`
**Operator directive:** draft the enforcement-spec FID for Option B (WMD-launch void + war-clan-join void) and run its Perfection Loop to loop-complete.

## Grounding (all 0-EOF, tool-verified)

- **Launch path:** `app/api/wmd/missiles/route.ts` (action='launch' branch :224-239) → `launchMissile` (`lib/wmd/missileService.ts:191-223`) validates only missile-exists + READY, then flips status — **zero protection interaction and no target validation at all** (the route's target select `{username, clanId}` feeds only the broadcast name).
- **`validateTargeting` has no production callers** (grep: definition + tests only) — the -002 infantry-route comment claiming "the WMD path enforces the same column via targetingValidator" is **false**; the spec includes a comment-correction row.
- **Join path:** `POST /api/clan/join` → `joinClan` (`lib/clanService.ts:473-557`): invitation → expiry → clan → capacity → player → already-in-clan, all throwing preconditions; identity is **username-keyed at the players seam** (`WHERE username = playerId`) — the void helper drops in with no translation.
- **War-state reader:** `getActiveWars(clanId)` (`lib/clanWarfareService.ts:724`) — exported, clan_wars rows, status 'ACTIVE', both directions.
- **Cycle audit:** `clanWarfareService` does NOT import `clanService` — the new edge is acyclic; helper exports verified at `lib/playerProtection.ts` (`voidProtectionOnAggression` :63).

## Deliverable

`dev/fids/FID-20260916-004-protection-forfeit-enforcement-wmd-clanwar.md` — **loop-complete**:
- GREEN: two service-level void insertions (launch after READY; join after all five preconditions, gated on `getActiveWars(clan_id).length > 0`, fail-open on war-lookup outage) + comment correction + 6-pin seam test file.
- Rejected alternatives recorded (refuse-not-void, route-level placement, invite-time forfeit); WMD target-validation gap recorded `[OPEN-OUT-OF-SCOPE]`.
- Pass 1: RED re-verified, GREEN survived intact, identity + import-path audits pass. Pass 2: Option-B completeness, acyclicity evidence, delta under cap → **convergence criterion met**. Every check executed against the tree (no pre-written results).

## Bookkeeping

- SCOPE row #62 (loop-complete, pending commit + implementation go-ahead).
- Staging plan (G1, FID-only stage): FID-004 + this session record + SCOPE.md — message `docs(fid): Option B enforcement spec loop-complete — WMD launch void + war-clan join void (FID-20260916-004)`.

## Files touched

- `dev/fids/FID-20260916-004-protection-forfeit-enforcement-wmd-clanwar.md` (new)
- `SCOPE.md` (row #62)
- `dev/session-summaries/SESSION-2026-09-16-008.md` (this file)
