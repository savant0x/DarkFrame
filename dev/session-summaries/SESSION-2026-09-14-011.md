# SESSION-2026-09-14-011 — Live E2E: unit escrow + cancel/expiry refunds

**Date:** 2026-09-14 · **Protocol:** ECHO v0.1.2 single-agent

**Task source:** operator directive — "Run a live end-to-end verification of the
unit-escrow and cancel/expiry refund paths against the dev server, mirroring the auction
money-path E2E."

---

## Intent (logged before implementation — Law 8)

FID-20260914-003's money-path legs (listing fee, buyout charge, leader refund, payouts)
were live-verified by `scripts/e2eAuctionLedger.ts`; the unit-escrow and cancel/expiry
refund legs had only in-process coverage. Intent: drive those legs over real HTTP against
the dev server with per-step DB assertions, mirroring the money-path driver's conventions
(route-only mutations, read-only ledger reads, guarded fixture cleanup).

## What ran (`scripts/e2eUnitEscrow.ts`, exit 0)

Four scratch accounts (`escSellerA/BuyerA/SellerB/SellerC<suffix>`), 3000 metal each, one
INFANTRY each granted to the three sellers (fixtures only — all escrow mutations rode the
production routes):

| Leg | Proven live |
| --- | --- |
| **Buyout delivery (A)** | unit LEFT the seller's army at listing (escrow) · frozen `unitSnapshot` identity preserved · buyout charged 300 · snapshot DELIVERED into the buyer's army · seller paid 285 (300 − 5%) · auction row sold/settled with winner · trade_history row exact |
| **Cancel refund (B)** | snapshot RETURNED on cancel · status cancelled · **second cancel REJECTED** (claim-first close) with **no duplicate refund** (units count still 1) |
| **Expiry refund (C)** | settled by the **real 5-minute settlement job** (the production trigger — no run-now route exists; `expires_at` backdated −1 min to simulate the clock only) · snapshot returned · status expired + settled=1 |

**Conservation (the money-path's signature check):** Σ(metal final − initial) = **−465**
= exactly 3×150 listing fee + 15 sale fee — pure fee burn, zero mint/burn leak; unit
conservation 3 minted → 3 owned (buyerA 1, sellerB 1, sellerC 1), no duplicates.

## Honesty notes (Law 14)

- Two driver-side bugs fixed en route (disclosed, no production surface): a missing paren
  in the unit-grant SQL and a register check demanding exactly 200 where the route
  returns 201-class success (the money-path driver treats any `success:true` as pass —
  my stricter check was wrong, not the route). The first aborted run's orphaned account
  was caught and cleaned by the guarded cleaner.
- Cleanup used a NEW `esc%` prefix (the retained `e2eCleanupAuction.ts` only covers
  `e2e%`): matched 5 accounts / 1 trade / 3 auctions, **residual 0/0/0 verified**.

## Gates

`tsc --noEmit` 0 · `npm run lint` 0 · `npm run test:ci` **769 passed / 1 skipped /
0 failures** (unchanged baseline — the driver is additive tooling).

## Bookkeeping

- `scripts/e2eUnitEscrow.ts` + `scripts/e2eCleanupUnitEscrow.ts` created (retained,
  mirroring the money-path E2E pair)
- FID-20260914-003 archive: post-closure verification addendum appended
- `SCOPE.md`: session-011 block

## Commit plan (presented, not executed — G1)

```
test(auction): live E2E for unit escrow + cancel/expiry refunds (FID-20260914-003 residual)

- scripts/e2eUnitEscrow.ts
- scripts/e2eCleanupUnitEscrow.ts
- dev/fids/archive/FID-20260914-003-auction-persistence-rebuild-residuals.md
- SCOPE.md (surgical: session-011 block only)
- dev/session-summaries/SESSION-2026-09-14-011.md
```

---

**Final status:** unit-escrow and cancel/expiry refund paths live-verified green; the
FID-20260914-003 verification matrix is now fully covered by retained, re-runnable E2E
drivers.
