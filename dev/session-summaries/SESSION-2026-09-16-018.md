# SESSION-2026-09-16-018 — FID-20260916-008 implemented and verified

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** implement FID-20260916-008 per its loop-complete spec — factory
`pvpCapture` void, latent-route refusal + void, pins, live probes, full gates.

## Seams shipped (per the loop-complete spec)

- **Factory capture** (`lib/factoryService.ts`): `pvpCapture` flag on the FID-002 select
  (player-owned + unprotected owner + non-beer-base owner); `voidProtectionOnAggression`
  after all four refusal preconditions, before the power roll. Wild/bot captures never
  forfeit — live-probed with a still-protected attacker on a wild factory.
- **Latent battle route** (`app/api/battle/attack/route.ts`): route-level defender refusal
  (beer-base prefix contract skips, `PROTECTION_REFUSAL_REASON` otherwise) + attacker void
  before `resolveBattle`. Shared resolver untouched (live beer-base raid calls
  `resolveBattle` directly; `executeBaseAttack` is dead). Route body contract preserved:
  schema validates `{targetUsername, units}`; raw body carries attacker/defender; attacker
  pinned to the session user.

## Verification

- **5 pins** `__tests__/lib/factoryCaptureVoid.test.ts`: protected-owner refusal,
  pvpCapture void, wild no-forfeit, max-factories precondition precedes void,
  unprotected-owner void — roll-independent by design (the capture roll never gates a
  protection assertion).
- **6/6 live probes** `scripts/e2eFactoryBattleProtection.ts` (service-direct factory +
  HTTP battle route with signed JWT):
  1. protected owner → -002 refusal message, attacker-2 window intact
  2. player-owned unprotected → attacker-1 void fired
  3. wild capture, protected PvE attacker → window untouched (PvE-safety constraint)
  4. protected defender → 400 refusal, battle-attacker window intact
  5. unprotected defender → void fired (identical geometry to 4; 400 vs void-then-500
     in degenerate `resolveBattle` on empty armies isolates protection state as the
     variable and proves the void fires before resolution)
  6. cleanup residual 0
- **Gates:** tsc 0 · eslint 0/0 · vitest **919 passed / 1 skipped** (+5 pins).

## Probing honesty (three driver iterations, all disclosed)

- v1/v2 design errors were the driver's, not the code's: missing `OWNER_PROT` player row
  (so refusal could not fire — power gate answered instead), `DEFENDER` inserted
  protected (void unreachable), attacker-window poisoning across probes (v3 gives each
  probe its own attacker), and the presence check (`verifyPresence`, Chebyshev distance 0)
  sits **before** the seams — fixtures placed at (1,1) with `factoryLocation {1,1}`.
- `scripts/probePurgeSVC8.ts`: one-shot residue purge (found 0 rows — the earlier
  duplicate-key crash was a driver double-insert of OWNER, fixed in place).
- Probe server (port 3002) torn down; PID file checked.

## Bookkeeping

- FID-008 §7 → done/verified with seams + evidence; status line → `verified`.
- SCOPE row #67 disposition → `Verified (implemented …; closed awaits G2 hash)`.
- Commit plan (G1): `git add lib/factoryService.ts app/api/battle/attack/route.ts
  __tests__/lib/factoryCaptureVoid.test.ts scripts/e2eFactoryBattleProtection.ts
  scripts/probePurgeSVC8.ts dev/fids/FID-20260916-008-factory-void-battle-route.md
  dev/session-summaries/SESSION-2026-09-16-018.md SCOPE.md` →
  `feat: factory-capture protection void + latent battle-route refusal+void (FID-20260916-008)`.
  On landing, -008 closes with its hash in §8; CHANGELOG entry rides the closure batch.
