# SESSION-2026-09-17-044 — FID-20260917-006 committed and closed (clan detail GET)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** commit the FID-20260917-006 batch per session 043's plan, then
close the FID on its hash with archive, SCOPE #80 flip, and CHANGELOG entry.

## Commits

| Hash | Content |
|---|---|
| `22f5889` | Batch A — the route (`app/api/clan/[id]/route.ts`), 4 pins, live-probe driver, FID, SCOPE row 80, session 043 (6 files, +486) |
| `3e92d17` | Batch B — FID §8 filled with hash + gates + probe evidence, status `closed`, archived (rename 86%), SCOPE #80 → Closed, CHANGELOG `[0.0.7]`, VERSION 0.0.7 |
| `56724cf` | Ledger repair — dropped the duplicate Open row 80 the closure assembly appended (1 deletion) |

## Gates (recorded at implementation, session 043)

tsc 0 · eslint 0 · vitest 999+1skip · live probe 4/4 exit 0 (CONTRACT / 404 / 401 /
CLEANUP). No runtime files changed after the gates ran.

## Incident (third recurrence of the reconstruction class, rule sharpened)

Batch B's SCOPE index reconstruction appended the Closed row from the worktree
instead of **substituting** it for HEAD's Open row — row 80 existed twice in HEAD
until the repair commit. Caught by the standard end-of-turn row census (row 80: 2).
Standing rule, now explicit: **when a row already exists in HEAD, reconstruction
replaces it in place — never appends.** (Progress note: the defect was caught before
any further work rode on it, and the repair's staged delta was verified as exactly
one deletion before commit.)

## End-state (probed)

- Worktree **clean** (zero residue).
- SCOPE rows 78/79/80: exactly one each in HEAD; row 80 = `Closed (commit 22f5889;
  FID archived; CHANGELOG [0.0.7])`.
- CHANGELOG newest-first intact in HEAD: 0.0.7 → 0.0.6 → 0.0.5 → 0.0.4 → 0.0.3 →
  0.0.2 → 0.0.1 → 0.0.1 (two 0.0.1 entries are legitimate dated history). VERSION
  file: 0.0.7.
- `dev/fids/` root **empty**; FID-006 archived at 86% rename similarity.

## Operator follow-up

Restart the dev server (`npm run dev:server`) — the clan sidebar and modal will load
"at once" with no client change; the fix is entirely server-side. The bug behind the
screenshot ("Failed to load clan data") is dead at the source.
