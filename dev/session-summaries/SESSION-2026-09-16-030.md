# SESSION-2026-09-16-030 — Post-survey re-verification: P2/P3 drift check

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** survey what product work makes sense next — the remaining P2
systems (territory capture UI, shrine sacrifice, tutorial completion) and the P3
specialization call.

## Method

Every survey claim re-probed today (Law 16: Open claims are hypotheses). Since the
2026-09-16 morning survey, several batches landed (FID-010 P0 endpoints, -011
sabotage UI, -012 research panel) — enough drift to re-verify before recommending.

## Findings

### Territory capture (`POST /api/clan/warfare/capture`) — the next real build

- Route live with full server-side gating: active war validated, permissions and
  success rate handled in `clanWarfareService.captureTerritory:798` (which calls
  `attemptTerritoryCapture` with `attackerStrength=0`).
- Body: `{ targetClanId, tileX, tileY }`; zero UI callers (re-probed today).
- Natural home: `ClanTerritoryPanel` (482 lines, already lists territories via
  `/api/clan/territory/list`, already receives `clan` + `currentUserRole` props).
- Shape: a "Capture territory" action on enemy/adjacent tiles with targeting (tile
  coords), wired to the panel — server refuses everything invalid, so the UI stays a
  thin adapter. FID-worthy; the largest remaining P2.

### Shrine — two findings, one real gap, one suspected dead twin

- Panel is live and wired: `ShrinePanel` calls `/activate` and `/boost-all`, reads
  boost state from `player.shrineBoosts` (sanitizer-bound) — `/status` is redundant
  for the panel's needs.
- **Extend** (`/api/shrine/extend`): 0 UI callers — a real small gap (timers exist
  in the panel; an "extend" action next to each boost timer is the natural fix).
- **Sacrifice vs activate**: the sacrifice route ("sacrifice items to activate a
  resource-yield boost") and the wired `/activate` look like the same feature with
  two entries — likely the sacrifice route is the dead pre-pivot twin. 5-minute
  body-diff disambiguation before deciding (wire it or file for deletion).

### Tutorial completion — survey claim refined, not a feature to build

- The tutorial ends BY DESIGN: `track-action` auto-completes steps at target; the
  overlay handles `result.tutorialComplete` (TutorialOverlay:338); `skipTutorial`
  via `/decline` also sets the flag (tutorialService:1836).
- `POST /api/tutorial/complete` (0 UI callers) is a likely-redundant legacy path —
  **deletion candidate** pending a writer census (does anything server-side call
  the completion logic directly?).

### P3 specialization — VERDICT FLIPPED (the survey's biggest drift)

The 2026-09-14 "built but inert — zero bonus consumers" verdict is **stale**:
- `battleService:374` — doctrine bonuses join the combat stack in `resolveBattle`
  (STR/DEF multipliers), mastery-amplified at `specializationService:195`
  (`masteryAmplificationPercent` folded into strMul/defMul).
- `factoryService:262,555` — doctrine cost multipliers applied at build/upgrade.
- `statTrackingService:77,116` — mastery XP awarded on battle wins / unit builds,
  feeding the 25/50/75/100% milestone ladder.
The choose-flow stays; **no wiring work needed**. Erratum appended to the survey
artifact.

## Recommendation (priority order)

1. **Territory-capture FID** — the last big unreached system; thin UI over a fully
   gated service; natural home exists.
2. **Shrine-extend action** — small; completes the shrine panel.
3. **Two deletion candidates** (tutorial `/complete`, likely shrine sacrifice
   route) — cheap hygiene FIDs after the writer census / body-diff.
4. **P3 specialization: closed, no action** — system is live; survey erratum recorded.

## Bookkeeping

- Survey artifact updated with the erratum section.
- SCOPE row #73 appended (`Closed` — re-verification pass with recommendations).
- This record. Batch uncommitted per convention; footer-free commit plan:
  `git add dev/audits/FEATURE-SURVEY-2026-09-16.md SCOPE.md dev/session-summaries/SESSION-2026-09-16-030.md`
  → `docs(audit): post-survey re-verification — P3 specialization verdict flipped, tutorial complete is a deletion candidate (SCOPE #73)`

## Erratum — 2026-09-17 (session 040): the shrine-extend gap was dissolved before any follow-up ran

The Extend recommendation above was overtaken by events: grounding for the operator's
FID request found FID-20260917-002 (filed and looped by session 036, operator-ratified
Option B, landed `b11c370`, verified by a 20/20 live E2E in `7f95217`) had already
proven the live ShrinePanel extends boosts through `POST /api/shrine/activate`
("Replace / Extend" — remaining time + new duration, same 8h cap), and the operator
ruled `/extend` a dead second economy and **deleted it** (with `/sacrifice` and
`shrineTradeCount`). The gap flagged here was resolved by deleting the candidate, not
wiring it. No FID was filed — the premise was already dissolved and executed.
