# Session 2026-09-04 (002) — Flag-Bot Boot Failure Resolved (SCOPE #20)

**Date:** 2026-09-04
**Protocol:** ECHO v0.1.2 single-agent
**Task source:** Operator: "Fix the flag-bot boot failure (SCOPE #20) — the boolean-into-smallint insert — and verify the job starts on a fresh server boot."

---

## Intent (logged before implementation)

Resolve `[OPEN-OUT-OF-SCOPE]` #20 per the operator's explicit approval: fix the boolean-into-smallint
insert in the flag-bot creation path and verify the flag-system scheduler job starts on a fresh server
boot.

## Root cause

`createFlagBot` (`lib/flagBotService.ts`) inserted `createBotPlayer`'s domain-shaped object directly
via `db.insert(players).values(... as any)`:

- `isBot: true` (boolean) into `is_bot smallint` → pg rejects `true` → `Failed to create flag bot` at
  boot (the direct drizzle insert bypasses the Mongo-compat shim's boolean coercion).
- Even had the boolean passed, the nested `base`/`currentPosition`/`resources`/`bank` fields would
  have silently dropped (no such columns).

Same disease present in `botSummoningService.summonBots` (5-bot bulk insert).

## Fixes

1. **Canonical inverse mapper** — `mapDomainPlayerToRow` added to `lib/playerService.ts` (Law 13:
   single mapping path, the inverse of `mapRowToPlayer`). Handles nested→flat fields, numeric-string
   columns, smallint booleans (`isBot/isSpecialBase/isAdmin/vip` → 1/0).
2. **`createFlagBot` insert** routed through the mapper; return value now re-read via
   `getPlayerByUsername` (single read path, verifies persistence).
3. **`botSummoningService` bulk insert** routed through the mapper (`botsToInsert.map(mapDomainPlayerToRow)`).
4. **`flags.id` overflow** — `crypto.randomUUID()` (36 chars) into `varchar(24)`; replaced with the
   project's `generateId()` (would have been the *next* boot failure).
5. **`getFlagBot` phantom read shape** — expected `currentHolder` to be an object with `botId`;
   every writer stores a plain username string. The read path had never worked (always null).
   Now resolves holder by username and returns the bot only when `isBot` (null → player holds flag,
   matching the manager job's contract).
6. **Raw-row casts** in `moveFlagBot`/`handleFlagBotDefeat` replaced with `mapRowToPlayer` (no more
   `as unknown as Player` seams in the flag flow).
7. Pre-existing lint debt in all touched files cleared (unused imports/vars, `any`s → typed shapes).

## Verification (live)

- Cold `tsc --noEmit`: 0 errors. ESLint touched files: clean. Tests: 333 passed / 1 skipped.
- **Fresh boot on PORT=3002** (stale pre-fix server killed first):
  `[Flag Bot Job] 🏴 Initializing flag system...` → `✅ Flag bot created: Flag-Bearer-8664 at (119, 79)`
  → `✅ Flag system initialized` → `✅ Started with 30min interval`.
- DB row proof: `is_bot=1`, base/current position flat (119,79), `current_hp/max_hp=1000`,
  flag row id 23 chars, `current_holder` matches bot username.
- `getFlagBot`/`moveFlagBot` live regression: read → domain Player (`isBot: true`), teleport
  (119,79)→(32,63), post-move read matches.

## Bookkeeping

- SCOPE #20 closed (resolved) with root-cause and evidence; ledger row added (session 019).

---

## Appendix (same session, later): Systematic Mongo-era Audit (SCOPE #24)

Operator instruction: "Run a systematic audit for any remaining Mongo-era code paths that will break
on Postgres, and list everything found before fixing." ("i also want to add hacker news" was confirmed
wrong-chat and disregarded.)

### Method

Six sweeps over lib/, app/, components/, scripts/, server.ts: shim API inventory → call-site
classification → collection-name resolution against the schema registry → operator coverage →
direct-drizzle seams → live probes of suspect paths. `countDocuments`, `$or`, `$inc`, `$push`,
`$exists`, anchored `$regex` all verified implemented; unknown names and unsupported operators were
traced to their actual runtime behavior rather than assumed.

### Headline correction

**SCOPE #21's crash claim is stale.** `getCurrentBeerBaseCount` runs
`countDocuments({ isBot: true, isSpecialBase: true })` through the shim — live probe returns 0 with no
error (the session-016 boolean coercion and registry rebuild fixed it as side effects). The remaining
#21 concern is different and real: **no scheduler registers a BeerBase respawn job** — `manualBeerBaseRespawn`
is only reachable via two admin/API endpoints; the weekly respawn never fires on its own.

### Findings (all recorded as SCOPE #24; no fixes applied — awaiting operator approval)

| # | Finding | Evidence | Severity |
|---|---|---|---|
| 1 | **22 unresolved collection names** → shim `table()` returns undefined → writes silently vanish, reads return empty, delete/update report fake success | `users`, `clan_territories`, `ActionLog` (3 sites), `adminLogs` (3), `playerAchievements`, `tradeHistory`, `players_temp`, `system_logs`, `tutorial_action_tracking`, `tutorial_progress`, 13× `wmd_*` (wmd.seed + wmd routes) | **Critical — silent data loss** |
| 2 | **`aggregate()` ignores its pipeline**, returns raw rows | antiCheatDetector:721, rankingService:166, achievement-stats:103, clan/leaderboard:171+182, stats:104, validate-referrals-cron:119 | **Critical — silently wrong results** |
| 3 | **Dot-path `$inc` silently no-ops** (`buildSetPayload` maps only top-level columns) | auctionService:164,466,471 (listing fee, buyout, payout — money never moves), statTrackingService:63,82,101,120,138 | **Critical — economy broken** |
| 4 | **Boolean `$set` values crash smallint columns** | wmd/notifications:97,110 (`read: true`), auctionService:212 (`units.$[unit].locked: true`) | High — runtime crash |
| 5 | **`chatService` writes 36-char `randomUUID` into `chat_messages.id` varchar(24)** | chatService:397→insert:418 | High — chat send crash |
| 6 | **Multi-key `sort()` honors only the first key** | referral/leaderboard:35 (`totalReferrals,lastReferralValidated`), player/build-unit:213 | Medium — misordered results |
| 7 | **9 `@ts-nocheck` admin routes** hide their DB seams from the type checker | admin/achievement-stats, active-sessions, analytics/session-trends, bot-config, bot-leaderboard, bot-stats, player-sessions, player-tracking, stats | Medium — hides whole defect classes |
| 8 | **BeerBase respawner never scheduled** | no job registration anywhere; only manual endpoints | Medium — feature dormant |
| 9 | **`lib/queryOptimization.ts` excluded from tsc** (dead Mongo module) | tsconfig exclude | Low — hygiene |

### Verified non-issues (probed, not assumed)

- `countDocuments({ isBot: true, isSpecialBase: true })` → 0, no crash (players=4, tiles=22500 sanity-checked).
- `clanBankService.transactionId` (36-char) lands in a jsonb transaction array — safe.
- `factoryService` `randomUUID` is a domain Unit id inside jsonb `units` — safe.
- `lib/common/errors.ts` `users` reference is a JSDoc example only.
- Direct drizzle `.set()` updates (85 sites) are row-shaped with smallint-safe literals at the sites
  sampled; the boolean-in-`$set` exposure is specific to shim `updateOne/updateMany` calls.
- `friendRequests` and `bans` DO have schema tables (`lib/db/schema/moderation.ts`, `friends.ts`) —
  the failures at those sites are the shim's no-op-on-unknown-name behavior (finding 1), not missing tables.

---

## Appendix 2 (same session): SCOPE #24 fixes executed (operator-approved batches 1-3)

### Batch 1 — collection names
- Shim registry now dual-keyed: schema export name AND SQL table name (`getTableColumns`-style
  `getTableName`), so snake_case call sites (`tutorial_progress`, `wmd_notifications`, …) resolve.
- Verified alias map: `users→players`, `playerAchievements→achievements`, `adminLogs→modLog`,
  `ActionLog→modLog`, `system_logs→modLog`, plus 6 `wmd_*` renames (`wmd_missiles→missiles`,
  `wmd_clan_defense_grid→wmd_defense_grids`, `wmd_interception_attempts→wmd_interceptions`,
  `wmd_launch_history→wmd_launch_authorizations`, `wmd_player_research→player_research`,
  `wmd_sabotage_events→wmd_sabotage_operations`). Aliased writes verified live: matching fields
  persist, unknown keys dropped harmlessly (drizzle ignores unknown columns — probed).
- `tradeHistory` has NO table; the insert site (`auctionService.buyAuction`) still no-ops on that
  write (recorded as residual #24 item; trade records lost).

### Batch 2 — shim semantics
- **aggregate() now executes pipelines**: leading `$match` pushed to SQL; remaining stages run in
  JS with real Mongo semantics — `$group` ($sum/$min/$max/$avg/$first/$last/$push + $cond),
  `$sort`, `$skip`, `$limit`, `$count`, `$project`, `$addFields` ($sum over arrays, $add, $ifNull,
  $cond, $size, $isArray), `$lookup` (both equality and $expr forms via preloaded foreign rows).
  Unknown stages/expressions THROW (loudly) instead of returning wrong data.
- **dot-path `$inc`** → `jsonb_set` arithmetic (pg text[] literal format `'{a,b}'` — JSON form is
  rejected). Note: this only fires when the root is a real jsonb column (`battleStats.x`); on
  `players`, `resources` is FLAT columns, so auctionService's dot-path form needed a call-site
  rewrite instead (done — see below).
- **$set boolean coercion** (smallint crash class closed).
- **Multi-key sort fixed** — root cause deeper than first key: drizzle `$dynamic().orderBy()`
  REPLACES prior order, so sequential per-key calls keep only the LAST key. Now one orderBy call
  with all keys spread. (This also fixed single-key sorts being silently dropped when a second
  orderBy was applied.)
- **chatService id overflow** fixed (36→24 chars) + `affectedRows` (MySQL-ism → always undefined,
  so delete/edit ALWAYS returned false) → `rowCount` (probed: drizzle pg returns rowCount).
- **wmd notifications** `read/readAt` (no such columns) → `viewedBy` $addToSet + `viewCount` $inc.

### Batch 3 — gates + scheduler
- Beer Base respawner registered: `lib/jobs/beerBaseManager.ts` (10-min interval, ISO-week dedupe,
  flagBotManager pattern) wired in `server.ts` with stop-on-shutdown.
- Discovered the WMD scheduler ALREADY runs a `beerBaseRespawner` (population maintenance, 60s) —
  it was crashing on every tick: usernames 34 chars > varchar(20), then nested-domain insert NULLs.
  Fixed: compact 14-char usernames `b<tier><ts8><rand4>` (tier letter parseable; battle/attack
  regex updated for both formats), shim insert now flattens known domain player fields
  (`flattenDomainPlayerFields`) — fixes every Mongo-era player-shaped insert at the seam.
- `queryOptimization` module: zero real consumers (its exports unused; the one grep hit is a
  comment word) — left in place per minimal-change policy; residual cleanup noted.

### Verification
- 9/9 live shim probes pass (group/$sort/$count/match-pushdown/jsonb $inc/alias/boolean $set/
  multi-key sort both orders).
- Fresh boot: all three jobs start (Flag Bot, Beer Base manager, WMD scheduler incl. BeerBase
  population); Beer Base SPAWNED live — row `bS299792251945`, base (148,117), lvl 15, 1.4M
  resources, 10 units, `is_bot=1`/`is_special_base=1`, specialization stored.
- Cold tsc 0; touched files lint-clean; 333 tests pass.

### Residual (recorded in #24, still open)
- auctionService money movement FIXED in-batch: the 3 `$inc: {'resources.metal'}` call sites
  (listing fee, buyout charge, seller payout) rewritten to the flat `resourcesMetal` column —
  auction money now actually moves (in-band with batch 2's dot-path work, verified by tsc + the
  shim's flat-column $inc path).
- tradeHistory insert no-ops (no table) — needs a table or removal.
- statTrackingService dot-path `$inc` (`stats.battlesWon` etc.) — `stats` IS a jsonb column, so the
  new dot-path translation applies; live-verify on next battle.
- 9 `@ts-nocheck` admin routes (finding 7) — not yet de-flagged.
- queryOptimization dead module — deletion deferred (destructive-action policy).


---

## Follow-through (same session, after the audit batches)

**Auction $inc call sites** (batch 2's core promise, finished): `lib/auctionService.ts` — listing fee, buyer debit, seller payout now increment the flat `resources_metal` column. Dot-path resolver in the shim also covers `app/api/factory/*`, `app/api/player/build-unit`, `botCombatService`, `botGrowthEngine` without call-site edits.

**Domain dot-path + alias layer in the shim** (the big one):
- `PLAYER_DOT_PATH_COLUMNS` maps the 17 known domain subpaths (`resources.metal`, `base.x`, `currentPosition.y`, `bank.*`, `inventory.*`, `gatheringBonus.*`, `activeBoosts.*`) to flat columns; `resolveKeyToProp()` resolves them in `buildWhere`, `$set`, `$inc`, and both sort paths.
- `shapeRowAliases()` attaches the nested domain alias view (`resources`, `base`, `currentPosition`, `bank`, …) to every players row returned through the shim (findOne/find/aggregate). Additive only — direct-drizzle callers and `=== 1` smallint checks unaffected. This retro-fixes every raw-row consumer: `botCombatService` loot reads (previously silently 0 → loot zeroing), `botScannerService`, `botGrowthEngine`, `antiCheatDetector`, stats route.
- `app/api/tile/route.ts`'s `findOne({ 'base.x': x, 'base.y': y })` now resolves (was match-nothing → tile base-owner lookup dead).
- Stats route sorts by `resources.metal` (was silently unsorted), aggregate `$sum: '$resources.metal'` returns real totals.

**$avg aggregate accumulator finalized**: it accumulated into hidden `__sum`/`__cnt` side channels and never folded them — every `$avg` consumer received a raw object. Now finalizes to the quotient in `$group` (live-probed: `avgLevel: 16.6`).

**Auction lint debt cleared** (11 errors): typed `lockUpdate: MongoUpdate`, unit/inventory/bid callbacks, removed unused `MarketStats`/`PlayerUnit`/`UnitType` imports and a dead `playersCollection`; exported `FilterBranch`/`MongoFilter`/`SortSpec` from the shim for consumers.

**NEW SCOPE #25 (open, operator decision needed)**: the auctions table and the `AuctionListing` domain doc share no keys — no auction has ever persisted on pg (table empty; inserts violate `seller_id` NOT NULL; reads address nonexistent columns). The economy fixes are real but reach a table listings can't enter. Needs a feature-level rebuild.

**Verification**: 9-probe shim suite (aliases, dot-path filter/sort/$inc/$set, $ne smallint, cleanup) + aggregate domain-path probe all PASS live; fresh boot clean (flag bot, BeerBase respawner job, WMD×5 jobs, stats + tile routes 200); cold tsc clean; eslint clean on touched files; 333 tests passing.

## Follow-up: Server-side attack presence enforcement (2026-09-04, session 003)

Operator directive: attacks must only be possible when the player physically stands
at the target's coordinates; client positions are never trusted.

- New `lib/presenceCheck.ts`: `verifyPresence(username, coords, maxDistance=0)` —
  resolves the attacker from the SESSION, reads position from the DB, Chebyshev
  distance (8-directional grid). Default requires standing ON the tile.
- `/api/factory/attack` (R key): was trusting body `username` (impersonatable) with
  NO presence check. Now session-authenticated + presence-at-factory-tile required.
- `/api/battle/attack`: added presence-at-`factoryLocation` + attacker/session match.
- `/api/combat/infantry` (PvP): added defender-position lookup + same-tile presence.
- `/api/flag/attack`: was validating range against the CLIENT-SUPPLIED
  `attackerPosition` (trivially spoofable). Now uses the DB position; kept the
  ranged ATTACK_RANGE=5 (flag combat is ranged by design).
- NEW `/api/combat/attack`: the endpoint BeerBasePanel already POSTs — previously
  404 (every panel attack failed). Created with session auth + presence + battle
  resolution + Beer Base analytics + loot rewards.
- Live verification (dev server): factory attack from wrong tile → 403 with the
  target coords in the message; from own tile → 200 (rate-limit gate, then battle
  path); infantry PvP from away → 400 presence block; co-located → passed presence
  to the next legitimate gate ("No valid units"); Beer Base attack from away → 403.
- Pre-existing lint debt in flag/attack cleaned (10 errors → 0).
- Gates: cold tsc clean, eslint clean on all touched files, 333 tests pass.
- Note: server booted on a random port this session — operator shell has PORT=0
  (known SCOPE #23).


## Follow-up: Beer Base intel gating (2026-09-04, session 003)

Operator directive: army size, strength, defense, loot, and position must be
discovered organically — not displayed from afar.

- `/api/beer-bases/list`: session-authenticated (the `username` query param was
  another impersonation vector). Payload now distinguishes scanned/unscanned:
  standing ON a base's tile (Chebyshev 0, via lib/presenceCheck) reveals full
  intel — position, army size, STR/DEF, loot, specialization, tier. From afar
  only name, power tier (visible on the map), and coarse distance are returned.
- `BeerBasePanel.tsx`: union BeerBase type; unscanned cards render `???` with an
  "Unscouted — walk here to scan" hint; Power/Loot sort buttons removed (they
  would leak hidden intel); list re-fetches every 5s while open so the distance
  compass stays live and scanning flips as the player walks.
- Live verification: away → payload has no intel fields; on tile → full intel.
  Player position restored after the probe. tsc/eslint/333 tests green.

## FID-20260904-004 — Hotkey Single-Mapping Invariant (qweasdzxc reserved)

Operator directive: every key has exactly ONE mapping; the movement keys
`qweasdzxc` are reserved for movement only (bare press = move; panels may use
Shift+letter combos since MovementControls ignores modifier-held presses).

**FID flow (ECHO v0.1.2):** RED evidence cataloged (9 findings: BeerBasePanel on
bare E, DiscoveryLog on bare D, BotScanner on bare X, game-page C/F/P conflicts,
DEFAULT_HOTKEYS binding 5 bare movement keys, no central registry) -> FID written
to `dev/fids/FID-20260904-004-hotkey-single-mapping-invariant.md` -> audited ->
converged -> implemented -> implementation audited.

**Implementation:**
- `lib/hotkeyRegistry.ts` (new): MOVEMENT_KEYS set, `isReservedMovementKey()`,
  `bindingId()`, `findHotkeyConflicts()` (both rules, single source of truth).
- `types/hotkey.types.ts` DEFAULT_HOTKEYS: all bare movement-key bindings
  displaced to Shift combos (E->Shift+E Beer Bases, D->Shift+D Discovery Log,
  X->Shift+X Bot Scanner, C->Shift+C Clan View, S->Shift+S AutoFarm Stats,
  Q->Shift+Q Flag Tracker); fixed double-maps (F: Shift+F AutoFarm vs bare-F
  harvest -> Shift+F / Shift+V; P leaderboard vs specialization panel ->
  Shift+P for specialization).
- Components: MovementControls ignores modifier-held presses; BeerBasePanel,
  BotScannerPanel, DiscoveryLogPanel, SpecializationPanel all require their
  Shift modifier and reject Ctrl/Alt/Meta; game page C branch requires Shift;
  footer hint renders the live combo.
- Server boundary: PUT /api/admin/hotkeys validates every binding with
  `findHotkeyConflicts` and rejects reserved/duplicate maps with 400;
  HotkeyManagerPanel conflict detector now uses the same registry (admin UI
  can't save a violating config either).

**Gates:** cold tsc clean (after typing the SpecializationPanel API response
interfaces properly instead of Record<string, unknown>), eslint clean on all
touched files, 333 tests green.

**Live verify (server booted, port 50633):** GET /api/admin/hotkeys as admin
returned 21 bindings — zero bare movement keys, zero duplicate combos (both
invariants hold on the served config). PUT with `{action: BANK_PANEL, key: e,
requiresShift: false}` -> 400 rejected. E now moves Northeast only; Shift+E
toggles the Beer Base panel only.

**Residual:** stored game_config hotkeys row still holds legacy values if it
existed before; fresh DBs and defaults are clean. Admin can re-save to migrate.
