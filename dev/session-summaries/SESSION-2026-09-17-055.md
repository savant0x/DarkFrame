# SESSION-2026-09-17-055 - FID-20260917-014: referral validation on login (row 92)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** "File the referral-validation-on-login FID from row 92 and run its Perfection Loop to loop-complete."

## Loop findings that shaped the design (all probed live before writing)

1. **The trigger was only half the defect.** `referrals.loginCount` is written exactly once, at referral creation (referralService.ts:435, `loginCount: 1`) - so the 4-login criterion was unreachable by construction even with a trigger. The hook must maintain the counter itself.
2. **A dead Mongo block was occupying the natural seam.** The login route's post-auth block (:114-121) dynamically imported `lib/mongodb` to bump `players.lastActive` - a silent no-op since the pg pivot (Cluster-B-class residue). The hook replaces it exactly there. Recorded for the Mongo census.
3. **The reward path is rich and worth reusing as-is:** validateReferral distributes metal/energy/rp/xp (+50% flag-bearer bonus per FID-20260917-001-era flag rule), milestone titles/badges (jsonb appends), and VIP days; checkReferralValidation enforces the 7d+4 criteria.

## What shipped

- `lib/referralService.ts`: `processLoginReferralEvents(username)` - indexed pending-lookup early return; loginCount/lastLogin maintenance; criteria-gated chain into the real reward path. Never throws (login must not break). D1: every successful login increments. D2: invalidated + abuse-flagged excluded from AUTO-validation (admin manual validation still available).
- `validateReferral` claim guard (D3): the validated flip is now `UPDATE ... WHERE id = ? AND validated = 0 RETURNING id`; a lost claim returns false before ANY reward write. Concurrent logins (multi-device) or a racing admin validation can no longer double-pay.
- `app/api/auth/login/route.ts`: hook call replaces the dead Mongo block; zero `lib/mongodb` references remain (source-pinned).

## Verification

- Pins x8 (`__tests__/api/referralLoginHook.test.ts`): zero-cost early return; structural WHERE-column pin (new_player_username/validated/invalidated/flagged_for_abuse); increment + lastLogin with criteria unmet; end-to-end through the REAL reward path (claim/referrer/rewardsClaimed writes asserted); claim-race-lost -> exactly 2 writes; lost-claim validateReferral -> false; 7d+4 criteria still enforced; login-route source pin (hook wired, no mongodb import).
- Live probe (`scripts/e2eReferralLoginHook.ts`): real dev DB, two existing players + seeded 8-day referral - fires 1-3 unvalidated (loginCount 1-3), fire 4 VALIDATES (validated=1, validationDate, loginCount=4, referrer totalReferrals+1, metal rewards >= +100), fire 5 idempotent. Snapshot/restore cleanup; exit 0, 10/10 assertions.
- Full gates: tsc 0; eslint 0; census exit 0; suite 1040 passed + 1 skip (+8).

## Discipline notes

- Two harness defects found and fixed by drilling (not by weakening pins): (1) `vi.clearAllMocks` does not clear once-queues - leaked a structural-pin mock into later tests; switched to `resetAllMocks`. (2) The claim-race fixture queued the record as a bare object where the service reads `recordRows[0]` - the chain "never fired" because the fixture fed it nothing; diagnostics (call counts, then re-awaiting recorded select results) localized it to my fixture, and the service was proven correct.
- SCOPE row 92 -> Closed (implemented as row 93); row 93 tracks the FID (Open, fills on commit).

## Standing notes

- The dead `lastActive` write removal is recorded for Mongo census Cluster B bookkeeping.
- Login-route daily-streak accrual remains a separate candidate (FID non-goal).
