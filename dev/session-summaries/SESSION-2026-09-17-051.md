# SESSION-2026-09-17-051 — FID-20260917-009 implemented: the Stripe VIP money path is alive

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** file and implement the Stripe VIP remediation FID — username-keyed
lookups, webhook false-success fix, pins — queue-jumped ahead of the other clusters.

## What shipped

**`lib/stripe/subscriptionService.ts`** — the money path re-keyed:
- `grantVIP` (lookup :93 + update :123), `revokeVIP` (:173), `extendVIP`
  (lookup :211 + update :233), `checkVIPStatus` (:420) — every lookup now
  `eq(players.username, userId)`.
- `getUserByStripeCustomerId` — `id` projection aliased off `players.username`
  directly (the old `mongoId` alias + `|| username` fallback existed only
  because mongoId was empty; the alias was lying about which key it returned).
- JSDoc `@param userId` ×4 now says "(maps to players.username)".

**`lib/stripe/webhookHandlers.ts` (NEW)** + **`app/api/stripe/webhook/route.ts`**
— the false-success fix, wider than filed:
- ALL five event handlers moved out of the route module (Next route files may
  only export HTTP verbs; the first attempt to export `handleCheckoutCompleted`
  for pins failed the generated route-type check — TS2344). Route = transport +
  signature verification + dispatch.
- Checkout: grant=false → error log + throw, payment transaction NOT recorded
  (→ 500 → Stripe retries; the ledger cannot contain "completed" rows for
  ungranted VIP).
- Renewal + cancellation: throws added AND the catch-and-swallow blocks around
  them removed — reading the route revealed the false-success disease had
  three sites, not one; without the catch removal the new throws would have
  been eaten and the webhook would still have answered 200.
- Cancellation revoke=true records nothing (correct — revocation isn't a
  payment).

**Probe driver `scripts/e2eStripeVipKeying.ts`** — clone-row method with the
premise pinned: the clone of `fame` carries `mongoId: null` and stripped
uniqueness (email/referral), modeling the audited pg-era cohort exactly. The
lifecycle (grant → isolation → check → extend → revoke → zero-residual) is
proven under the production failure condition, not a convenient seed.

## Pins (11)

Service layer (`stripeVipKeying.test.ts`, ×6): WHERE-key assertions walk the
drizzle expression's `queryChunks` for Column nodes — username present,
`_id` (mongoId's physical column) absent — on grant/revoke/extend/check;
missing-player → false + no update; extend's Date arithmetic pinned from an
existing future expiration; Stripe-customer alias projects username.

Webhook behavior (`stripeWebhookFalseSuccess.test.ts`, ×5): checkout
grant=false → THROWS + no transaction record; grant=true → records exactly
one; renewal grant=false → throws (was swallowed); cancellation revoke=false
→ throws (was swallowed); revoke=true → resolves, records nothing. Service
partial-mocked (real module, overridden fns — handlers capture their imports
at module evaluation).

## Verification

- Pins 11/11; full suite **1015 passed + 1 skip** (was 1004; +11).
- Live probe **5/5, exit 0**: grant ok (vip=1, MONTHLY, exp ≈ now+30d), fame
  untouched, check echoes tier, extend advances exactly +7d, revoke nulls.
- Gates: tsc 0 · eslint 0 (6 files) · census exit 0 (push gate intact).

## Probe/gate-authored fixes, disclosed

1. Route-module handler export → TS2344 → relocation to lib/ (§ above).
2. Clone INSERT died on `email_unique` (fame's email rode along); the fix made
   the probe stronger (mongoId:null premise + uniqueness strip).
3. drizzle version exports `PgColumn`, not `Column` (pin TS2724).
4. A `git show :SCOPE.md` pre-check confirmed the index does NOT carry the
   queued batches' rows 82–85 — those live worktree-only across four staged-
   ready batches; row 86 appended with post-write assertions (exactly once,
   +1 line) after verifying the worktree's integrity (rows 80–85 exactly once).

## Board state

Five batches now queue staged-ready for the operator's commit word:
① census gate (hook + census + row 82 + session 047) · ② FID-008 inventory
batch (route + test + driver + FID + row 83 + session 048) · ③ Mongo census
audit batch (audit + row 84 + session 049) · ④ Stripe audit batch (audit +
row 85 + session 050) · ⑤ this FID batch (service + handlers + route + 2 pins
+ driver + FID + row 86 + session 051).

Cluster D's mongoId-column drop is now unblocked for the money path: lib/stripe
no longer touches mongoId at all.
