# Session 2026-09-03 (003) — Live-Credential Scrub + First Push to savant0x/DarkFrame

**Session ID:** SESSION-2026-09-03-003
**Date:** 2026-09-03
**Protocol:** ECHO v0.1.2 single-agent
**Task source:** Operator pasted the empty `savant0x/DarkFrame` quick-setup page ("push -u origin main, the github token is in .env.local GITHUB_TOKEN"); clarified `fame0528` is retired-but-theirs and `savant0x` is the active account; deleted `.env.example`; directed "move on to something useful".

---

## Intent (logged before implementation)

1. Repoint `origin` from the retired `fame0528/DarkFrame` to the new empty `savant0x/DarkFrame` and push `main` (operator-directed; the pasted quick-setup block is the instruction).
2. Before pushing: verify the tree carries no live credentials (pre-push secret sweep). Sweep found a real Atlas URI in 4 tracked files; operator authorized the push, deleted `.env.example` themselves, and the remaining files were scrubbed in-tree.
3. Record the exposure honestly: history still contains the URI (already public on the retired fame0528 repo); rotation is operator-side and remains open.

## Plan steps + evidence

| Step | Status | Evidence |
| ---- | ------ | -------- |
| Pre-push secret sweep of tracked tree | implemented | `git grep -nIE` over HEAD: 4 live-URI matches + ~15 placeholder matches (distinctions verified individually: `fame:` real vs `user:pass` placeholders) |
| Live-URI exposure context established | implemented | `git show 23cdc63:.env.example` contains the URI → already public on fame0528 at remote HEAD; `git log --all -S'REDACTED_MONGO_URI_<username>:<password>@ empty (URI predates local history) |
| `scripts/fix-player-schema.js` fallback scrubbed | implemented | `process.env.MONGODB_URI \|\| 'mongodb://localhost:27017/darkframe'` (env first, no live secret) |
| `dev/lessons-learned.md` + archived copy redacted | implemented | Real URI line → dated redaction note; `code_search` for the URI now returns 0 matches in tracked files |
| `.env.example` deleted (operator) and committed as deletion | implemented | `git status`: ` D .env.example` before staging |
| Remote repointed + pushed with upstream | implemented | `git remote set-url origin https://github.com/savant0x/DarkFrame.git`; `git push -u origin main` (operator-delegated execution) |
| Atlas password rotation | blocked | Operator-side action at the provider; tracked as SCOPE #19 |

## Addendum (2026-09-03) — history rewrite: sole identity, URI purged, force-pushed

Operator reported the pushed repo showed 4 contributors (fame0528, openclaudeai, codebuff-team, savant0x) and intended to delete the repo. Root cause found before deletion: all pre-rewrite commits were authored by the operator's personal email identity (redacted here 2026-09-03), one merge commit to `GitHub <noreply@github.com>`, and the two agent commits carried `Co-Authored-By: Codebuff` trailers — themselves a violation of the protocol's attribution rule.

`git filter-branch --env-filter` (all identities → `savant0x <296677002+savant0x@users.noreply.github.com>`) + `--msg-filter` (Codebuff trailers stripped) + `--tree-filter` (live Atlas URI → placeholders in historical blobs); reflog expired, gc pruned; force-pushed with the GITHUB_TOKEN credential helper from `.env.local` (`http.version=HTTP/1.1`; note: the repo pack is ~417 MiB, which is why pushes exceed 10-minute timeouts — the one that "hung" had completed as a detached process).

**Remote-verified evidence:** tip `53c1531` = local; `git log origin/main` → 28/28 commits sole savant0x identity; 0 Codebuff/co-authored trailers; 0 live-URI occurrences across remote history. Contributor graph will show only savant0x after GitHub recomputes. No tags existed; no other branches pushed. Working tree and all file contents at the tip unchanged by the rewrite (identity/message/history-blob edits only).

## Double audit record

- Method 1: post-scrub `git grep` = 0 live-URI matches in the new HEAD; `git push` output confirms remote tip.
- Method 2: 0-EOF re-read of all four edited files before commit; sweep regex re-run against the new tip.

## Out-of-scope discoveries

- None new; #19 (rotation) and the history-copy question (documented above, operator's call whether to also delete the retired fame0528 repo) remain open.

## Addendum 2 (2026-09-03) — final full-tree scrub before repo deletion

Operator decision: repo will be deleted regardless of contributor-graph state; final directive was "review the code, scrub everything". A 16-pattern secret sweep (Stripe/GitHub/AWS/Google/Slack/JWT/privkey/URL-credential/Sentry/Upstash/Ably/DB-URL classes) over the full tracked tree found **no live credentials** (all matches verified individually as placeholders — `user:pass`, `sk_live_...`, `your-key`, `...`, type-member names). Real findings scrubbed: the `fame0528` handle in 7 doc/script files (repo URLs and owner labels → `savant0x`; audit-trail references kept factual), and the operator's personal email in a `debug-login.js` usage example → `player@example.com`. Remaining handle/email mentions exist only in `SCOPE.md` and session summaries as the factual audit record the protocol requires.

## Operator decisions

- Push authorized (quick-setup paste + "move on to something useful" after `.env.example` deletion).
- #17 (checkpoint commit review) resolved implicitly by the push directive; `af1e61e` accepted as-is.
- Stash: kept (explicit answer earlier this session).
