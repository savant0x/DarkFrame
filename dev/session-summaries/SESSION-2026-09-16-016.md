# SESSION-2026-09-16-016 — Factory-void + latent-battle spec loop-complete (FID-20260916-008)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** record FID-006 policy decisions (already done earlier — D1/D2 in §9),
then file the factory-capture + latent-battle enforcement FID and run its loop.

## Grounding findings (all tool-verified, re-run in pass 1)

- **Factory:** the -002 owner-protection refusal exists (`factoryService:386`) but
  `grep -c voidProtectionOnAggression` → **0** — shielded players capture player-owned
  factories with no forfeit. Capture block :437-447.
- **Latent battle route:** `app/api/battle/attack` has **neither** refusal **nor** void
  (`grep -c "protectionActive|voidProtection"` → 0). This produced an **erratum to -006's
  matrix**, which claimed the void fired "inside resolveBattle" — function boundaries prove
  the void lives in `executeInfantryAttack` (battleService:675).
- **PvE-safety constraint (the spec's central constraint):** `resolveBattle` is shared with
  the LIVE beer-base raid (`combat/attack:325`), so the fix must not touch it. Pass 2
  refined this: `executeBaseAttack` — the function the in-code comment discusses — is dead
  (zero callers); the constraint rests on `resolveBattle`'s live-caller shared use, which is
  proven. Design unchanged: route-level seams only.

## Spec shape (final per §6)

Factory void after all four refusal preconditions, before the power roll, gated on
`pvpCapture` (player-owned + unprotected + non-bot — wild/bot captures never forfeit);
extends the existing `./playerProtection` import (:24). Latent route: defender refusal
(beer-base names skip, parity with -005 self-target) + attacker void before `resolveBattle`.
~5 pins + HTTP live probes; `resolveBattle` untouched.

## With this FID, the -006 matrix is fully dispositioned

Sabotage → -007 (`loop-complete`); recon + flag → D1/D2 no-code (FID-006 §9); factory +
latent battle → -008 (this FID). Remaining work on the protection track is implementation
gated on go-aheads: -007 and -008.

## Staging plan (G1 — presented, not executed)

```bash
git add dev/fids/FID-20260916-008-factory-void-battle-route.md \
  dev/session-summaries/SESSION-2026-09-16-016.md SCOPE.md
git commit -m "docs(fid): factory-capture void + latent battle-route protection spec loop-complete (FID-20260916-008)"
```

**Final status:** complete — FID-008 `loop-complete`.
