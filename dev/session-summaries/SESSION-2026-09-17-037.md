# SESSION-2026-09-17-037 — Gates re-verified · 49-commit push · shrine dead-economy cleanup (FID-20260917-002)

**Agent:** Buffy (Codebuff) · **Model:** z-ai/glm-5.3-flash · **Date:** 2026-09-17
**Continuation of:** SESSION-2026-09-17-036 (FID-20260917-001) + the session 037 SCOPE block

## Operator directives

1. "Run the three verification gates fresh to confirm the recorded baseline still holds"
2. "Reconcile the 49 unpushed commits on main with a staging/push plan" (+ "Run the three verification
   gates fresh" — queued together)
3. "Work the shrine-extend UI gap flagged in the feature survey" — the in-flight thread, approved to
   proceed via structured prompt; disposition fork resolved to **Option B (full dead-economy cleanup)**
   with **XP once per transaction**

## 1. Gate re-verification — baseline reproduced

Fresh runs (2026-09-17, before any change):

```
npx tsc --noEmit → TSC_EXIT=0
npm run lint     → LINT_EXIT=0
npm run test:ci  → Test Files 99 passed | 1 skipped (100) · Tests 975 passed | 1 skipped (976)
```

Zero drift vs the session-036 ledger.

## 2. Push reconciliation — 49 commits shipped

Probes (all read-only before the push): pure fast-forward (`origin/main` = direct ancestor, zero
remote-only commits) · all 49 authored solely `savant0x` · attribution-trailer scan → **0 matches**
(Co-Authored-By / codebuff / other agent names — the 2026-09-15 standing rule holds) · span
09-16 12:18 → 09-17 02:59 · G8: 38/49 exact `<type>(<scope>):` format, 11 omit the empty `(<scope>)`
parenthetical (type + description + FID-ID present; hook-accepted; rewrite declined as churn) ·
`git push --dry-run` exit 0 with the pre-push attribution hook scanning clean.

Operator answered **"Execute the push"** → `git push origin main` → `5117ecd..6235c89`, hook clean,
branch in sync with origin.

## 3. Shrine-extend: the survey gap dissolved

Grounding (fresh probes after the earlier search-tooling malfunction): `ShrinePanel`'s per-suit button
already reads **"Replace / Extend"** and calls `POST /api/shrine/activate`, which extends an active
boost (`timeRemaining + durationMs`, 8h cap). The orphaned `extend` route was **redundant**, not missing.
Real findings cataloged (FID §2, 9 rows with evidence):

- `extend`: zero callers, phantom `'speed'` tier, divergent rarity table (Rare 30/Epic 60 vs canonical
  `shrineHelpers` 60/90), legacy error style
- `sacrifice`: zero callers, fixed-cost model superseded — and the SOLE writer of `shrineTradeCount`
  + shrine XP → **SHRINE_DEVOTEE unearnable via the live UI**
- LIVE `activate`/`boost-all`: no server-side shrine-presence check (client gate only)
- sacrifice's error text pointed players at the dead `extend` route
- `ShrineSacrificeSchema` embedded the same phantom tier; sole consumer was the dead route
- Recorded, not absorbed ([OPEN-OUT-OF-SCOPE] #75): `GET /api/shrine/status` also orphaned;
  `activityLogger` maps two nonexistent shrine routes (`visit`, `boost`)

## 4. FID-20260917-002 — loop, then implementation (operator go-ahead via Option-B text)

- **Perfection Loop:** loop-complete on pass 2 — 1 Law-13 refinement (shared `assertAtShrine` helper
  instead of duplicating the tiles check in two routes); delta far under the 10% cap; Method-1 gates
  re-run at loop-complete: tsc 0 · lint 0 · vitest 975/1 (zero drift, document-only at that point).
- **Implementation** (Option B, operator-ratified): deleted `app/api/shrine/extend/` + `sacrifice/`;
  removed the `ShrineSacrificeSchema` block from `lib/validation/schemas.ts`; created
  `lib/shrineServer.ts` (`assertAtShrine` — fail-closed tiles check); wired into BOTH live routes:
  presence refusal after the player fetch, then ONE `trackShrineTrade` + ONE `awardXP(SHRINE_SACRIFICE)`
  per transaction (boost-all = one call = one trade per operator ruling), bookkeeping failures logged
  via log.warn and never reported as transaction failures, response enriched with
  `xpAwarded/levelUp/newLevel`.
- **Tests:** activate.test.ts rewritten — collection-aware mock (players vs tiles), bookkeeping modules
  mocked at the seam, all 5 FID-028 pins preserved + 4 new parity pins; boost-all.test.ts created
  (4 pins).
- **Mid-flight self-corrections (both repaired, disclosed in FID §7):** (1) one edit briefly corrupted
  boost-all's `$set` (`existingBoosts 4/4`) — caught on the edit-output read, repaired,
  `CORRUPTION_RESIDUE=0` sweep; (2) post-deletion tsc showed 6 errors entirely inside stale
  `.next/types/` generated artifacts (tsbuildinfo-class hazard) — artifacts removed, cold tsc → 0.

## Completion evidence (fresh, post-final-edit)

```
npx tsc --noEmit → TSC_EXIT=0
npm run lint     → LINT_EXIT=0
npm run test:ci  → Test Files 100 passed | 1 skipped (101) · Tests 983 passed | 1 skipped (984)
                   (975 baseline → +8 new pins)
```

Law-4 greps: ShrinePanel.tsx:252/:294 live · parity writers activate:189-190 + boost-all:200-201 ·
`ShrineSacrificeSchema` census → 0 · `assertAtShrine` def + 2 production call sites · dead-route refs → 0.

## Post-approval streams (same session)

**Commit 1 — FID-002 fix (`b11c370`):** staged exactly the planned paths, G8 message verbatim, 8 files
+366/−476; G2 hash captured; FID-002 flipped `verified` → `closed`.

**SCOPE #75 resolution (operator picked it alongside the E2E directive):** deletion censuses re-run
(status callers = 0 outside its own header; `SHRINE_VISIT`/`SHRINE_BOOST` = exactly 4 refs — the 2 dead
mappings + their definitions; `SHRINE_COMPLETE` = 0 refs, deleted with them; no activityLogger tests).
`app/api/shrine/status/` deleted; both dead mappings + the 3 shrine action-type enum members removed
(replaced by an explanatory comment). Gates: tsc 0 (2 stale `.next/types/` artifacts swept first — same
hazard class) · lint 0 · vitest **983+1 byte-identical** — proving the deleted code was fully inert.
Commit **`16a7fcb`** (+3/−107).

**Live E2E — PASSED 20/20 (operator-directed run):** a dev server was already running on `:3000` (PID
53456; my :3002 spawn exited on Next's one-instance-per-dir guard — harmless). Against it:
`E2E_BASE=http://localhost:3000 npx tsx -r dotenv/config scripts/e2eShrine.ts` → **all 20 checks, exit
0**: off-shrine activate refused 400 with verbatim presence message + ZERO mutation (inventory intact,
no boost, no trade); on-shrine activate 200 with `xpAwarded: 40` + `shrineTradeCount: 1` + spade boost
written + item pruned; boost-all 200 with `xpAwarded: 40` exactly once + all four tiers active +
`shrineTradeCount: 2` (ONE more trade, not four). Driver defect (test-side only): the first run failed
its own message assertion — refusal text lives at `error.details.message` in the `createErrorResponse`
envelope (the SCOPE #59 lesson); system behavior was verbatim-correct on the first live call, the
assertion was fixed and the re-run clean. One probe account retained per convention
(`shre2eP6355511`), cleaned by the next run's guarded `shre2e%` sweep.

**Operator defect report — base artwork stuck at level 1 (FID-20260917-003):** "was the user artwork
properly wired to the users level?" — NO. Diagnosis, FID, loop, implementation, and closure all
executed this session (operator selected "File FID + implement"); full record in SCOPE session-038
block + the archived FID. Commit **`57dbfef`**; vitest **989+1**.

**Base-artwork visual verification (operator-directed):** render chain proven live — DB probe:
`fame` = **level 19, rank 1** (the exact old-bug input pair); live server serves `bases/2.jpg` and
`bases/1.jpg` (HTTP 200); `levelToBaseTier(19) = 2` pinned in the suite. Final pixel confirmation is
one browser refresh on the operator's side (the running dev server hot-compiles current HEAD, so the
fix is already live).

**Closure batch:** both FIDs archived to `dev/fids/archive/` (live dir empty); CHANGELOG 0.0.4 entry;
VERSION 0.0.3 → 0.0.4; SCOPE rows finalized (#75 closed, session-038 block added); push executed
`6235c89..6d34914` (hook clean); FID-002 post-closure addendum records the live E2E pass.

## Ledger

FID-20260917-002 status **`verified`** — closes on commit per G2 (hash pending). SCOPE session-037
block finalized + row #75 filed. Staging plan (presented, not executed — G1):

```
git add app/api/shrine lib/shrineServer.ts lib/validation/schemas.ts \
        __tests__/api/shrine dev/fids/FID-20260917-002-shrine-dead-economy-cleanup-and-parity.md \
        dev/fids/archive/ SCOPE.md dev/session-summaries/SESSION-2026-09-17-037.md \
        CHANGELOG.md VERSION
```

Commit message (G8): `fix(shrine): delete dead sacrifice/extend economy, wire trade parity + presence enforcement (FID-20260917-002)`
