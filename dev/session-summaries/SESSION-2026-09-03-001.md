# Session 2026-09-03 — WMD Schema Completion, Lint Burn-down, Repo Relocation

**Date:** 2026-09-03
**Project home:** `C:\Users\spenc\dev\DarkFrame` (moved from `D:\dev\DarkFrame` late in the session)
**Protocol:** ECHO v0.1.2 single-agent
**FIDs:** FID-20260903-002 (WMD phantom tables) implemented; FID-20260903-001 (typed Mongo-compat seam) implemented

## What happened

1. **WMD lint burn-down completed to zero.** researchService (27 findings) got an exported
   `PlayerResearchRow` type plus an honest `rowToPlayerResearch` mapper computing the domain's
   nested `currentResearch`; all `(pr as any)` casts removed. Completing research now also
   increments `totalTechsUnlocked` (was silently never incremented).
2. **Dead-table discovery.** `wmd_votes` was read by analytics/admin services but written by
   nobody. All voting analytics, health metrics, and the admin force-expire action were
   rewritten against the live `wmd_clan_votes` table (approval computed from
   `votesFor`/`votesAgainst`, participation via clan-members join).
3. **Remaining WMD files (19 findings) burned**: typed `getPlayerMissiles`/`getPlayerData` to
   `$inferSelect` rows; `generateIntelligence` now returns a schema-true `IntelligenceReport`;
   example-file functions exported; dead mission-status query (`'IN_PROGRESS'`, never written)
   fixed to `'ACTIVE'`; counter-intel route no longer passes `null` as sweep area; clanless
   players get an explicit recruitment rejection instead of a magic `'UNASSIGNED'` clan id.
4. **`protection_until` restored** to `players` (lost in the pg migration — the new-player
   protection check was silently dead). Migrations 0004/0005 generated and applied.
5. **Font Awesome** `@fortawesome/fontawesome-free` v7.3.1 installed and wired in `app/layout.tsx`.
6. **Repo relocated to NTFS.** Root cause of all build failures: exFAT cannot store
   symlinks/junctions, and Node 25.2.1's `fs.readlinkSync` throws EISDIR on regular files there
   (breaking webpack, Turbopack, and tsx). After the move, the environment bugs vanished.
7. **Next 16.3-canary build fixes** (all real defects surfaced by the stricter checker):
   - admin/profile/tech-tree pages took an `embedded` prop but are rendered as tabs by
     `app/game/page.tsx` → extracted `AdminView.tsx`, `ProfileView.tsx`, `TechTreeView.tsx`,
     leaving 5-line route pages. WMD page's dead `embedded` prop deleted.
   - middleware wrappers typed `context` as optional `{ params: any }` → required
     `{ params: Promise<Record<string, string>> }` in requestLogger/rateLimiter/activityLogger.
   - **7 missing `ENDPOINT_RATE_LIMITS` keys** (`ADMIN_OPERATIONS`, `AUTH`, `BANK`,
     `FAST_TRAVEL`, `TIER_UNLOCK`, `adminVIPGrant`, `adminVIPRevoke`) — routes called
     `createRateLimiter(undefined)`, which crashes at import. Latent runtime breaker.
   - game page: auto-farm combat handler stuffed player-combat fields into `attackResult`,
     whose renderer calls `.playerPower.toLocaleString()` → runtime crash. Removed; typed the
     three `any` states to real domain types (`HarvestResult`/`AttackResult`/`Factory`).
   - 36 lint findings burned across every file touched during the move (Law 15).

## Final gate status

| Gate | Result |
| --- | --- |
| `npx tsc --noEmit` | 0 errors project-wide |
| `npm run build` | PASSES (first successful production build on this machine) |
| eslint on all touched files | 0 findings |
| Migrations | 0003–0005 applied to live Supabase; schema ↔ DB in sync |

## Notes for next session

- `D:\dev\DarkFrame` is a stale fork — the operator intends to delete it. Mainline git history
  lives on C: (`23cdc63` Merge branch 'good-raisin'); mainline-only extras (husky, ~40 admin
  routes, logger migration) are recoverable from that history if ever needed.
- Node runtime is still v25.2.1 (non-LTS); `.nvmrc` requests 22. Works on NTFS, but LTS is
  recommended.
- `dev`/`build` scripts remain pinned to `--webpack` (safe on NTFS too); Turbopack is now
  viable again if desired since the drive is NTFS.
