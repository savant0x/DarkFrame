# Session 2026-09-16 (034) — FID-20260916-013 territory-capture: exploration, partial implementation, amendment, redesign directive, HANDOFF

**Outcome:** session ended early by operator. FID-20260916-013 is **NOT converged**
(status `analyzed` in-file). Working tree carries **partial, unverified, uncommitted**
implementation work. Operator directed a redesign of the capture mechanic from first
principles and is handing the project to a different (larger) model for continuation.

---

## 1. Session timeline (factual)

1. **Project exploration.** Read README, package.json, SCOPE.md (all 1,095 lines),
   CHANGELOG head. Established: gates at last record tsc 0 / eslint 0 / vitest
   951+1skip; sole active FID was 20260916-013 (territory-capture UI, loop-complete);
   recent commits = protection-window track (-002..-009), post-survey feature
   completion (-010..-012), toolchain modernization, streak-bonus curve.
2. **"Start FID 013."** Read the FID 0-EOF plus: capture POST route, full
   clanWarfareService (833 lines pre-edit), ClanTerritoryPanel (482 lines),
   /api/clan/wars route, clanWarfareV2 test, clan.types role tables.
3. **Implementation began** (before the defects below surfaced): added
   `deriveAttackerStrength` + `getCaptureTargets` to clanWarfareService, created
   `app/api/clan/warfare/capture/targets/route.ts`, began ClanTerritoryPanel capture
   flow (imports + `canCapture` gate + response types only).
4. **Halt — three defects found in the capture flow itself** (presented, not fixed):
   - **A1:** success-path transaction writes treasury keys computed from the
     ATTACKER's balance into the DEFENDER's row (line ~507-516); attacker never
     charged. Defender treasury corruption, possibly negative.
   - **A2:** repelled captures return `{ success: true, captured: false }`; route
     forwards `success` verbatim → a UI built on the route's own documented contract
     ("Failed Capture (success: false)") would toast victory on a repel.
   - **A3:** treasury-short + daily-cap refusal messages fall through the route's
     error mapping to the generic 500 (business-rule branch matches only
     'No active war'/'not owned by target').
5. **Operator: "amend it, then re-run the perfection loop."** Amendment written into
   FID-013 (§5.0 A1-A3 with line evidence; loop table). Initial loop verdict was
   prematurely declared loop-complete — later retracted by the agent itself.
6. **Audit of own amendment found more problems:**
   - The candidate strength curve guarantees level-10 attackers win every roll
     (min roll 8500 > max wall 7500) — captures deterministic for all
     war-declaration-eligible clans; the FID's own "reliably wins" phrasing had
     been overstated; probability cells corrected via arithmetic probe
     (level 0 vs 0% wall = 50%, not the ≈43% first written).
   - Snapshot-then-transaction writes (treasury, territory arrays, daily counters
     all read before `db.transaction`) are not concurrency-safe under simultaneous
     claims/captures/distributions.
   - `getCaptureTargets` uses unordered `limit(1)` on outgoing wars; declarations
     enforce pair-uniqueness only, so multiple outgoing ACTIVE wars are possible —
     the enumeration design is wrong.
7. **FID status set `analyzed`, amendment-loop verdict re-written to NOT CONVERGED**
   with a final-audit decision gate (balance decision D5, transaction redesign,
   multi-war enumeration).
8. **Operator standing directive:** legacy (GPT-4-era) plans are NOT concrete — when
   something is wrong or low quality, inform the operator, run the Perfection Loop,
   and **redesign properly against the current codebase**. All capture constants,
   formulas, attacker-only rule, costs, caps are back under design review; the
   partial implementation is NOT a foundation to preserve.
9. **Operator ended the session**, citing model-qualification risk for a valuable
   project, and requested this handoff for a larger model.

## 2. Working-tree state (uncommitted — reconcile first)

```
M  components/clan/ClanTerritoryPanel.tsx   # capture imports + canCapture gate + response types ONLY; no flow wired
M  dev/fids/FID-20260916-013-territory-capture-ui.md   # amended: §5.0 A1-A3, §5 header "Proposed spec (GREEN reopened)", §6 NOT-CONVERGED verdict + final audit; Status: analyzed
M  lib/clanWarfareService.ts                # deriveAttackerStrength (partial, untested), getCaptureTargets (multi-war bug, untested), captureTerritory strength call-site (pre-A1 fix — still has the A1 treasury bug in the success path)
?? app/api/clan/warfare/capture/targets/route.ts   # new GET, untested, depends on buggy getCaptureTargets
```

**Nothing in this session was verified** — no gates were run against the edits.
Recommended disposition for the successor: review the diffs and either keep as
scaffolding for the redesigned implementation or revert; do NOT treat any of it as
tested. The FID document edits are accurate as of the final state and worth keeping.

## 3. Verified defect inventory (line evidence, current code)

All in `lib/clanWarfareService.ts` (post-edit line numbers approximate; the file was
re-read at each stage) and `app/api/clan/warfare/capture/route.ts`:

| ID | Defect | Evidence |
|----|--------|----------|
| A1 | Success capture corrupts defender treasury (attacker-derived values written to defender row); attacker never debited | success tx: defender `.set()` carries `bankTreasuryMetal/Energy: treasury.metal − cost` (attacker's `treasury`), `.where(eq(clans.id, targetClanId))`; attacker update has no treasury keys |
| A2 | Repel surfaces as success to the route/UI (`success: true, captured: false`) | repel return in attemptTerritoryCapture; route forwards `success` only |
| A3 | Cap-reached and treasury-short refusals → HTTP 500 (should be 400 verbatim) | route error-mapping branch lacks 'Daily capture limit reached' and 'Capture costs' prefixes |
| S1 | `attackerStrength = 0` at the route seam → every capture guaranteed-repelled while still charging cost + burning a daily attempt | captureTerritory → attemptTerritoryCapture(..., 0); strength math `0 * jitter >= 5000` always false |
| C1 | Snapshot-then-write concurrency hazard across capture/claim/unclaim/distribution | all reads (treasury, territories jsonb, captureDay counters) precede `db.transaction`; writes recompute from snapshots |

Design-level (not defects of code but of the old plan): the level-only strength
curve makes captures deterministic at level ≥ 10; defender clan level has zero
weight in the wall (adjacency only, max +50%).

## 4. Handoff directives for the successor (operator-issued)

1. **Standing rule:** legacy docs/plans are proposals only. When wrong or low
   quality → inform operator → Perfection Loop → redesign against current code.
   Do not patch-to-preserve old formulas.
2. **FID-013 must be re-designed from first principles** as a territory-warfare
   mechanic review: what attackers commit, what defenders can do, how ownership
   changes, economy impact, concurrency model. The old constants (25k/25k, 3/day,
   5000-wall, ±15% jitter, attacker-only) are all back in scope for redesign.
3. Present grounded recommendations with tradeoffs; do not ask the operator to
   invent numeric balance targets from nothing.
4. The session's partial code is unverified scaffolding — reconcile (keep or revert)
   before any new work. Gates must be run fresh (tsc / eslint / vitest) before any
   completion claim; none were run this session.
5. G1 holds: agent prepares commits, operator executes (no git execution by the agent).

## 5. Open decision queue (for the successor's redesign)

- **D5 (blocks FID-013):** contested vs guaranteed capture at war-declaration level;
  if contested — should defender clan level enter the wall, and what odds for
  equal-level clans on an isolated tile?
- Multi-outgoing-war enumeration shape (targets read + UI selection).
- Concurrency approach for treasury/territory jsonb writers (C1) — likely
  declarative SQL writes (jsonb ops / relative updates) instead of snapshot
  rewrites; needs its own design pass with the schema.
- Whether `captureTerritory` (v1 wrapper) should survive at all or the route should
  call `attemptTerritoryCapture` directly post-redesign.
- Disposition of the four uncommitted paths above.

## 6. Honesty notes (protocol compliance)

- The agent prematurely declared the amendment loop converged once; corrected by its
  own re-audit in the same session (the FID §6 now records both the retracted verdict
  and the final audit).
- One loop-record probability cell was written from mental arithmetic and later
  corrected via an explicit probe (43% → 50% on two cells; level-5/40% case added).
- No verification gates were run this session; no completion claims are backed by
  tool output. Every uncommitted edit must be treated as unverified.
- The successor should re-read: FID-20260916-013 (full), SCOPE.md rows 73-74,
  protocol.config.yaml (Law-16 + loop-complete vocabulary), and the operator
  directive in §4.1 above.
