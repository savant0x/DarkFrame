# SESSION-2026-09-02-009 — Lint burn-down: territoryService.ts (33 findings)

**Date:** 2026-09-02 | **Protocol:** ECHO v0.1.2 single-agent | **Operator instruction:** "Continue the lint burn-down with the territoryService.ts batch (33 findings, next-highest density)."

## Scope (approved)

- Remediate `lib/territoryService.ts` lint findings (33 = 24 `no-explicit-any` + 9 `no-unused-vars`) with honest types; remove the dead imports/assignment they point at. No `any`, no rule suppression, no other files.

## Law 1 evidence (pre-edit)

**Findings breakdown (eslint):** 24 `no-explicit-any` + 9 `no-unused-vars` — 8 dead drizzle-operator imports at line 29 (`and, gt, lt, gte, lte, desc, asc`), dead `players` schema import, dead `updateResult` at 717. File: 828 lines.

**Finding families:**

1. Compat-cast selects: `(clanResult as any)[0]` ×7 — only because `clans.members`, `activePerks`, `territories`, `bankTransactions` are `json().$type<any[]>()` in the schema.
2. Jsonb element callbacks: `(m: any)`, `(t: any)` ×12.
3. Raw-SQL `db.execute` results: `(clanResult as any).length/[0]` ×5 sites + `last_territory_income_collection` reads ×4.
4. `catch (error: any)` ×2 (537, 757) → optional catch binding.
5. `updateResult` assigned but unused (717).

**Law 2 discoveries (pre-existing, recorded — NOT silently fixed):**

- **The file is NOT tsc-clean today**: 13 pre-existing errors inside the 2,043 baseline — 9 dialect-purgatory (`MySqlTableWithColumns` not assignable to `PgTable`, lines 108/212/265/295/363/446/648/790 — FID-20260902-001-owned, unfixable without the DB decision) + **4 `QueryResult` errors (139, 479, 678, 815) that prove runtime-dead checks**: `db.execute` on node-postgres drizzle returns pg `QueryResult` (rows under `.rows`), so the un-cast `.length > 0` guards evaluate `undefined > 0` → always false. Most severe: the **income double-collection dedup in `collectDailyTerritoryIncome`/`getProjectedTerritoryIncome`** can never trigger — a second same-day call re-collects. The `as any` casts were masking this; the un-cast sites expose it to tsc.
- tsconfig: `strict: true`, `noUncheckedIndexedAccess` OFF (verified) → row indexing needs no undefined-guard churn.
- FID-20260902-001 already inventories this file's raw MySQL SQL (JSON_CONTAINS, JSON_OBJECT — flagged for Option A translation).

**Fix strategy (recorded intent):** typed local `TerritoryRow` domain interface replacing `any[]` annotations at use sites; drizzle select results typed via local row types; raw-SQL results typed with an explicitly-named local row shape that preserves today's runtime behavior (including its dead guards) — the 4 runtime-dead guards stay a FID decision, not a lint-batch side effect.

## Step Status Ledger

| Step | Status |
| ---- | ------ |
| Law 1: findings breakdown + file 0-EOF + schema/driver/tsconfig ground truth | implemented |
| Scope + intent logged (SCOPE.md + this file) | implemented |
| GREEN: fix 33 findings with honest types | implemented |
| AUDIT: file lint 0 + repo lint 1,869 → 1,836 + tsc 2,043 → 2,039 (−4, exactly the resolved QueryResult errors) | implemented |
| Re-read changed files + sync tracking docs | implemented |
| Close: session record + SCOPE.md + temp cleanup | implemented |

## Evidence

- **File lint: 33 → 0**; repo total **1,869 → 1,836 (exact −33)**, 3 warnings untouched.
- **tsc: 2,043 → 2,039 (−4, exactly the resolved `QueryResult` baseline errors)**. The file keeps
  exactly its 9 FID-owned dialect-purgatory errors (new locations 159/265/318/348/421/504/706/774/848);
  zero new errors; no new findings elsewhere.
- Residual `any` grep in file: 0 (one comment describing the schema's `$type<any[]>()` is the only hit).
- Zero rule suppressions; zero behavioral changes beyond the recorded ones.

## Outcome

All 33 findings remediated with honest types:

- **Type block** (8 exports): `ClanMemberRow`, `ClanPerkRow`, `ClanBankTransactionRow`, `ClanRow`,
  `LastCollectionRow`, `ClanSqlRow` (+ type alias with a QueryResult-caveat docstring) — each raw-SQL
  result now names its row shape, replacing the `as any` casts.
- **Unused vars (9)**: the 8 dead drizzle-operator imports and dead `players` import removed; the dead
  `updateResult` assignment dropped.
- **Catches (2)**: `error: any` → optional catch binding with `instanceof Error` message extraction
  (behavior-equivalent).
- **`perk.bonus.value`** (×2): `?? 0` guard — honest optional typing without behavior change.
- **`getTerritoryAt`** (behavior fix per recorded intent): `claimedAt` is normalized to `new Date(...)`
  so the function keeps its own `{ claimedAt: Date }` contract regardless of jsonb string-vs-Date
  serialization, plus an explicit null-guard for a missing tile.
- **Runtime-dead `QueryResult` guards (4 sites)**: preserved exactly as designed (documented inline,
  FID-20260902-001 owns the dialect fix) — but now they are *typed and documented*, not masked by casts.

**Latent bug surfaced (recorded, NOT fixed):** the income double-collection dedup in
`collectDailyTerritoryIncome`/`getProjectedTerritoryIncome` can never trigger today — `db.execute` on
node-postgres resolves to a pg `QueryResult` (rows under `.rows`), so the `.length > 0` guards evaluate
`undefined > 0` → always false, and a second same-day call re-collects income. Fixing it changes runtime
behavior and is owned by the DB-direction decision; recorded here and in SCOPE #11.

Tracking docs synced: progress.md header + correction table (2,039), QUICK_START gate table + commands,
issues.md burn-down chain.
