# SESSION-2026-09-16-005 — FID-20260916-003 filed (protection forfeit edges debate)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent · **Branch:** `main`
**Operator directive:** add a debate FID — should protection also break on harvesting contested tiles or joining a clan, not just outgoing attacks?

## Grounding (all claims tool-verified before writing)

- Template re-read 0-EOF post-amendment (status vocabulary, no attribution fields, archival-only-at-closed). `dev/fids/` held only -002 → next number **003**.
- Void coverage: `grep -n "voidProtectionOnAggression" lib/battleService.ts` → import :57, single call :675 (infantry path only).
- Harvest: `app/api/harvest/route.ts` read 0-EOF — dispatch purely on `tile.terrain` (Metal/Energy/Cave/Forest); **no tile-ownership or contested mechanic exists**. Schema `tiles.ts` has only `baseOwner` (bot bases).
- "Contested" exists only in `lib/clanWarfareService.ts` (war-gated territory capture, treasury-funded).
- Clan join: `app/api/clan/join/route.ts` — **invitation acceptance** (`joinClan(validated.invitationId, auth.playerId)`).
- WMD: `grep -rn "protection" app/api/wmd/ lib/wmd/` → only target-side refusal in `targetingValidator.ts:37-38`. **Launch side has zero protection interaction** — the sharpest hole found.

## Deliverable

`dev/fids/FID-20260916-003-protection-forfeit-edges-debate.md` (`created`, MEDIUM) — a decision record, not a fix proposal:
- RED table: 6 findings with file:line evidence.
- Option space: **A** violence-only (+ refuse protected WMD launch) / **B** outgoing-aggression forfeit (WMD launch voids; clan join forfeits only if the clan is at ACTIVE war) — **recommended** / **C** any-clan-join forfeit. Rejected outright: forfeit-on-attacked, forfeit-on-neutral-harvest.
- Design principle extracted: the shield protects *arrival*, not *action* — forfeit what projects force.
- §7 explicitly blocked on operator decision; the enforcement-spec loop (if approved) runs in a follow-up FID.

## Bookkeeping

- SCOPE row #60 (Open — decision pending operator).
- Staging plan in FID §8 (this FID + this session record + SCOPE.md).

## Files touched

- `dev/fids/FID-20260916-003-protection-forfeit-edges-debate.md` (new)
- `SCOPE.md` (row #60)
- `dev/session-summaries/SESSION-2026-09-16-005.md` (this file)

Note: Freebuff restarted before this session's work; no processes were left running (probe server was already torn down last session). No code touched this session.
