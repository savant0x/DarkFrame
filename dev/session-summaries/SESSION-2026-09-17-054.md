# SESSION-2026-09-17-054 - Seven-item work order executed (FID-20260917-011)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** "alright, lets make a fid to approve all 7, run perfection loop on the fid then present for final approval" -> approved -> execute items 0-7 without further check-ins.

## Per-item outcomes

| Item | Commit | Result |
|---|---|---|
| 0 | `233bbca` | Work-order FID filed (loop-complete; premises re-probed live) |
| 1 | `296b48c` | FID-010 alliance-mount batch + SCOPE mojibake repair (21 sequences, rows 86-88) |
| 2 | `85a03b4` | Triple closure FID-008/009/010: s8 hashes, archives, SCOPE flips, CHANGELOG 0.0.9, VERSION (amended to carry the s8 fills) |
| 3 | `d89ac93` | Chat honesty: chat_reports + blocked_users tables, POST /api/chat/report, /api/chat/block (GET/POST/DELETE), server-side enforcement in chatService (viewerId row filter) + messagingService (conversation filter), admin moderation type=reports, honest Report/Block buttons on live ChatPanel rows, orphan ChatMessage stubs rewired. Pins x12 |
| 4 | `d51992f` | Tutorial /complete deleted (zero callers re-verified; census exit 0 post-deletion) + FID-013 |
| 5 | `af7afae` | Row 11 closed as ALREADY HEALED: the dedupe guard now re-checks the locked row under withClanTreasuryLock (FID-20260917-001 rework); JSON_CONTAINS checks are typed builder queries; repo-wide sweep of the .length-off-QueryResult class: zero live instances |
| 6 | `7de2230` | Referral cron decoy deleted (script + npm wiring); guide truthed (crontab, Vercel-cron route that never existed, troubleshooting steps); rows 22/23 re-probed and confirmed already-Closed; row 92 files the follow-up candidate (wire pg referral validation on login) |
| 7 | (this commit) | Session record + row 89 closure |

## Gate defects found and fixed during item 3 (per discipline: stop, disclose, fix)

1. **Stale generated types**: `.next/types/app/api/tutorial/complete` produced 2 phantom
   TS2307s after the deletion (item 4) - cleared, not a source defect.
2. **TS18048 x3 (test surface, real root cause in route)**: the block route's
   `requireUser` `as const` union merged into dual-optionals; under
   `exactOptionalPropertyTypes` tsc inferred `Promise<NextResponse | undefined>`
   for both handlers (proved via a throwaway type-print probe file, then deleted;
   throwaway committed 0 bytes of residue). Fixed by retagging to an
   `{ ok: false; error } | { ok: true; user }` discriminant; call sites guard
   `if (!auth.ok)`. House note: under exactOptionalPropertyTypes, prefer tagged
   discriminants over `in`-checks on `as const` pairs for auth-shaped helpers.
3. **TS2322 x2**: test passed `'global'` string literals where the `ChannelType`
   enum is required - fixed to `ChannelType.GLOBAL`.
4. **eslint 2+1**: unused mock destructures (`blockDelete`, `getBlockedUsernames`)
   removed; exhaustive-deps warning fixed by adding `message.senderId`.

## Findings that changed the work order's shape (evidence over plan)

- Item 3's survey premise had drifted: the lying report/block buttons live in
  `ChatMessage.tsx`, which is mounted NOWHERE (the live ChatPanel renders its own
  rows with working edit/delete). Scope corrected: real backends + affordances in
  the LIVE panel + orphan stubs made honest. FID-012 s3b records the truth pass.
- Item 5's promised repair had already shipped inside FID-20260917-001; the pin
  work was replaced by a repo-wide class sweep (clean).
- Item 6's rows 22/23 were already Closed with evidence; fresh probes (401 on all
  three fetched admin routes; PORT guard in tree) re-confirmed rather than flipped.

## Gates at closure

tsc 0 - eslint 0 (12 touched files) - full suite 1032 passed + 1 skip (+12 chat
pins) - inverted census exit 0 (post tutorial deletion) - commit-msg gate
enforced footer-free messages (attribution trailer rejected on first attempt,
recommitted per house style).

## Standing notes

- Row 92 (referral validation trigger) is the only new open item created by this
  work order, filed as a candidate awaiting operator approval.
- Work order non-goals remain: Mongo Clusters B-D, chat polish, CMV unification.
