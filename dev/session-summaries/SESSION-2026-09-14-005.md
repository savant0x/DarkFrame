# SESSION-2026-09-14-005 — Implement FID-20260914-004 (shim hardening)

**Date:** 2026-09-14 · **Protocol:** ECHO v0.1.2 single-agent

**Task source:** operator directive — "Implement converged FID-20260914-004: honest
update/delete counts via .returning(), the jsonb_agg $pull rewrite, and real $addToSet
semantics — full gates and evidence."

---

## Intent (logged before implementation — Law 8)

Ship the converged GREEN exactly as specified: (A) replace the shim's dead `jsonb - jsonb`
`$pull` fragment with the probe-verified `jsonb_agg` rewrite; (B) make every
update/delete/bulkWrite count honest via `.returning()` (upsert branches included, per
the loop-2 refinement); (C) give `$addToSet` real set semantics via the containment
guard. Zero caller edits — the seven failure branches and three reporting sites start
working unchanged. Full gates + live evidence; FID → `verified` (closes on commit per G2).

## Implementation

- `lib/mongodb.ts`: `$pull` → `coalesce((SELECT jsonb_agg(e) FROM
  jsonb_array_elements(coalesce(col,'[]'::jsonb)) e WHERE e <> <operand>::jsonb),
  '[]'::jsonb)`; `$addToSet` → `CASE WHEN col @> [operand] THEN col ELSE col ||
  [operand] END`; `updateOne`/`updateMany`/`deleteOne`/`deleteMany` chain bare
  `.returning()` and report `rows.length`; empty-set updates return 0 honestly;
  upsert insert branches count returned rows; `bulkWrite` sums real per-op counts.
- Tests: `__tests__/lib/shimUpdateSemantics.test.ts` (13 cases) over
  `__tests__/lib/fakeDrizzle.ts` (scripted drizzle layer — CI is DB-less; drizzle-0.45
  StringChunk-aware SQL-shape flattener).
- Probe extended (`scripts/probePullObjectOperand.ts`): shipped fragments verified live,
  exit 0 — `$pull` object + scalar, JSON-null element PRESERVED, `$addToSet` guard
  absent/present × scalar/object; legacy jsonb-minus legs kept as expected-failing docs;
  exit gates only shipped shapes.
- Live seam verification (`scripts/verifyShimSemanticsLive.ts`, exit 0, fresh-boot
  server on :3002 serving the new shim): scratch players via the real register route;
  matching `$set` → 1, **non-matching filter → 0 (the honest-failure proof)**, `$inc`
  fragment → 1, `$push`+`$pull` over `players.units` removes exactly the matching unit,
  `$addToSet` absent-tier appends / present-tier does not duplicate, `deleteMany` →
  real 2. Route-level drive: abandon-nonexistent → honest `Factory not found` failure.
  Cleanup guarded (`scripts/cleanupShimVerify.ts`), residual 0/0.

## Implementation discoveries (Law 1, disclosed in FID §7)

1. The upsert `onConflictDoNothing` branch is dead code — the upsert block is gated on
   `upsert && setPayload` while that branch requires `!setPayload`; empty-update upserts
   fall through to the final honest `modifiedCount: 0`. Not widened (no live caller).
2. The abandon route's HTTP path is gated by its `findOne` pre-check; the
   `modifiedCount === 0` branch behind it was previously DEAD (fictional count) and is
   now live defense-in-depth.
3. Test-harness findings recorded for posterity: drizzle 0.45 `StringChunk.value` is a
   string[]; column-key aliasing (`resources_metal` input → `resourcesMetal` recorded).

## Verification evidence

| Gate | Result |
| --- | --- |
| `npx tsc --noEmit` | exit 0 |
| `npm run lint` | exit 0 |
| `npm run test:ci` | 76 files passed / 1 skipped · **760 passed / 1 skipped / 0 failures** (+13 over the 747 baseline — the new suite) |
| `scripts/probePullObjectOperand.ts` | exit 0 — ALL SHIPPED FRAGMENTS VERIFIED |
| `scripts/verifyShimSemanticsLive.ts` | exit 0 — ALL SEAM SEMANTICS VERIFIED (live engine) |

**Law 4:** all changes internal to the seam; return types widened additively; zero
caller edits made and zero required — grep before/after confirms call sites unchanged
(`build-unit`'s batch check, ban-player, factory abandon/upgrade, greeting, analytics
reporting all gain live paths with their existing code).

**FID-20260914-004 status → `verified`** (closes on commit per G2).

## Live regression sweep addendum (same session, operator-directed)

Drove every route whose failure branch came alive over real HTTP with per-step DB
assertions (`scripts/sweepHonestBranchRoutes.ts`): build-unit, greeting, factory
upgrade/abandon, ban → login blocked → unban → login restored. Final verdict **23/23,
exit 0**; gates re-proven after the sweep's fixes (tsc 0 · lint 0 · vitest 760/1/0 —
identical baseline).

The sweep's real value — three pre-existing production defects found and fixed:
1. ban-player + clear-flags audit inserts 500'd AFTER applying (legacy Mongo doc keys
   resolve to no mod_log column → NOT NULL columns rendered `default`); fixed to column
   keys, legacy fields preserved in `details`. give-resources/hotkeys verified unaffected
   (log-only usage).
2. player build-unit pushed a plain array as `$push` operand → ONE nested element
   `[[u1,u2,u3]]` in `players.units`, plus full-quantity stamping that would triple-count
   a flat append; fixed to probe-verified `{ $each }` with per-unit `quantity: 1`.
3. `db.collection('units')` unmapped post-pivot → abandon's unit-loss accounting silently
   no-ops (0 units lost, units persist in players.units). Recorded, NOT patched —
   candidate FID.

Sweep-script honesty notes (disclosed, all test-side): the original fixed (3,3) factory
fixture inherited boot-time bot-economy investment history (now picks a verified-empty
cell); the invested assertions initially measured the wrong route's contract
(`/api/player/build-unit` writes no invested — `/api/factory/build-unit` does); units
stack with a `quantity` field (sum, not entries). A debugging probe
(`scripts/probeFactory33.ts`) was deleted after use.

## Commit plan (presented, not executed — G1)

`fix(shim): real update/delete counts, working $pull, true $addToSet (FID-20260914-004)`
staging `lib/mongodb.ts`, `__tests__/lib/shimUpdateSemantics.test.ts`,
`__tests__/lib/fakeDrizzle.ts`, `scripts/probePullObjectOperand.ts`,
`scripts/verifyShimSemanticsLive.ts`, `scripts/cleanupShimVerify.ts`,
`scripts/sweepHonestBranchRoutes.ts`, `app/api/admin/ban-player/route.ts`,
`app/api/admin/anti-cheat/clear-flags/route.ts`,
`app/api/player/build-unit/route.ts`,
`dev/fids/FID-20260914-004-shim-hardening-pull-and-update-counts.md`, `SCOPE.md`, this
record. Closure commit then flips the FID to `closed` with the hash and archives it.
Note: SCOPE.md now carries only FID-004-related deltas (FID-003's were committed in
`e860b4a`/`33f9c5a`), so no surgical staging is required for this stream.

## Closure (G2)

Fix commit **`f21f6f1`** executed 2026-09-14, path-scoped per the plan above. Two
disclosed deviations:

1. The SCOPE note above went stale within the session chain — sessions 006–010 appended
   their own blocks and ledger row 38 to the same file, so SCOPE **was** staged
   surgically: a machine-assembled "HEAD + session-004/005 blocks + ledger row 37" blob
   (`git hash-object` → `update-index --cacheinfo`), sanity-checked to contain zero
   other-stream content. The other streams' SCOPE deltas remain unstaged in the working
   tree for their own commits.
2. The repo's commit-msg hook rejects AI-attribution trailers; the commit carries no
   Co-Authored-By line (matches the FID-003 precedent commits).

Status → `closed`; FID archived to `dev/fids/archive/` with the hash recorded in §8;
CHANGELOG entry added.
