# SESSION-2026-09-15-005 — Deep audit: balance-in-combat, real-garrison floor, vault caps, factory pill

**Date:** 2026-09-15 · **Protocol:** ECHO v0.1.2 single-agent · Automation level 3

**Task source:** operator directives — "we need to do a deep audit" following the live
raid report (VICTORY, 1 round, 0 losses, 452,319,097 energy loot vs tier-2
Zulu_Nightmares_333) + "my base is out of balance … I don't think there is any side
effect to having an out of balance army either" + the Unit Factory owned-count pill
request + the bot-registry resource table.

---

## Audit findings (all code-confirmed)

1. **The balance suite never executed in combat.** The full tier config exists in
   `lib/balanceService.ts` (CRITICAL power ×0.5 / dealt ×0.8 / taken ×1.3 / gathering
   ×0.75 / slots ×0.85 …), but consumers were display-only (StatsPanel via
   combatPowerService, leaderboard via rankingService) — plus the REAL gathering penalty
   in `harvestEstimate`. Proof: the raid's R1 strike was 886,655 = 978,700 − 184,090/2
   EXACTLY (full STR; no ×0.8 dealt, no ×1.3 taken). `slotRegenMultiplier` has zero
   consumers anywhere (dead knob, recorded).
2. **Real regrown garrisons had no weight-class floor.** Sequential resolution + dead
   defenders never counter (FID-20260915-001 rule) ⇒ an overmatched raider kills the
   garrison inside his own strike phase ⇒ counter 0 ⇒ zero-loss free wins at any
   overmatch. Tier-sim Flag 2, proven live.
3. **Bot vaults grew unbounded** (compound +5–15% per growth tick, no ceiling): T1 bots
   at 1.5B after months of uptime; loot = vault × beer-multiplier (×3), uncapped ⇒ the
   452M payout.
4. **Unit Factory cards had no owned counts.**

## Fixes shipped (FID-20260915-004)

- **A (engine seam):** `resolveBattle` computes `calculateBalanceEffects` per side from
  RAW stats (pre-flag/pre-doctrine) and multiplies every strike by dealt × taken.
  Applies to PvP and raids alike; options overrides for tests. CRITICAL raiders now
  strike ×0.8 and absorb ×1.3.
- **B (route):** `reinforceRealGarrison` supplements real garrisons with ephemeral
  T1_BARRICADE (DEF 100) / T1_MILITIA (STR 90) units to the ladder target — the same
  weight-class treatment synthesized bases get. Battle-scoped; never persisted.
- **C (economy):** vault cap = 2× `getResourceRange(spec, tier).max` (spawner source of
  truth) in `botGrowthEngine.regenerateBotResources`; raid loot capped the same way
  (× beer multiplier preserved); one-time `scripts/resyncBotVaults.ts` drained
  **8,796,189,580** from 45 vaults (Devil_Agent 2.12B → 0.2M). Player balances untouched.
- **D (UI):** owned-count pill (×N, top-right, cyan) on every Unit Factory card,
  offensive + defensive maps, from `player.units` totals.

## Verification

- 29/29 battle tests, including the re-pinned incident replay: both sides CRITICAL ⇒
  compose 1.04 ⇒ 4 rounds, 7,836/10,725 ≈ 73.1% (balance deepens the Pyrrhic toll —
  designed anti-glass-cannon tax). Three new FID-004 seam tests pin exact numbers
  (745 / 448 / 932) and the override path.
- Gates: tsc 0 · lint 0 · vitest 797/1 skipped.
- Live E2E 3 legs PASS: laddered tier-6 raid 3 rounds / 2,174 losses (±5 projection
  tolerance — integer-order drift, documented); R1 repulsion; **fame's real raid vs a
  real regrown tier-1 garrison: 2 rounds, 1,050/6,733 losses (15.6%), projection exact**,
  army + loot verified in the DB, position restored.

## Notes for the operator

- fame's army now reflects correct game outcomes across today's verification raids
  (10,725 → 6,733 infantry). Restores are yours to request; they are no longer bugs.
- The ×0.50 "Combat Power" figure remains a display/leaderboard concept (combat uses
  dealt/taken multipliers, which are the mechanically meaningful ones).
