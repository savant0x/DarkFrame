# SESSION-2026-09-16-021 — Work-order execution: -009 D1/D2/D3 + ledger truth-sweep (7 rows closed)

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** full work order from the open-items survey — approvals across #22,
#24, #36, #44, #51, #23, FID-003, and -009 D1/D2/D3 ("fix with the most robust option").

## Headline: the D2 probe caught a LIVE defect the audit had classified latent

`scripts/e2eProtectionUntilRoundTrip.ts` (run before the migration) measured a **+4h
read-back shift** on this EDT host: node-pg writes the UTC instant as a naive literal but
parses naive literals back as *local time* — every protection window on a non-UTC host
expired 4h late. F2's "lossless round-trip" claim was wrong; the FID carries the erratum
and F2 is reclassified latent → live HIGH. The `timestamptz` migration
(`0032_protection_until_timestamptz.sql`, applied via `scripts/applyMigration0032.ts` with
`USING (col AT TIME ZONE 'UTC')`) fixed it: post-migration round-trip delta **0 ms**.

## -009 D1/D2/D3 executed (FID → verified)

- **D1:** `movementService` + `wmd/targetingValidator` now call the canonical
  `protectionActive`; targetingValidator's message unified to `PROTECTION_REFUSAL_REASON`
  (parity across all six refusal sites).
- **D2:** schema column → `{ withTimezone: true }`; migration applied live; probe driver
  proves +14,400,000 ms → 0 ms.
- **D3:** dead no-auth `createPlayer` deleted (zero callers re-verified immediately prior);
  `createPlayerWithAuth` is the sole issuance path.
- Gates: tsc 0 · eslint 0/0 · vitest 919+1skip. 14/14 predicate+targeting pins green.

## Ledger truth-sweep (survey follow-through)

Five "open" rows were actually **stale** — verified closed, dispositions rewritten with
evidence: **#22** (all six admin endpoints rebuilt 2026-09-04, headers cite SCOPE #22),
**#24** (all nine sub-items fixed: registry aliases cover the collapsed live surface,
aggregate evaluator matches its 4 callers' stages, chat id truncated, respawn job
registered, zero ts-nocheck), **#36** (lib/ down to one audited socket.io emit
suppression, documented load-bearing), **#44** (regen-curve + hoarder-cap shipped —
`getVaultCap()` at botService:304, acceptance gate passed), **#51** ("Partial" label
stale — all five items closed via `f5d5b3b`/`c63a167`). **#23** closed (PORT=0
normalization present at server.ts:49, rides the parallel estate's server.ts batch).
**FID-003 closed + archived** (Option B already implemented via -004; harvest stands
no-gate per the ratified policy). Rows 52–54 were already closed (survey erratum —
dispositions embedded in description cells).

## Net: the SCOPE ledger now has zero genuinely-open operator-decision rows.

## Commit plan (G1 — presented, not executed)

Batch A (this work order's code+docs):
`git add lib/movementService.ts lib/wmd/targetingValidator.ts lib/playerService.ts
lib/db/schema/players.ts lib/db/migrations/0032_protection_until_timestamptz.sql
scripts/e2eProtectionUntilRoundTrip.ts scripts/applyMigration0032.ts
dev/fids/FID-20260916-009-window-mechanics-audit.md dev/fids/archive/FID-20260916-003-protection-forfeit-edges-debate.md
dev/session-summaries/SESSION-2026-09-16-021.md SCOPE.md CHANGELOG.md` →
`fix: protection window expiry was +offset on non-UTC hosts — timestamptz + canonical predicate everywhere (FID-20260916-009)`

Batch B (already staged-ready from session 019): the -007/-008 closures.
Batch C: the -009 audit doc batch if separated — folded into A here by the operator's
combined directive.
