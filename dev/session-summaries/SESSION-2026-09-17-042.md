# SESSION-2026-09-17-042 — Next.js 16 deprecation sweep (beyond middleware)

**Date:** 2026-09-17 · **Protocol:** ECHO v0.1.2 single-agent (16 laws) · **Branch:** `main`
**Operator directive:** sweep the codebase for other Next.js 16 deprecations beyond
middleware — APIs, config keys, and route conventions that will break on upgrade.

## Method

The deprecation inventory was derived from the **installed next@16.3.5 dist** — every
`warnOnce`/`errorOnce`/`Error`/`@deprecated` string under `dist/{server,build,lib,
client,api}` — rather than from blog posts, so the class list is exactly what this
shipping version warns about. 18 classes; each swept against `app/ components/ lib/
hooks/ scripts/ server.ts next.config.js package.json`.

## Verdict: clean — middleware→proxy was the last active deprecation

All 18 classes **absent or already remediated** (full table with per-class evidence in
`dev/audits/NEXT16-DEPRECATION-SWEEP-2026-09-17.md`):

- Segment configs: no `runtime: 'edge'`, no `preferredRegion`; the proxy carries no
  `runtime` key since session 038's smoke finding.
- APIs: no `request.ua`, no `next/legacy/image`, no sync `cookies()/headers()`, no
  `next/head`, no `@next/font` (only supported `next/font/google`), no `unstable_*`.
- Config keys: no `images.domains`, no `skipMiddlewareUrlNormalize`, no `eslint` key,
  no `icss`/`target` — next.config.js is fully clean.
- Conventions: no `legacyBehavior`, no AMP, no pages-router, no `next lint`
  (`"lint": "eslint ."`), and `server.ts` uses only the supported custom-server pair
  (`app.prepare()` + `getRequestHandler()`) — never the removed `app.render*`.

## Recorded context (not code findings)

- exFAT/Turbopack constraint stands: scripts pin `--webpack`, so Turbopack-only
  warnings can't surface in-repo (documented in the audit for future upgrade runs).
- Environment strays outside the repo (`C:\Users\spenc\package{,-lock}.json`) remain
  an operator call.
- CHANGELOG `[0.0.5]`-below-`[0.0.4]` ordering defect noted in the audit; the
  parallel session's entry, not reordered here.

## Bookkeeping

- Audit artifact: `dev/audits/NEXT16-DEPRECATION-SWEEP-2026-09-16.md` → corrected on
  write to `NEXT16-DEPRECATION-SWEEP-2026-09-17.md` (dated today).
- SCOPE row #79 appended (`Closed` — audit citation).
- Zero source modifications → no gates re-run (session 041's green gates stand).

## Staging note (worktree residue, all mine, uncommitted)

- Session-040 finding batch: FID-20260917-005, errata ×2, SCOPE row 78, session 040.
- Proxy migration: `proxy.ts` (+`middleware.ts` deletion), session 038.
- Tracing-root fix: `next.config.js`, session 039.
- This sweep: audit + SCOPE row 79 + session 042.

Footer-free commit plan: four single-concern doc/infra batches in the order above,
each with an owner-checked staged-set probe (SCOPE rows 78+79 share one file — stage
the sweep batch's row by index reconstruction from HEAD + row 79 only).
