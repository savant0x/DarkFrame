# SESSION-2026-09-16-019 — FID-20260916-007/-008 closed; protection parity track complete

**Date:** 2026-09-16 · **Protocol:** ECHO v0.1.2 single-agent (amended) · **Branch:** `main`
**Operator directive:** after the implementation commits landed, close -007 and -008 with
their hashes in §8, archive both FIDs, and add the CHANGELOG entry.

## Closures

| FID | G2 hash | End state |
| --- | --- | --- |
| FID-20260916-007 (spy sabotage) | `cf7437a` | §8 hash recorded, status `closed`, archived |
| FID-20260916-008 (factory void + battle route) | `e9bf162` | §8 hash recorded, §8 gates ticked, status `closed`, archived |

Both FIDs' §8 G1/G8 rows are marked *superseded* with an honest note: the doc-batch
messages from the loop-complete phase were never used — the implementation commits folded
code + spec + sessions + SCOPE row into one commit each (`cf7437a`/`e9bf162`), so the
original staging plans describe a history that did not happen.

## CHANGELOG

New newest-first section under `[0.0.1]` — "FID-20260916-006/-007/-008: protection parity
completed across all PvP surfaces (closed, commits `cf7437a`/`e9bf162`)": the -006
disposition (D1/D2 ratified open), the -007 sabotage repair + 4th void site + disclosed
varchar(24) fix, the -008 `pvpCapture` void + latent-route seams, and the verification
totals (11 pins, 4/4 + 6/6 live probes, 919+1skip).

## SCOPE

Rows #66/#67 → `Closed (commit … — implemented + verified; archived)`. Two transient sed
issues on the way, both caught and corrected in the same turn: backticks in the
replacement text were command-substituted by an unquoted double-quoted sed (hashes
briefly vanished; restored literal via single-quoted line-scoped sed), and the
line-number variables were re-derived rather than assumed after edits shifted rows.

## Ledger state

`dev/fids/` root now holds only the two intentionally-open FIDs (-003 debate,
-006 audit with D1/D2). Archive holds -001, -002, -004, -005, -007, -008.

## Commit plan (G1 — presented, not executed)

```bash
git add CHANGELOG.md SCOPE.md dev/fids/FID-20260916-007-spy-sabotage-protection-enforcement.md \
  dev/fids/FID-20260916-008-factory-void-battle-route.md \
  dev/session-summaries/SESSION-2026-09-16-019.md
# rename detection carries the archive moves
git commit -m "docs(fid): close FID-20260916-007/-008 — hashes recorded, FIDs archived, CHANGELOG entry"
```
