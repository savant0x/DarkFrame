# SESSION-2026-09-15-001 — Battle-annihilation FID + conn-pool fix + FID-008/009 implementations

**Date:** 2026-09-15 · **Protocol:** ECHO v0.1.2 single-agent

**Task source:** live incident report (operator) — beer-base raid ended DRAW with the
army wiped to 0 and a flood of `Failed query` errors; then the directive to "address all
in full": finish FID-008 Phases 1–3, the MySQL-era SQL sweep, FID-009 Phases A/B, the
census regression test, and the per-stream commits.

---

## What happened (live incident)

- **The wipe is real and it is a battle-resolution defect.** `BATTLE-1789450926316-w6eb25`
  (BASE_RAID vs Hex_Lord_655, 1 round): attacker 10,725 units / 1,072,500 STR dealt
  678,275 damage (formula-verified: STR − DEF/2) into the garrison's 76,110 HP; the
  garrison's counter (252,200) hit the attacker's 107,250 HP. **Both HP pools zeroed in
  the same round → outcome DRAW → `applyBattleResults` applied FULL casualties to both
  sides** — 10,725/10,725 attacker units lost, 6,673/6,673 defender units lost. The
  account wipe (`players.units` = 0, totals 0) was that model executing faithfully.
- **The error flood was connection exhaustion, not corruption.** Server log:
  `EMAXCONNSESSION … pool_size: 15` (Supabase Supavisor session pooler) at
  `getPlayerByEmail` → login failed → WebSocket retry storm. Root: the dev server's
  module-scope pg Pool is not hot-reload-safe; orphaned pool generations kept idle
  clients connected until the provider cap tripped (8 idle clients observed post-incident;
  0 ungranted locks — no deadlock).
- **The defender needed no restore:** Hex_Lord_655 is a bot (`is_bot=1`); the beer-base
  scheduler regrows garrisons (observed live: 20 → 132 units within the hour).

## FID-20260915-001 (opened + converged 2 loops; implemented + live-verified)

- **Phase 1 — sequential resolution** (`lib/battleService.ts`): attacker strikes first;
  a dead defender never counter-attacks; casualties derive from HP actually deducted
  (clamped), never raw overkill; a side destroyed with its pool records its remaining
  units as casualties (no phantom armies); the 100-round cap is a REPELLED raid
  (`DefenderWin`), not a casualty-charging Draw. Draw survives only as the
  empty-input guard.
- **Phase 2 — conn-pool hardening** (`lib/db/connection.ts`): pool cached on
  `globalThis` so hot-reload generations share one process-wide pool.
- **Phase 4 — restore:** `scripts/restoreFameArmy.ts` folded the battle row's exact
  10,725-entry snapshot back to the canonical quantity-folded PlayerUnit shape
  (totals verified 1,072,500 STR) and restored **only because the army was empty**
  (guarded against double-restore). **fame's army is back.**
- **Phase 3 (recorded, follow-up):** rebalance — HP scale (flat 10/15 vs damage in the
  10⁵–10⁶ range makes every battle 1-round) and per-unit HP proportional to quality.
- Tests: `__tests__/lib/battleResolution.test.ts` (7 — incl. the incident replay);
  live E2E `scripts/e2eBattleFix.ts` through the production raid route: the exact
  old-DRAW boundary matchup (4,755 damage vs a 1,630 HP pool) → `ATTACKER_WIN`,
  zero attacker losses, cleanup residual 0.

## FID-20260914-008 Phases 1–3 (implemented + live-verified; addendum in FID)

Doctrine bonuses real at the power seam, the combat seam (inside the existing bonus
stack), and all three cost sites; server-side mastery (+10/+25 hooks; exploit POST now
admin-only, live-probed 403); respec ledger entry; nav entry; doc drift; admin remnant.
Live probe green end-to-end. **Finding recorded:** the 48h respec cooldown anchors only
to `lastRespecAt` (choose never sets it) — first respec is always immediately available;
recommendation to anchor at choose in a follow-up.

## MySQL-era SQL sweep: zero live remnants

Full pattern sweep (JSON_* friends, GROUP_CONCAT, ON DUPLICATE KEY, INSERT IGNORE,
LIMIT x,y, backticks): every hit is a historical comment or valid pg syntax
(`NOW() - INTERVAL '24 hours'` is pg-legal). Only defect: two stale `JSON_CONTAINS`
doc-comments in `dmService.ts` — corrected.

## FID-20260914-009 Phases A + B (implemented + live-verified; addendum in FID)

- **Phase A:** abandon's obsolete units-accounting removed (unmapped `units`);
  cacheWarming territory branch removed (unmapped `clan_territories`, zero-reader keys).
- **Phase B:** migration 0031 `player_level_history` (applied, information_schema-verified)
  + `playerLevelHistory` schema export + service rewritten on drizzle (API intact) +
  snapshot cron fixed (phantom `lastActive` filter → real `lastLoginDate`; `_id` →
  username keys) + retention on the mapped `battleLogs` with honest counts. Live:
  snapshot → growth → prediction → cleanup all verified on the real DB.

## Census regression gate

`__tests__/lib/collectionCensus.test.ts` imports the machine census and fails on any
unmapped production collection name — with self-validation guards (scan liveness,
registry spot-checks, retired aliases must stay retired). Part of the full suite.

## Gates

tsc 0 · eslint 0 · vitest **793 passed / 1 skipped / 0 failures** (baseline 769 + 24 new).

## Commits (this session's streams, path-scoped per G1/G2)

Executed after this record: FID-005 → FID-006 → FID-007 → FID-008 → FID-009 →
FID-20260915-001, each `fix(...)`/`feat(...)` + a `docs(protocol)` closure commit
carrying the FID's hash. The other session's WIP (FID-20260914-001/002, jsdom FID,
battle-logs UI work, assets) remains untouched and uncommitted.
