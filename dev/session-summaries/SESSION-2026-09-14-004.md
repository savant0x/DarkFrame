# SESSION-2026-09-14-004 — FID-20260914-004 converged (shim hardening spec)

**Date:** 2026-09-14 · **Protocol:** ECHO v0.1.2 single-agent

**Task source:** operator directive — "Build and converge the shim-hardening FID: replace
the dead `$pull` SQL with the verified `jsonb_agg` rewrite and add conditional-update
semantics to the compat seam."

---

## Intent (logged before work — Law 8)

Convert the two follow-ups banked by FID-20260914-003 (dead `$pull` SQL; honest
conditional-update counts) into a converged FID per the template, applying the
Ground-Truth rule (verify every defect claim against source before specifying), the
Perfection Loop (RED → GREEN → double audit → zero actionable improvements), and the
G1 default (no implementation, commit plan presented only).

## Files created / modified

| File | Change |
| ---- | ------ |
| `dev/fids/FID-20260914-004-shim-hardening-pull-and-update-counts.md` | NEW: converged FID specifying (a) the probe-verified `jsonb_agg` `$pull` rewrite, (b) `.returning()`-based honest counts for `updateOne`/`updateMany`/`deleteOne`/`deleteMany`/`bulkWrite` (upsert branches included per loop-2 refinement), (c) containment-guarded `$addToSet`. Status `converged`; §7 not-started, gated on operator go-ahead. |
| `SCOPE.md` | Ledger row #37 (the seam-hardening item, referencing the FID); header Last-updated line; session 004 block. |

## Key findings (all source-verified this session)

1. **`$pull` is a latent guaranteed-500**: the shim emits `jsonb - jsonb` (probe-proven
   invalid on this engine); zero live callers remain since FID-20260914-003 removed the
   last — but `MongoUpdate.$pull` still advertises the crashing operator to the next
   caller.
2. **Unconditional counts lie to seven live branches**: `updateOne`/`updateMany`/
   `deleteOne`/`deleteMany`/`bulkWrite` always report `1`, so the `modifiedCount === 0`
   failure branches in ban-player, factory abandon/upgrade, build-unit (batch integrity
   — `< factoryUpdates.length` can never trip), and greeting are dead code, and three
   `deletedCount` reporting sites (beerBaseAnalytics, playerHistoryService,
   beerBaseService) report fiction. FID-003 had to route around this via
   `findOneAndUpdate`; this FID removes the need.
3. **`$addToSet` is push in disguise** (unconditional `\|\|` append; one live caller,
   `tierUnlockService.ts:105` — duplicate tier entries possible). Found while
   enumerating the operator family; one-line fix alongside the others.
4. **Cleared**: the insert-if-no-match block in `updateOne` is `upsert`-gated — no
   phantom-row hazard; documented as finding 7 so the implementation audit does not
   re-flag it.

## Perfection Loop record

- **Loop 1**: Method 1 (gates re-run on the document-only session: tsc 0 · eslint 0 ·
  vitest 747/1/0 — zero code drift) + Method 2 (every file:line claim re-read, including
  the seven branch sites and the upsert gate) → PASS.
- **Loop 2 (deep audit)**: five adversarial probes — `.returning()` composition with
  SQL-fragment payloads (cleared), honest counts vs the seven branches' match-semantics
  (cleared — all check `=== 0`/`< expected`), `jsonb_agg` NULL-element semantics
  (1 probe hardening applied: JSON-null case added to the planned probe extension),
  `$addToSet` scalar/object containment wrapping (spec tightened to wrap both), upsert
  under honest counts (1 GREEN refinement applied: insert branches also counted).
- **Termination**: 2 of 10 iterations; delta < 2%; no oscillation; zero further
  actionable improvements → `converged`. Closure re-read caught and fixed one formatting
  defect of my own (a stray non-word token) — the converging artifact is itself clean.

## Verification evidence (fresh tool output)

| Gate | Result |
| --- | --- |
| `npx tsc --noEmit` | **exit 0** |
| `npm run lint` | **exit 0, zero findings** |
| `npm run test:ci` | **75 passed / 1 skipped (76 files) · 747 passed / 1 skipped (748 tests) · 0 failures** (unchanged baseline — document-only session) |

## Disclosure

- A third defect (`$addToSet`) was added to the FID beyond the operator's two named
  items — same operator family, discovered during the census, one-line fix, disclosed
  here and in SCOPE #37.
- No code was changed in this session (probe script and shim untouched; the probe
  extension is specified in the FID's §5, to ship with implementation).
- Commit presented, not executed (G1): `docs(shim): FID-20260914-004 shim-hardening
  spec (converged)` staging `dev/fids/FID-20260914-004-*.md`, `SCOPE.md`, this record.
