# Session Record — SESSION-2026-09-02-005

**Date:** 2026-09-02
**Protocol:** ECHO v0.1.2 single-agent (`dev/echo-v0.1.2-single-agent.md`)
**Task:** Create and converge the DB-direction FID (finish Postgres/Supabase pivot vs revert to MariaDB).
**Scope authority:** `SCOPE.md` → "Session 2026-09-02 (005)".
**Deliverable:** `dev/fids/FID-20260902-001-db-direction-postgres-pivot-vs-mariadb-revert.md` — FID-bound
execution: no implementation this session; the FID converges and the direction decision is presented.

---

## Intent (logged before implementation — Law 8)

The 2,043 type errors and the `blocked` mongodb.ts seam (SESSION-2026-09-02-004) all trace to one open
decision: the tree is half Postgres (driver, `DATABASE_URL`, `supabase/` init) and half MariaDB (14+1
MySQL-dialect schema files, `drizzle.config.ts`). The FID must specify both directions completely enough
that choosing one becomes an informed, low-risk decision, then converge through the Perfection Loop.
Complex-task routing applies (>75 lines, novel architecture, cross-cutting) → FID-bound: RED → GREEN →
AUDIT → `converged`; code only after the operator picks the direction.

## Facts established (RED evidence, all reproducible)

| Fact | Evidence |
| ---- | -------- |
| 14 schema files MySQL-dialect + 15th file `lib/migrations/factorySlots.ts`; 57 `mysqlTable` defs; 68 `json(`; 0 `bigint(` | grep counts over `lib/db/schema/*.ts` + code_search |
| Connection layer is Postgres-only: `drizzle-orm/node-postgres`, `DATABASE_URL=postgresql…` | `lib/db/connection.ts` read; env name-prefix probe |
| `drizzle.config.ts` targets MariaDB (`dialect: 'mysql'`, SkySQL `DB_*` creds) | file re-read 2026-09-02 |
| Supabase pivot was *initialized*: `supabase/config.toml` with `project_id = "DarkFrame"`, local Postgres stack (54321), CLI `.temp` state | `find supabase -type f` + config read (names only) |
| No `@supabase/supabase-js` client anywhere (CLI only, devDependencies) | repo grep → 0 src matches |
| Zero runtime mysql2 driver imports (dmService's hit is a doc comment at :827) | grep `drizzle-orm/mysql2` → 1 file, comment-only |
| 6 files → **corrected by audit to 7 files, 17 executable fragments** carry raw MySQL-dialect SQL: `JSON_CONTAINS` ×14 code (authMiddleware 1, territoryService 3, dmService 4, **clanAllianceService 6 — missed in the first sweep, caught by the audit**), `ON DUPLICATE KEY UPDATE` (researchPointService:364), `DATE_FORMAT` ×2 (clanActivityService) | grep with code-vs-comment filter |
| 117 files consume `@/lib/db`; 77 files consume the `lib/mongodb` compat layer | grep -rl counts |
| `lib/db/migrations` exists but is EMPTY — no drizzle migrations ever generated | `ls` + `test -d` |
| `mongodb` package still in deps; 5 script/seed files import `from 'mongodb'` (wmd.seed, cleanup-duplicate-users, createIndexes, initialize-wmd-players, cleanup-beer-bases) | grep list |
| April state was genuinely green: FID-20260403-001 recorded "0 TypeScript errors" post-MariaDB-migration | `dev/fids/FID-20260403-001.md` read |

## Deliverable

`dev/fids/FID-20260902-001` — status driven `created → analyzed → converged` in this session with both
GREEN options specified, recommendation recorded, and audit evidence pasted.

## Evidence

### AUDIT (Perfection Loop) — executed 2026-09-02

**Method 1 (claim re-verification, commands re-run):** schema files 14 ✓ · mysqlTable defs 57 ✓ ·
mysql2 runtime imports 0 ✓ · `@/lib/db` consumers 117 ✓ · compat consumers 77 ✓ ·
`DATABASE_URL=postgresql…` ✓ · `supabase/config.toml` present ✓ · `lib/db/migrations` empty (0 entries) ✓ ·
`drizzle.config.ts` dialect 'mysql' ✓ · tsc 2,043 (standing baseline) ✓.

**Discrepancy found and corrected (1st audit pass):** `JSON_CONTAINS` undercounted — 18 total occurrences,
**14 executable** (not the ~9–12 estimated), and `lib/clanAllianceService.ts` (6 fragments, lines
166/167/178/179/605/606) was **missing entirely** from the first sweep. SELF-CORRECT applied: FID RED row 10,
both GREEN tables, decision matrix, and mechanics note updated; re-run → 0 remaining discrepancies.
**2nd re-read pass found 2 stale decision-matrix cells** ("6 SQL files", "10 fragments") → corrected.
Convergence criteria met (delta 0% on final pass; no oscillation; 2 iterations < hard-stop 10).

**Method 2 (manual re-read):** FID read 0-EOF twice (post-write, post-correction); template conformance
checked field-by-field (metadata, RED table, Five Questions, dual GREEN, audit record, decision gate,
planned closure); no attribution fields.

**Outcome: PASS → status `converged`.** Implementation gated on the operator picking Option A or B
(presented as a blocking step; the decision gate is recorded in FID §6).

## Step Status Ledger

| Step | Status |
| ---- | ------ |
| Scope + intent logged | implemented |
| RED evidence sweep (reproducible) | implemented |
| Write FID-20260902-001 (RED+GREEN both options) | implemented |
| AUDIT (two methods) → status `converged` | implemented (2 discrepancies found & corrected: SQL-fragment undercount + stale matrix cells) |
| Present direction decision to operator (blocking) | implemented (presentation made; decision pending — FID remains converged, not implemented) |
