# SESSION-2026-09-15-004 — Endgame pacing: cap falsified, tier ladder shipped + live-verified; fame's real raid

**Date:** 2026-09-15 · **Protocol:** ECHO v0.1.2 single-agent · Automation level 3

**Task source:** operator directives — "Tune the endgame raid difficulty: raise
GARRISON_SIZE_CAP and re-run the rebalance E2E to prove high-tier bases now fight
multi-round." + "Check my account and run a real raid in the game to feel the new battle
pacing firsthand."

---

## Intent (Law 8)

Test the premise before implementing it. If the cap sweep disproves the operator's
mechanism, present the measured alternative and ship the smallest knob that produces the
requested pacing. The fame raid must be a REAL raid through the production route with
full account-integrity assertions — not a simulation.

## What the loop falsified

**`GARRISON_SIZE_CAP` cannot tune raid difficulty.** Swept cap × tier-multiplier on the
real engine (Matrix 4): cap 60/150/300/600 → byte-identical outcomes at every multiplier.
Algebra: the weight floor is `RATIO × attackerSTR` distributed across the units — the
unit count only changes per-unit stats (and casualty optics), never the pool or the
counter. The knob that governs difficulty is the floor's magnitude → tier ladder.

## What shipped (FID-20260915-003)

- `GARRISON_TIER_MULT = { 1: 1.0, 2: 1.05, 3: 1.1, 4: 1.2, 5: 1.35, 6: 1.5 }` on the
  weight floor in `app/api/combat/attack/route.ts` (curve selected from measured sweeps:
  ×1.4–1.5 = the multi-round band for endgame raiders).
- **Live find:** tier came from the `b[WMSEUL]` username marker — matches nothing on the
  live map (name-shaped bot usernames) → every live bot was tier 1, for the ladder AND
  tier-scaled XP/RP. `resolveBotTier` reads canonical `bot_config.tier` (spawner field,
  `botTierResync`-maintained), marker fallback.

## Live verification (production route, port 3000 after the old 3002 server died)

- Leg A: 2200×STR100 endgame raider (level 45, gap 20 = no protection) vs tier-6 fresh
  base (`Flag_Bearer_1027`, garrison fixture-emptied then snapshot-restored):
  **3 rounds, 2,090/2,200 losses, 110 survivors intact in the DB.**
- Leg B: 1-unit raider → R1 repulsion, garrison unscratched. Never a DRAW.
- Leg C (the operator's account): fame (10,725 units, 1.07M STR) raided tier-3
  `Rusted_Depot` → **2 rounds, 1,514,950 damage, 938 losses (8.7%), 9,787 survivors
  intact, loot credited, base removed + tile released (route win path)**. Home position
  restored.
- Driver corrections found live: fixture must snapshot `units` AND `total_defense`
  (partial restore healed); leg C needs its own `finally` for the position restore.

## Gates

tsc 0 · lint 0 · vitest 794/1 skipped · full E2E PASS (3 legs).
